<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ModernWpf.Gallery.Controls">

    <controls:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

    <Style x:Key="SelectionTextBox" TargetType="TextBox">
        <Setter Property="Foreground" Value="{DynamicResource TextControlForeground}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="KeyboardNavigation.IsTabStop" Value="False" />
    </Style>

    <Style TargetType="{x:Type controls:ControlExample}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ControlExample}">
                    <Grid AutomationProperties.AutomationId="{Binding HeaderText, RelativeSource={RelativeSource TemplatedParent}, StringFormat=GallerySample_{0}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Margin="0,0,0,10"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Style="{DynamicResource BodyStrongTextBlockStyle}"
                            Text="{TemplateBinding HeaderText}"
                            Visibility="{TemplateBinding HeaderText, Converter={StaticResource NullToVisibilityConverter}}" />

                        <Border
                            Grid.Row="1"
                            Padding="16"
                            Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1,1,1,0"
                            CornerRadius="8,8,0,0">
                            <ContentPresenter Content="{TemplateBinding ExampleContent}" />
                        </Border>

                        <Expander
                            x:Name="SourceCodeExpander"
                            Grid.Row="2"
                            AutomationProperties.Name="{Binding HeaderText, RelativeSource={RelativeSource TemplatedParent}, StringFormat=View Source Code for {0}}"
                            Header="Source code">
                            <StackPanel>
                                <StackPanel x:Name="XamlCodeBlock">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock
                                            Grid.Column="0"
                                            Margin="0,0,0,5"
                                            FontWeight="Bold"
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Text="XAML" />

                                        <Button
                                            Grid.Column="1"
                                            Padding="8"
                                            AutomationProperties.Name="Copy XAML Code"
                                            Command="ApplicationCommands.Copy"
                                            CommandParameter="Copy_XamlCode"
                                            ToolTipService.ToolTip="Copy to clipboard">
                                            <Grid>
                                                <TextBlock
                                                    x:Name="CopyGlyphTextBlock"
                                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                    Text="&#xE8C8;" />
                                                <TextBlock
                                                    x:Name="SuccessGlyphTextBlock"
                                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                    Opacity="0"
                                                    Text="&#xE73E;" />
                                            </Grid>
                                            <Button.Triggers>
                                                <EventTrigger RoutedEvent="Button.Click">
                                                    <BeginStoryboard>
                                                        <Storyboard BeginTime="00:00:00">
                                                            <DoubleAnimation
                                                                Duration="0:0:0.333"
                                                                Storyboard.TargetName="CopyGlyphTextBlock"
                                                                Storyboard.TargetProperty="Opacity"
                                                                To="0" />
                                                            <DoubleAnimation
                                                                BeginTime="0:0:0.333"
                                                                Duration="0:0:0.666"
                                                                Storyboard.TargetName="SuccessGlyphTextBlock"
                                                                Storyboard.TargetProperty="Opacity"
                                                                To="1" />
                                                            <DoubleAnimation
                                                                BeginTime="0:0:2"
                                                                Duration="0:0:0.01"
                                                                Storyboard.TargetName="SuccessGlyphTextBlock"
                                                                Storyboard.TargetProperty="Opacity"
                                                                To="0" />
                                                            <DoubleAnimation
                                                                BeginTime="0:0:2.1"
                                                                Duration="0:0:0.01"
                                                                Storyboard.TargetName="CopyGlyphTextBlock"
                                                                Storyboard.TargetProperty="Opacity"
                                                                To="1" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Button.Triggers>
                                        </Button>
                                    </Grid>
                                    <TextBox
                                        AutomationProperties.Name="XAML Source Code"
                                        Style="{StaticResource SelectionTextBox}"
                                        Text="{TemplateBinding XamlCode}" />
                                </StackPanel>

                                <Border
                                    x:Name="CodeBlockDivider"
                                    Margin="0,20"
                                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" />

                                <StackPanel x:Name="CSharpCodeBlock">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock
                                            Margin="0,0,0,5"
                                            Style="{DynamicResource BodyStrongTextBlockStyle}"
                                            Text="C#" />

                                        <Button
                                            Grid.Column="1"
                                            Padding="8"
                                            Command="ApplicationCommands.Copy"
                                            CommandParameter="Copy_CSharpCode"
                                            FocusManager.IsFocusScope="True"
                                            ToolTipService.ToolTip="Copy to clipboard">
                                            <TextBlock
                                                FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                Text="&#xE8C8;" />
                                        </Button>
                                    </Grid>
                                    <TextBox
                                        AutomationProperties.Name="CSharp Source Code"
                                        Style="{StaticResource SelectionTextBox}"
                                        Text="{TemplateBinding CSharpCode}" />
                                </StackPanel>
                            </StackPanel>
                        </Expander>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="XamlCode" Value="{x:Null}">
                            <Setter TargetName="XamlCodeBlock" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="CodeBlockDivider" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="CSharpCode" Value="{x:Null}">
                            <Setter TargetName="CSharpCodeBlock" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="CodeBlockDivider" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="XamlCode" Value="{x:Null}" />
                                <Condition Property="CSharpCode" Value="{x:Null}" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="SourceCodeExpander" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="CodeBlockDivider" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
