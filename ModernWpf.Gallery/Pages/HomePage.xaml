<UserControl
    x:Class="ModernWpf.Gallery.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="900"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="GalleryCard.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid
        AutomationProperties.AutomationId="ContentRootGrid"
        Margin="-24,-16,-24,12"
        Style="{StaticResource GalleryPageRootStyle}">
        <ScrollViewer>
            <Grid x:Name="HomeContentGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border
                    Grid.RowSpan="2"
                    CornerRadius="8,0,0,0">
                    <Border.Background>
                        <ImageBrush
                            ImageSource="pack://application:,,,/ModernWpf.Gallery;component/Assets/win11-dashboard.light.png"
                            Stretch="UniformToFill" />
                    </Border.Background>
                    <Border.OpacityMask>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="Black" Offset="0.6" />
                            <GradientStop Color="Transparent" Offset="0.9" />
                        </LinearGradientBrush>
                    </Border.OpacityMask>
                </Border>

                <Border
                    Height="220"
                    BorderThickness="0,0,0,1"
                    CornerRadius="8,0,0,0">
                    <StackPanel
                        Margin="36,48,0,0"
                        TextElement.Foreground="Black"
                        VerticalAlignment="Top">
                        <TextBlock
                            x:Name="HeroVersionText"
                            Margin="0,0,0,2"
                            Foreground="Black"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            Text=".NET 10" />
                        <TextBlock
                            x:Name="HeroTitleText"
                            Margin="0,0,0,8"
                            Foreground="Black"
                            Style="{StaticResource TitleLargeTextBlockStyle}"
                            Text="WPF Gallery" />
                        <Border
                            MaxWidth="300"
                            HorizontalAlignment="Left"
                            Background="Transparent"
                            CornerRadius="8">
                            <TextBlock
                                MaxWidth="300"
                                Padding="0,8,12,8"
                                HorizontalAlignment="Left"
                                Foreground="Black"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                Text="A collection of controls, guidelines and samples to build great WPF applications"
                                TextAlignment="Left" />
                        </Border>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1">
                <ScrollViewer
                    x:Name="RootScrollViewer"
                    Margin="24,0,0,0"
                    HorizontalScrollBarVisibility="Hidden"
                    SizeChanged="OnRootScrollViewerSizeChanged"
                    VerticalScrollBarVisibility="Disabled">
                <StackPanel
                    x:Name="TilesPanel"
                    Orientation="Horizontal">
                    <Button
                        AutomationProperties.Name="Getting started"
                        Click="OnHeaderTileClick"
                        Margin="6,6,12,6"
                        Style="{StaticResource GalleryHeaderTileButtonStyle}"
                        Tag="https://learn.microsoft.com/windows/apps/get-started/">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="56" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.RowSpan="2"
                                Margin="-12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontFamily="{StaticResource SymbolThemeFontFamily}"
                                FontSize="16"
                                Text="&#xE8A7;" />
                            <Image
                                Width="48"
                                Height="48"
                                HorizontalAlignment="Left"
                                Source="pack://application:,,,/ModernWpf.Gallery;component/Assets/AppIcons/WPFGallery_48px.png" />
                            <StackPanel Grid.Row="1" Margin="0,16,0,0">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="18"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="Getting started" />
                                <TextBlock
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="An overview of app development options, tools, and samples." />
                            </StackPanel>
                        </Grid>
                    </Button>
                    <Button
                        AutomationProperties.Name="Windows design"
                        Click="OnHeaderTileClick"
                        Style="{StaticResource GalleryHeaderTileButtonStyle}"
                        Tag="https://learn.microsoft.com/windows/apps/design/">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="56" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.RowSpan="2"
                                Margin="-12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontFamily="{StaticResource SymbolThemeFontFamily}"
                                FontSize="16"
                                Text="&#xE8A7;" />
                            <Image
                                Width="56"
                                Height="56"
                                HorizontalAlignment="Left"
                                Source="pack://application:,,,/ModernWpf.Gallery;component/Assets/HomeHeaderTiles/Header-WindowsDesign.png" />
                            <StackPanel Grid.Row="1" Margin="0,16,0,0">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="18"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="Windows design" />
                                <TextBlock
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="Design guidelines and toolkits for creating native app experiences." />
                            </StackPanel>
                        </Grid>
                    </Button>
                    <Button
                        AutomationProperties.Name="WPF GitHub"
                        Click="OnHeaderTileClick"
                        Style="{StaticResource GalleryHeaderTileButtonStyle}"
                        Tag="https://github.com/dotnet/wpf">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="56" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.RowSpan="2"
                                Margin="-12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontFamily="{StaticResource SymbolThemeFontFamily}"
                                FontSize="16"
                                Text="&#xE8A7;" />
                            <Viewbox Height="52" Margin="-20,0,0,0" HorizontalAlignment="Left">
                                <Path
                                    Data="{StaticResource GitHubIconGeometry}"
                                    Fill="{DynamicResource TextFillColorPrimaryBrush}" />
                            </Viewbox>
                            <StackPanel Grid.Row="1" Margin="0,16,0,0">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="18"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="WPF GitHub" />
                                <TextBlock
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="A robust UI framework for your desktop applications." />
                            </StackPanel>
                        </Grid>
                    </Button>
                    <Button
                        AutomationProperties.Name="Code samples"
                        Click="OnHeaderTileClick"
                        Style="{StaticResource GalleryHeaderTileButtonStyle}"
                        Tag="https://github.com/microsoft/WPF-Samples">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="56" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.RowSpan="2"
                                Margin="-12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontFamily="{StaticResource SymbolThemeFontFamily}"
                                FontSize="16"
                                Text="&#xE8A7;" />
                            <Viewbox Height="52" Margin="-20,0,0,0" HorizontalAlignment="Left">
                                <Path
                                    Data="{StaticResource GitHubIconGeometry}"
                                    Fill="{DynamicResource TextFillColorPrimaryBrush}" />
                            </Viewbox>
                            <StackPanel Grid.Row="1" Margin="0,16,0,0">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="18"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="Code samples" />
                                <TextBlock
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="Find WPF samples that demonstrate specific tasks, features, and APIs." />
                            </StackPanel>
                        </Grid>
                    </Button>
                    <Button
                        AutomationProperties.Name="Partner Center"
                        Click="OnHeaderTileClick"
                        Style="{StaticResource GalleryHeaderTileButtonStyle}"
                        Tag="https://developer.microsoft.com/windows/">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="56" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.RowSpan="2"
                                Margin="-12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontFamily="{StaticResource SymbolThemeFontFamily}"
                                FontSize="16"
                                Text="&#xE8A7;" />
                            <Image
                                Width="56"
                                Height="56"
                                HorizontalAlignment="Left"
                                Source="pack://application:,,,/ModernWpf.Gallery;component/Assets/HomeHeaderTiles/Header-Store.dark.png" />
                            <StackPanel Grid.Row="1" Margin="0,16,0,0">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="18"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="Partner Center" />
                                <TextBlock
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="Upload your app to the Store." />
                            </StackPanel>
                        </Grid>
                    </Button>
                </StackPanel>
                </ScrollViewer>
                    <Button
                        x:Name="ScrollBackButton"
                        AutomationProperties.Name="Scroll left"
                        Margin="8,-16,0,0"
                        Click="OnScrollBackButtonClick"
                        HorizontalAlignment="Left"
                        Style="{StaticResource GalleryHeaderTileScrollButtonStyle}"
                        ToolTip="Scroll left">
                        <TextBlock
                            FontFamily="{StaticResource SymbolThemeFontFamily}"
                            FontSize="8"
                            Text="&#xEDD9;" />
                    </Button>
                    <Button
                        x:Name="ScrollForwardButton"
                        AutomationProperties.Name="Scroll right"
                        Margin="0,-16,8,0"
                        Click="OnScrollForwardButtonClick"
                        HorizontalAlignment="Right"
                        Style="{StaticResource GalleryHeaderTileScrollButtonStyle}"
                        ToolTip="Scroll right">
                        <TextBlock
                            FontFamily="{StaticResource SymbolThemeFontFamily}"
                            FontSize="8"
                            Text="&#xEDDA;" />
                    </Button>
                </Grid>

                <StackPanel
                    Grid.Row="2"
                    Margin="32,24,0,0"
                    Orientation="Vertical">
                    <TextBlock
                        x:Name="OverviewHeaderText"
                        FontSize="16"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="Overview" />

                    <ItemsControl
                        x:Name="OverviewItemsControl"
                        AutomationProperties.Name="Items in group"
                        Focusable="False"
                        Margin="-20,0,0,0"
                        ItemsSource="{Binding Groups}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Margin="10" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button
                                    AutomationProperties.Name="{Binding Title, StringFormat='{}{0}Page'}"
                                    Click="OnGroupCardClick"
                                    Content="{Binding}"
                                    ContentTemplate="{StaticResource GalleryGroupCardTemplate}"
                                    Style="{StaticResource GalleryCardButtonStyle}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock
                        x:Name="RecentlyAddedHeaderText"
                        FontSize="16"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="Recently added and updated" />

                    <ItemsControl
                        x:Name="RecentlyAddedItemsControl"
                        AutomationProperties.Name="Recently Added and Updated Samples Section"
                        Focusable="False"
                        Margin="-18,0,0,0"
                        ItemsSource="{Binding FeaturedItems}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Margin="10" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button
                                    AutomationProperties.Name="{Binding Title, StringFormat='{}{0}Page'}"
                                    Click="OnItemCardClick"
                                    Content="{Binding}"
                                    ContentTemplate="{StaticResource GalleryItemCardTemplate}"
                                    Style="{StaticResource GalleryCardButtonStyle}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
