<Page
    x:Class="ModernWpf.Gallery.Pages.WpfGallery.DesignGuidance.IconographyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ModernWpf.Gallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="IconsPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Loaded="OnLoaded"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="CodeValuePresenterStyle" TargetType="TextBlock">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="0,0,0,4" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="Opacity" Value="0.7" />
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style x:Key="IconData" TargetType="{x:Type ContentControl}">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContentControl}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Padding="0,6"
                                VerticalAlignment="Center"
                                Style="{StaticResource CodeValuePresenterStyle}"
                                Text="{TemplateBinding Content}"
                                TextWrapping="Wrap" />
                            <Button
                                Grid.Column="1"
                                Padding="8"
                                AutomationProperties.Name="{Binding Tag, StringFormat='{}Copy {0} to clipboard', RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                Command="ApplicationCommands.Copy"
                                CommandParameter="{TemplateBinding Content}"
                                CommandTarget="{Binding RelativeSource={RelativeSource AncestorType=Page}}"
                                FocusManager.IsFocusScope="True"
                                ToolTipService.ToolTip="Copy to clipboard">
                                <Grid>
                                    <TextBlock
                                        x:Name="CopyGlyphTextBlock"
                                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                                        Text="&#xE8C8;" />
                                    <TextBlock
                                        x:Name="SuccessGlyphTextBlock"
                                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                                        Opacity="0"
                                        Text="&#xE73E;" />
                                </Grid>
                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="Button.Click">
                                        <EventTrigger.Actions>
                                            <BeginStoryboard>
                                                <Storyboard BeginTime="00:00:00">
                                                    <DoubleAnimation
                                                        Storyboard.TargetName="CopyGlyphTextBlock"
                                                        Storyboard.TargetProperty="Opacity"
                                                        Duration="0:0:0.333"
                                                        To="0" />
                                                    <DoubleAnimation
                                                        Storyboard.TargetName="SuccessGlyphTextBlock"
                                                        Storyboard.TargetProperty="Opacity"
                                                        BeginTime="0:0:0.333"
                                                        Duration="0:0:0.666"
                                                        To="1" />
                                                    <DoubleAnimation
                                                        Storyboard.TargetName="SuccessGlyphTextBlock"
                                                        Storyboard.TargetProperty="Opacity"
                                                        BeginTime="0:0:2"
                                                        Duration="0:0:0.01"
                                                        To="0" />
                                                    <DoubleAnimation
                                                        Storyboard.TargetName="CopyGlyphTextBlock"
                                                        Storyboard.TargetProperty="Opacity"
                                                        BeginTime="0:0:2.1"
                                                        Duration="0:0:0.01"
                                                        To="1" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger.Actions>
                                    </EventTrigger>
                                </Button.Triggers>
                            </Button>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="IconTagChipButtonStyle"
            BasedOn="{StaticResource DefaultButtonStyle}"
            TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Border.CornerRadius" Value="12" />
            <Setter Property="Padding" Value="8,2" />
            <Setter Property="Margin" Value="0,4,4,4" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="IconsListViewItemFocusVisualStyle">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle
                            Margin="5"
                            RadiusX="4"
                            RadiusY="4"
                            Stroke="{DynamicResource KeyboardFocusBorderColorBrush}"
                            StrokeThickness="2" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="PaginationButtonStyle"
            BasedOn="{StaticResource DefaultButtonStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ButtonBase}">
                        <Border
                            x:Name="ContentBorder"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            MinWidth="{TemplateBinding MinWidth}"
                            MinHeight="{TemplateBinding MinHeight}"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalAlignment}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}">
                            <ContentPresenter
                                x:Name="ContentPresenter"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                RecognizesAccessKey="True"
                                TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource ControlAltFillColorQuarternaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundDisabled}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource ControlAltFillColorTertiaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="0,0,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:PageHeader
            Margin="2,0,0,32"
            Description="{Binding ViewModel.PageDescription}"
            Title="{Binding ViewModel.PageTitle}" />

        <Expander
            Grid.Row="1"
            Margin="2,-8,0,0"
            Header="Instructions on how to use Segoe Fluent Icons"
            IsExpanded="False">
            <TextBlock TextWrapping="Wrap">
                <Run FontWeight="SemiBold">How to get the font</Run>
                <LineBreak />
                <Span>
                    On Windows 11: There's nothing you need to do, the font comes with Windows.
                    <LineBreak />
                    On Windows 10: Segoe Fluent Icons is not included by default on Windows 10. You can download it
                    <Hyperlink Click="Open_SegoeFontDownloadPage">here</Hyperlink> .
                </Span>
                <LineBreak />
                <LineBreak />
                <Span FontWeight="SemiBold">How to use the font</Span>
                <LineBreak />
                <Span>
                    An icon with a 16-epx font size is the equivalent of a 16x16-epx icon, to make sizing and positioning more predictable.
                    For optimal appearance, use these specific sizes: 16, 20, 24, 32, 40, 48, and 64. Deviating from these font sizes could lead to less crisp or blurry outcomes.
                </Span>
                <LineBreak />
                <Span>
                    All glyphs in Segoe Fluent Icons have the same fixed width with a consistent height and left origin point, so
                    <Hyperlink Click="Open_IconDesignGuidelinesPage">layering</Hyperlink>
                    and colorization effects can be achieved by drawing glyphs directly on top of each other.
                </Span>
                <LineBreak />
                <LineBreak />
                <Run FontWeight="SemiBold">XAML</Run>
                <LineBreak />
                <Span>&lt;Grid&gt;</Span>
                <LineBreak />
                <Span>&lt;TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&amp;#xEB51;" Foreground="#C72335"/&gt;</Span>
                <LineBreak />
                <Span>&#x09;&lt;TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&amp;#xEB51;" /&gt;</Span>
                <LineBreak />
                <Span>&lt;/Grid&gt;</Span>
            </TextBlock>
        </Expander>

        <TextBlock
            Grid.Row="2"
            Margin="2,24,0,10"
            Style="{StaticResource BodyStrongTextBlockStyle}"
            Text="Fluent Icons Library" />

        <Grid Grid.Row="3">
            <TextBox
                x:Name="IconsSearchBox"
                Width="500"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                AutomationProperties.Name="Search Icons by Name, Tag"
                GotKeyboardFocus="IconsSearchBox_GotKeyboardFocus"
                LostKeyboardFocus="IconsSearchBox_LostKeyboardFocus"
                Text="{Binding ViewModel.SearchText, UpdateSourceTrigger=PropertyChanged, Delay=500}"
                TextChanged="IconsSearchBox_TextChanged" />
            <TextBlock
                x:Name="IconsSearchBoxPlaceholder"
                Margin="14,0,0,0"
                VerticalAlignment="Center"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                IsHitTestVisible="False"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="Search Icons by Name, Tag" />
        </Grid>

        <Grid Grid.Row="4" Margin="2,10,2,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                CornerRadius="8,0,0,8" />

            <ListView
                Padding="4"
                AutomationProperties.Name="Icons"
                ItemsSource="{Binding ViewModel.DisplayedIcons}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                SelectedItem="{Binding ViewModel.SelectedIcon}"
                SelectionMode="Single">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel
                            Margin="10"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                        <Setter Property="FocusVisualStyle" Value="{StaticResource IconsListViewItemFocusVisualStyle}" />
                        <Setter Property="AutomationProperties.Name" Value="{Binding Name, Mode=OneWay}" />
                        <Setter Property="HorizontalAlignment" Value="Center" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <Border
                                        x:Name="Border"
                                        Margin="5"
                                        Padding="0"
                                        HorizontalAlignment="Center"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="{TemplateBinding Border.CornerRadius}">
                                        <ContentPresenter
                                            Margin="-4"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsEnabled" Value="True" />
                                                <Condition Property="IsMouseOver" Value="True" />
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource ListViewItemBackgroundPointerOver}" />
                                        </MultiTrigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource ListViewItemPillFillBrush}" />
                                            <Setter TargetName="Border" Property="BorderThickness" Value="2" />
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource ListViewItemBackgroundPointerOver}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Background="{DynamicResource ButtonBackground}"
                            BorderThickness="4"
                            CornerRadius="8">
                            <Grid
                                Width="96"
                                Height="96"
                                ToolTip="{Binding Name}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock
                                    Grid.Row="0"
                                    Width="28"
                                    Height="28"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    AutomationProperties.Name="{Binding Name, StringFormat='{}{0} Icon'}"
                                    Focusable="False"
                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                    FontSize="28"
                                    Text="{Binding Character}" />
                                <TextBlock
                                    Grid.Row="1"
                                    Margin="6,-4,6,8"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Bottom"
                                    Focusable="False"
                                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{Binding Name}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ListView>

            <Grid
                Grid.Row="0"
                Grid.Column="1"
                Background="{DynamicResource ButtonBackground}">
                <ScrollViewer>
                    <StackPanel Margin="16" Orientation="Vertical">
                        <TextBlock
                            VerticalAlignment="Center"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            Text="{Binding ViewModel.SelectedIcon.Name}" />
                        <TextBlock
                            Margin="0,12,0,32"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="{Binding ViewModel.SelectedIcon.Name, StringFormat='{}{0} Icon'}"
                            FontFamily="{StaticResource SymbolThemeFontFamily}"
                            FontSize="50"
                            Text="{Binding ViewModel.SelectedIcon.Character}" />
                        <TextBlock Text="Icon Name" />
                        <ContentControl
                            Content="{Binding ViewModel.SelectedIcon.Name}"
                            Style="{StaticResource IconData}"
                            Tag="Icon Name" />
                        <TextBlock Text="Unicode point" />
                        <ContentControl
                            Content="{Binding ViewModel.SelectedIcon.Code}"
                            Style="{StaticResource IconData}"
                            Tag="Unicode Point" />
                        <TextBlock Text="Text glyph" />
                        <ContentControl
                            Content="{Binding ViewModel.SelectedIcon.TextGlyph}"
                            Style="{StaticResource IconData}"
                            Tag="Text Glyph" />
                        <TextBlock Text="Code glyph" />
                        <ContentControl
                            Content="{Binding ViewModel.SelectedIcon.CodeGlyph}"
                            Style="{StaticResource IconData}"
                            Tag="Code Glyph" />
                        <TextBlock
                            Text="Tags"
                            Visibility="{Binding ElementName=TagsItemsControl, Path=HasItems, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <ItemsControl
                            x:Name="TagsItemsControl"
                            Margin="0,0,0,12"
                            AutomationProperties.Name="Selected Icon Tags"
                            ItemsSource="{Binding ViewModel.SelectedIcon.Tags}"
                            Visibility="{Binding RelativeSource={RelativeSource Self}, Path=HasItems, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button
                                        AutomationProperties.Name="{Binding}"
                                        Command="{Binding ViewModel.ApplyTagFilterCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource IconTagChipButtonStyle}">
                                        <TextBlock
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Text="{Binding}" />
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="XAML" />
                        <TextBlock
                            x:Name="XAMLCode"
                            Text="{Binding ViewModel.SelectedIcon.TextGlyph, StringFormat='&lt;TextBlock FontFamily=&#x22;{{StaticResource SymbolThemeFontFamily}}&#x22; Text=&#x22;{0}&#x22;/&gt;'}"
                            Visibility="Collapsed" />
                        <ContentControl
                            Content="{Binding ElementName=XAMLCode, Path=Text}"
                            Style="{StaticResource IconData}"
                            Tag="XAML Code" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel
                Margin="0,0,0,0"
                HorizontalAlignment="Left"
                Orientation="Horizontal">
                <Button
                    Margin="0,0,8,0"
                    Padding="8"
                    AutomationProperties.Name="Previous Page"
                    Command="{Binding ViewModel.PreviousPageCommand}"
                    Style="{StaticResource PaginationButtonStyle}"
                    ToolTip="Previous Page">
                    <TextBlock
                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                        FontSize="12"
                        Text="&#xF08D;" />
                </Button>
                <TextBlock
                    Margin="0,0,4,0"
                    VerticalAlignment="Center"
                    Text="{Binding ViewModel.CurrentPage, StringFormat='Page {0} of'}" />
                <TextBlock
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Text="{Binding ViewModel.TotalPages}" />
                <Button
                    Padding="8"
                    AutomationProperties.Name="Next Page"
                    Command="{Binding ViewModel.NextPageCommand}"
                    Style="{StaticResource PaginationButtonStyle}"
                    ToolTip="Next Page">
                    <TextBlock
                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                        FontSize="12"
                        Text="&#xF08F;" />
                </Button>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock
                    Margin="10,0,0,0"
                    VerticalAlignment="Center"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="Icons per page" />
                <ComboBox
                    Margin="10,0,0,0"
                    AutomationProperties.Name="Icons per page"
                    ItemsSource="{Binding ViewModel.PageSizeOptions}"
                    SelectedIndex="{Binding ViewModel.SelectedPageSizeIndex}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
