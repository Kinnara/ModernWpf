<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:local="clr-namespace:ModernWpf.Controls">

    <Style TargetType="local:GridViewItem">
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="Background" Value="{DynamicResource GridViewItemBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource GridViewItemForeground}" />
        <Setter Property="BorderBrush" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{DynamicResource GridViewItemCornerRadius}" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,4,4" />
        <Setter Property="MinWidth" Value="{DynamicResource GridViewItemMinWidth}" />
        <Setter Property="MinHeight" Value="{DynamicResource GridViewItemMinHeight}" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualMargin" Value="-2" />
        <Setter Property="FocusVisualPrimaryBrush" Value="{DynamicResource GridViewItemFocusVisualPrimaryBrush}" />
        <Setter Property="FocusVisualPrimaryThickness" Value="2" />
        <Setter Property="FocusVisualSecondaryBrush" Value="{DynamicResource GridViewItemFocusVisualSecondaryBrush}" />
        <Setter Property="FocusVisualSecondaryThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:GridViewItem">
                    <ControlTemplate.Resources>
                        <StreamGeometry x:Key="CheckMark">M 17.939453 5.439453 L 7.5 15.888672 L 2.060547 10.439453 L 2.939453 9.560547 L 7.5 14.111328 L 17.060547 4.560547 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    <Border
                        x:Name="ContentBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <VisualStateManager.CustomVisualStateManager>
                            <ui:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <ui:VisualStateEx x:Name="PointerOver">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="ContentBorder.Background" Value="{DynamicResource GridViewItemBackgroundPointerOver}" />
                                        <ui:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource GridViewItemForegroundPointerOver}" />
                                        <ui:VisualStateSetter Target="SelectedBorder.BorderBrush" Value="{DynamicResource GridViewItemBackgroundPointerOver}" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="Pressed">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="ContentBorder.Background" Value="{DynamicResource GridViewItemBackgroundPressed}" />
                                        <ui:VisualStateSetter Target="SelectedBorder.BorderBrush" Value="{DynamicResource GridViewItemBackgroundPressed}" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="Selected">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="ContentBorder.Background" Value="{DynamicResource GridViewItemBackgroundSelected}" />
                                        <ui:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource GridViewItemForegroundSelected}" />
                                        <ui:VisualStateSetter Target="SelectedBorder.Opacity" Value="1" />
                                        <ui:VisualStateSetter Target="SelectedBorder.BorderBrush" Value="{DynamicResource GridViewItemCheckBoxBrush}" />
                                        <ui:VisualStateSetter Target="MultiSelectCheck.Opacity" Value="1" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="PointerOverSelected">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="ContentBorder.Background" Value="{DynamicResource GridViewItemBackgroundSelectedPointerOver}" />
                                        <ui:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource GridViewItemForegroundSelected}" />
                                        <ui:VisualStateSetter Target="SelectedBorder.Opacity" Value="1" />
                                        <ui:VisualStateSetter Target="SelectedBorder.BorderBrush" Value="{DynamicResource GridViewItemBackgroundSelectedPointerOver}" />
                                        <ui:VisualStateSetter Target="MultiSelectCheck.Opacity" Value="1" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="PressedSelected">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="ContentBorder.Background" Value="{DynamicResource GridViewItemBackgroundSelectedPressed}" />
                                        <ui:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource GridViewItemForegroundSelected}" />
                                        <ui:VisualStateSetter Target="SelectedBorder.Opacity" Value="1" />
                                        <ui:VisualStateSetter Target="SelectedBorder.BorderBrush" Value="{DynamicResource GridViewItemBackgroundSelectedPressed}" />
                                        <ui:VisualStateSetter Target="MultiSelectCheck.Opacity" Value="1" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="SelectedDisabled">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="ContentBorder.Background" Value="{DynamicResource GridViewItemBackgroundSelectedDisabled}" />
                                        <ui:VisualStateSetter Target="ContentBorder.Opacity" Value="{DynamicResource ListViewItemDisabledThemeOpacity}" />
                                        <ui:VisualStateSetter Target="SelectedBorder.Opacity" Value="1" />
                                        <ui:VisualStateSetter Target="MultiSelectCheck.Opacity" Value="1" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="Disabled">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="ContentBorder.Opacity" Value="{DynamicResource ListViewItemDisabledThemeOpacity}" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="MultiSelectStates">
                                <ui:VisualStateEx x:Name="NoMultiSelect">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="MultiSelectSquare.Visibility" Value="{x:Static Visibility.Collapsed}" />
                                        <ui:VisualStateSetter Target="MultiSelectCheck.Visibility" Value="{x:Static Visibility.Collapsed}" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <VisualState x:Name="ListMultiSelect" />
                                <ui:VisualStateEx x:Name="GridMultiSelect">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="MultiSelectSquare.Visibility" Value="{x:Static Visibility.Visible}" />
                                        <ui:VisualStateSetter Target="MultiSelectCheck.Visibility" Value="{x:Static Visibility.Visible}" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid>
                            <ui:ContentPresenterEx
                                x:Name="ContentPresenter"
                                ClipToBounds="True"
                                Content="{TemplateBinding Content}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Foreground="{TemplateBinding Foreground}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Margin="{TemplateBinding Padding}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            <Border
                                x:Name="SelectedBorder"
                                BorderBrush="{DynamicResource GridViewItemCheckBoxBrush}"
                                BorderThickness="2"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                IsHitTestVisible="False"
                                Opacity="0" />
                            <Border
                                x:Name="MultiSelectSquare"
                                Background="{DynamicResource GridViewItemCheckBoxBrush}"
                                Height="20"
                                HorizontalAlignment="Right"
                                Margin="0,2,2,0"
                                VerticalAlignment="Top"
                                Visibility="Collapsed"
                                Width="20">
                                <ui:FontIconFallback
                                    x:Name="MultiSelectCheck"
                                    Data="{StaticResource CheckMark}"
                                    FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                    FontSize="16"
                                    Foreground="{DynamicResource GridViewItemCheckBrush}"
                                    Opacity="0"
                                    Visibility="Collapsed" />
                            </Border>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:GridViewHeaderItem">
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource GridViewHeaderItemThemeFontSize}" />
        <Setter Property="Background" Value="{DynamicResource GridViewHeaderItemBackground}" />
        <Setter Property="Margin" Value="0,0,0,4" />
        <Setter Property="Padding" Value="12,8,12,0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="MinHeight" Value="{DynamicResource GridViewHeaderItemMinHeight}" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:GridViewHeaderItem">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <StackPanel>
                            <ui:ContentPresenterEx
                                x:Name="ContentPresenter"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Margin="{TemplateBinding Padding}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            <Rectangle
                                Height="1"
                                HorizontalAlignment="Stretch"
                                Margin="12,8,12,0"
                                Stroke="{DynamicResource GridViewHeaderItemDividerStroke}"
                                StrokeThickness="0.5"
                                VerticalAlignment="Bottom" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:GridView">
        <Setter Property="Padding" Value="0,0,0,10" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Once" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualMargin" Value="-2" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:GridView">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <ui:ScrollViewerEx
                            x:Name="ScrollViewer"
                            Focusable="false"
                            HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                            IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                            KeyboardNavigation.TabNavigation="{TemplateBinding KeyboardNavigation.TabNavigation}"
                            VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                            <ItemsPresenter
                                Margin="{TemplateBinding Padding}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ui:ScrollViewerEx>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
