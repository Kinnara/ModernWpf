<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:local="clr-namespace:ModernWpf.Controls">

    <Style TargetType="local:BreadcrumbBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:BreadcrumbBar">
                    <StackPanel x:Name="PART_RootPanel" Orientation="Horizontal" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:BreadcrumbBarItem">
        <Setter Property="Focusable" Value="True" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:BreadcrumbBarItem">
                    <Grid x:Name="PART_LayoutRoot">
                        <VisualStateManager.CustomVisualStateManager>
                            <ui:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="InlineItemTypeStates">
                                <ui:VisualStateEx x:Name="Default">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="PART_ChevronTextBlock.Text" Value="&#xE76C;" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="LastItem">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="PART_ItemButton.Visibility" Value="Collapsed" />
                                        <ui:VisualStateSetter Target="PART_ChevronTextBlock.Visibility" Value="Collapsed" />
                                        <ui:VisualStateSetter Target="PART_LastItemContentPresenter.Visibility" Value="Visible" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button
                            x:Name="PART_ItemButton"
                            Padding="{TemplateBinding Padding}"
                            Background="Transparent"
                            BorderThickness="0"
                            Focusable="False"
                            Foreground="{TemplateBinding Foreground}"
                            ui:ControlHelper.ContentTransitions="{TemplateBinding ContentTransitions}"
                            ui:ControlHelper.CornerRadius="{TemplateBinding CornerRadius}">
                            <ui:ContentPresenterEx
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                Foreground="{TemplateBinding Foreground}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Button>
                        <ui:ContentPresenterEx
                            x:Name="PART_LastItemContentPresenter"
                            Visibility="Collapsed"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                            ContentStringFormat="{TemplateBinding ContentStringFormat}"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            FontWeight="SemiBold"
                            Foreground="{TemplateBinding Foreground}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <TextBlock
                            x:Name="PART_ChevronTextBlock"
                            Grid.Column="1"
                            Margin="2,0"
                            VerticalAlignment="Center"
                            FontFamily="Segoe MDL2 Assets"
                            Text="&#xE76C;" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
