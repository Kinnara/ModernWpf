<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:local="clr-namespace:ModernWpf.Controls">

    <Style TargetType="local:DropDownButton">
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ButtonBorderThemeThickness}" />
        <Setter Property="Padding" Value="{DynamicResource ButtonPadding}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualMargin" Value="-3" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
        <Setter Property="ui:ButtonHelper.VisualStateSettersEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:DropDownButton">
                    <ControlTemplate.Resources>
                        <StreamGeometry x:Key="ChevronDownSmall">F1 M 17.568359 3.408203 L 19.931641 5.761719 L 10 15.693359 L 0.068359 5.761719 L 2.431641 3.408203 L 10 10.976562 L 17.568359 3.408203 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    <ui:GridEx x:Name="RootGrid"
                        Padding="{TemplateBinding Padding}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                        BackgroundSizing="{TemplateBinding BackgroundSizing}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <VisualStateManager.CustomVisualStateManager>
                            <ui:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>

                        <ui:GridEx.BackgroundTransition>
                            <ui:BrushTransition Duration="0:0:0.083" />
                        </ui:GridEx.BackgroundTransition>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <ui:VisualStateEx x:Name="PointerOver">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="RootGrid.Background" Value="{DynamicResource ButtonBackgroundPointerOver}" />
                                        <ui:VisualStateSetter Target="RootGrid.BorderBrush" Value="{DynamicResource ButtonBorderBrushPointerOver}" />
                                        <ui:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource ButtonForegroundPointerOver}" />
                                        <ui:VisualStateSetter Target="ChevronIcon.Foreground" Value="{DynamicResource DropDownButtonForegroundSecondaryPointerOver}" />
                                        <ui:VisualStateSetter Target="ChevronIcon.(ui:AnimatedIcon.State)" Value="PointerOver" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="Pressed">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="RootGrid.Background" Value="{DynamicResource ButtonBackgroundPressed}" />
                                        <ui:VisualStateSetter Target="RootGrid.BorderBrush" Value="{DynamicResource ButtonBorderBrushPressed}" />
                                        <ui:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource ButtonForegroundPressed}" />
                                        <ui:VisualStateSetter Target="ChevronIcon.Foreground" Value="{DynamicResource DropDownButtonForegroundSecondaryPressed}" />
                                        <ui:VisualStateSetter Target="ChevronIcon.(ui:AnimatedIcon.State)" Value="Pressed" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                                <ui:VisualStateEx x:Name="Disabled">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="RootGrid.Background" Value="{DynamicResource ButtonBackgroundDisabled}" />
                                        <ui:VisualStateSetter Target="RootGrid.BorderBrush" Value="{DynamicResource ButtonBorderBrushDisabled}" />
                                        <ui:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource ButtonForegroundDisabled}" />
                                        <ui:VisualStateSetter Target="ChevronIcon.Foreground" Value="{DynamicResource ButtonForegroundDisabled}" />
                                        <ui:VisualStateSetter Target="ChevronIcon.(ui:AnimatedIcon.State)" Value="Normal" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <StackPanel
                            x:Name="ContentPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ui:ContentPresenterEx
                                x:Name="ContentPresenter"
                                CharacterSpacing="{TemplateBinding CharacterSpacing}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                Foreground="{TemplateBinding Foreground}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                IsTextScaleFactorEnabled="{TemplateBinding IsTextScaleFactorEnabled}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                Focusable="False"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                            <ui:FontIconFallback
                                x:Name="ChevronIcon"
                                Width="12"
                                Height="12"
                                FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                FontSize="8"
                                Foreground="{DynamicResource DropDownButtonForegroundSecondary}"
                                Data="{StaticResource ChevronDownSmall}"
                                VerticalAlignment="Center"
                                Margin="8,0,0,0"
                                ui:AnimatedIcon.State="Normal" />
                        </StackPanel>

                    </ui:GridEx>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
