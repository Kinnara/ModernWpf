<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ModernWpf.Controls"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:m="clr-namespace:ModernWpf.Markup;assembly=ModernWpf">

    <Style x:Key="DefaultSwipeItemStyle" TargetType="local:AppBarButton">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="MinWidth" Value="68" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Background" Value="{DynamicResource SwipeItemBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource SwipeItemForeground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:AppBarButton">
                    <Grid
                        x:Name="Root"
                        MinWidth="{TemplateBinding MinWidth}"
                        MinHeight="{TemplateBinding MinHeight}"
                        Background="{TemplateBinding Background}">
                        <VisualStateManager.CustomVisualStateManager>
                            <ui:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <ui:VisualStateEx x:Name="Normal" />
                                <ui:VisualStateEx x:Name="Disabled" />
                                <ui:VisualStateEx x:Name="PointerOver" />
                                <ui:VisualStateEx x:Name="Pressed">
                                    <ui:VisualStateEx.Setters>
                                        <ui:VisualStateSetter Target="Root.Background" Value="{DynamicResource SwipeItemBackgroundPressed}" />
                                    </ui:VisualStateEx.Setters>
                                </ui:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid
                            x:Name="ContentRoot"
                            Margin="4,4,4,2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Viewbox MaxHeight="16" MinWidth="16">
                                <ContentPresenter
                                    x:Name="Content"
                                    Margin="0,0,0,2"
                                    Content="{TemplateBinding Icon}" />
                            </Viewbox>
                            <TextBlock
                                x:Name="TextLabel"
                                Grid.Row="1"
                                FontSize="12"
                                Foreground="{TemplateBinding Foreground}"
                                Text="{TemplateBinding Label}"
                                TextAlignment="Center"
                                TextWrapping="Wrap" />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:SwipeControl">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MinHeight" Value="{DynamicResource ListViewItemMinHeight}" />
        <Setter Property="MinWidth" Value="{DynamicResource ListViewItemMinWidth}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:SwipeControl">
                    <Grid
                        x:Name="RootGrid"
                        Background="Transparent"
                        ClipToBounds="True">
                        <Grid.Resources>
                            <m:StaticResource x:Key="SwipeItemBackground" ResourceKey="ControlFillColorTertiaryBrush" />
                            <m:StaticResource x:Key="SwipeItemForeground" ResourceKey="TextFillColorPrimaryBrush" />
                            <m:StaticResource x:Key="SwipeItemBackgroundPressed" ResourceKey="ControlAltFillColorQuarternaryBrush" />
                            <m:StaticResource x:Key="SwipeItemPreThresholdExecuteForeground" ResourceKey="ControlStrongFillColorDefaultBrush" />
                            <m:StaticResource x:Key="SwipeItemPreThresholdExecuteBackground" ResourceKey="ControlFillColorTertiaryBrush" />
                            <m:StaticResource x:Key="SwipeItemPostThresholdExecuteForeground" ResourceKey="TextOnAccentFillColorPrimaryBrush" />
                            <m:StaticResource x:Key="SwipeItemPostThresholdExecuteBackground" ResourceKey="AccentFillColorDefaultBrush" />
                            <Style x:Key="SwipeItemStyle" TargetType="local:AppBarButton" BasedOn="{StaticResource DefaultSwipeItemStyle}" />
                        </Grid.Resources>

                        <Grid x:Name="SwipeContentRoot">
                            <StackPanel x:Name="SwipeContentStackPanel">
                                <StackPanel.RenderTransform>
                                    <TranslateTransform x:Name="SwipeContentStackPanelTransform" />
                                </StackPanel.RenderTransform>
                            </StackPanel>
                        </Grid>

                        <Grid x:Name="ContentRoot">
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="ContentRootTransform" />
                            </Grid.RenderTransform>
                            <ui:ContentPresenterEx
                                x:Name="ContentPresenter"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}"
                                Content="{TemplateBinding Content}"
                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch" />
                            <Grid
                                x:Name="InputEater"
                                Background="Transparent"
                                IsHitTestVisible="False"
                                Visibility="Collapsed" />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
