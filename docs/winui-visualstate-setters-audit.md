# WinUI VisualState.Setters Audit

This tracks WinUI `VisualState.Setters` parity for ModernWpf 1.x. The primary
source is the local final WinUI 2.8.7 checkout:
`D:\repos\microsoft-ui-xaml-v2.8.7\dev`. The current WinUI 3 checkout at
`D:\repos\microsoft-ui-xaml\src\controls\dev` was also counted to identify
newer deltas, but the repo-wide sync matrix is still anchored on WinUI 2.8.7.

## Status Legend

- `Converted`: ModernWpf product XAML uses `VisualStateEx.Setters`.
- `Partial`: some setters are converted, but unsupported setter shapes remain
  represented by WPF storyboards/triggers/code.
- `Pending`: the WinUI setter blocks map to an existing ModernWpf template and
  still need conversion or an explicit compatibility decision.
- `Unsupported`: the setter target needs runtime support that does not exist yet.
- `Excluded`: the upstream file maps to a WinUI surface this repo does not carry.

## Current Conversions

| WinUI 2.8.7 source | Setter blocks | ModernWpf file | Status | Notes |
| --- | ---: | --- | --- | --- |
| `dev\ProgressBar\ProgressBar.xaml` | 4 | `ModernWpf\ProgressBar\ProgressBar.xaml` | Converted | Direct opacity setters and the `UpdatingError` nested brush-color setter now use `VisualStateEx.Setters`; `VisualStateManagerEx` resolves WinUI-style nested target paths such as `(Shape.Fill).(SolidColorBrush.Color)`. Transition storyboards remain for animated progress movement and color transitions. |
| `dev\CommonStyles\AppBarSeparator_themeresources.xaml` | 2 | `ModernWpf.Controls\CommandBar\AppBarSeparator.xaml` | Converted | Compact and overflow layout states now use `VisualStateEx.Setters` instead of a `VisualStateGroupListener` trigger bridge. |
| `dev\CommonStyles\CalendarView_themeresources.xaml` | 2 | `ModernWpf\Styles\Calendar.xaml` | Converted | Calendar navigation button pointer-over/pressed border and foreground setters now use `VisualStateEx.Setters` instead of WPF template triggers. |
| `dev\CommonStyles\CommandBar_themeresources.xaml` | 3 | `ModernWpf.Controls\CommandBar\CommandBar.xaml` | Converted | The `DynamicOverflowEnabled` column-width setters and `CommandBarOverflowPresenter` full-width open-up/down padding and border-thickness setters now use `VisualStateEx.Setters`; the overflow presenter drives its display-mode states from popup placement. |
| `dev\InfoBadge\InfoBadge_themeresources.xaml` | 3 | `ModernWpf.Controls\InfoBadge\InfoBadge.xaml` | Converted | `Dot`, `Icon`, `FontIcon`, and `Value` state behavior now lives in `VisualStateEx.Setters`; code only chooses the state and creates the icon element. |
| `dev\InfoBar\InfoBar.xaml` | 8 | `ModernWpf.Controls\InfoBar\InfoBar.xaml` | Converted | Severity, icon, close-button, open/closed, and foreground states now use `VisualStateEx.Setters`; `VisualStateSetter` supports binding-valued setters for WinUI's `ForegroundSet` shape. |
| `dev\NumberBox\NumberBox.xaml` | 7 | `ModernWpf.Controls\NumberBox\NumberBox.xaml` | Converted | Disabled header, inline/compact spin-button placement, `PopupIndicator.Visibility`, `SpinButtonsColumn.Width`, and spin-button enabled states now use `VisualStateEx.Setters`; `NumberBox` drives `SpinButtonStates` on both the outer control and inner text box like WinUI. |
| `dev\PersonPicture\PersonPicture.xaml` | 5 | `ModernWpf.Controls\PersonPicture\PersonPicture.xaml` | Converted | Photo, placeholder/group, and badge states now use `VisualStateEx.Setters`, including the image-brush object setter for `BadgingEllipse.Fill`. The WPF template keeps its `FontIconFallback` placeholder mapping instead of WinUI's symbol-font `TextBlock` glyph setters. |
| `dev\ProgressRing\ProgressRing.xaml` | 1 | `ModernWpf.Controls\ProgressRing\ProgressRing.xaml` | Converted | The inactive opacity setter is represented with `VisualStateEx.Setters`; WinUI's `LottiePlayer.(AutomationProperties.AccessibilityView)=Raw` has no WPF attached-property equivalent, so ModernWpf uses `ProgressRingAutomationPeer.IsControlElementCore()` to remove inactive rings from the control view. |
| `dev\RadioButtons\RadioButtons.xaml` | 1 | `ModernWpf.Controls\RadioButtons\RadioButtons.xaml` | Converted | Disabled header foreground now uses a dynamic-resource `VisualStateEx.Setters` entry instead of a WPF template trigger. |
| `dev\CommonStyles\ScrollBar_themeresources.xaml` | 8 | `ModernWpf\Styles\ScrollBar.xaml` | Partial | Existing WPF conscious expand/collapse states now use `VisualStateEx.Setters` for pointer-over chrome and panning thumb brushes, and the vertical/horizontal disabled common states now use `VisualStateEx.Setters` driven by `ScrollBarHelper`. WinUI's separate touch/mouse/no-indicator root and panning-root setters are still not represented by this WPF ScrollBar template surface. |
| `dev\SplitView\SplitView_themeresources.xaml` | 4 | `ModernWpf.Controls\SplitView\SplitView.xaml` | Converted | Closed compact, open inline left, and open compact overlay left direct setter blocks now use `VisualStateEx.Setters`; transition storyboards remain for animated pane movement. |
| `dev\TeachingTip\TeachingTip.xaml` | 29 | `ModernWpf.Controls\TeachingTip\TeachingTip.xaml` | Converted | Light-dismiss background, button/content/close-button-location/icon/hero-placement, tail-placement, title, and subtitle states now use `VisualStateEx.Setters` driven by `TeachingTip`; WPF popup placement, sizing, and animations remain code-driven. |
| `dev\CommonStyles\ToggleSwitch_themeresources.xaml` | 1 | `ModernWpf.Controls\ToggleSwitch\ToggleSwitch.xaml` | Converted | WinUI's pressed knob alignment setters are represented in ModernWpf's active `Dragging` state, because this WPF port routes thumb interaction through that state instead of `CommonStates.Pressed`. |
| `dev\TwoPaneView\TwoPaneView.xaml` | 5 | `ModernWpf.Controls\TwoPaneView\TwoPaneView.xaml` | Converted | Mode-state pane row/column and single-pane visibility setters now live in `VisualStateEx.Setters`; code still computes mode and pane lengths. |

## Relevant Pending WinUI 2.8.7 Sources

| WinUI 2.8.7 source | Setter blocks | ModernWpf mapping | Status | Main blocker or next action |
| --- | ---: | --- | --- | --- |
| `dev\AutoSuggestBox\AutoSuggestBox_themeresources.xaml` | 2 | `ModernWpf\Styles\AutoSuggestBox.xaml` | Unsupported | WinUI setters only target `AnimatedIcon.State`; ModernWpf uses static icon presenter fallback and has no compatible `AnimatedIcon.State` surface yet. |
| `dev\Breadcrumb\BreadcrumbBar.xaml` | 16 | `ModernWpf.Controls\BreadcrumbBar\BreadcrumbBar.xaml` | Partial | Inline item `Default` chevron text and `LastItem` item/chevron/current-content visibility setters now use `VisualStateEx.Setters` driven by `BreadcrumbBarItem`; WinUI's ellipsis dropdown layout/focus setters and nested item-button common-state foreground/background setters are not represented by the simplified WPF BreadcrumbBar surface yet. |
| `dev\ColorPicker\ColorPicker.xaml` | 20 | `ModernWpf.Controls\ColorPicker\ColorPicker.xaml` | Partial | Top-level spectrum, preview, previous-color, slider, more-button, text-input visibility, alpha hex-length, and simplified horizontal-orientation layout states now use `VisualStateEx.Setters` and WinUI state names. Color-representation selection and the nested slider template state setters remain pending because the current WPF template is still structurally simpler than WinUI. |
| `dev\ColorPicker\ColorSpectrum.xaml` | 5 | `ModernWpf.Controls\ColorPicker\ColorPicker.xaml` | Partial | Shape, pointer-over, pressed-large, focus, and selection-ellipse color states now use `VisualStateEx.Setters` driven by `ColorSpectrum`. WinUI's separate rectangle/ellipse border visibility targets are still not represented by the current WPF spectrum border surface. |
| `dev\ComboBox\ComboBox_themeresources.xaml` | 10 | `ModernWpf\Styles\ComboBox.xaml` | Partial | Editable overlay states now use `VisualStateEx.Setters` driven by `ComboBoxHelper.VisualStateSettersEnabled`. The `AnimatedIcon.State` common-state setters and inner editable TextBox setter blocks remain represented by ModernWpf's static glyph and WPF-native TextBox triggers. |
| `dev\CommandBarFlyout\CommandBarFlyout_themeresources.xaml` | 41 | `ModernWpf.Controls\CommandBarFlyout\CommandBarFlyout.xaml` | Partial | Flyout `AppBarButton` application-view overflow layout, common chrome, keyboard-accelerator visibility, and input-mode padding states now use `VisualStateEx.Setters`; flyout `AppBarToggleButton` application-view overflow layout, common/checked/overflow chrome, keyboard-accelerator visibility, and input-mode padding/check-glyph margin states also use setters. Toolbar available-command and combined placement/corner states now use setters while preserving the WPF popup-placement behavior. Expansion animation and shadow setter groups remain pending, and touch/game-controller input-mode selection is source-portable but not automatically driven by WPF runtime input detection. |
| `dev\CommonStyles\AppBarButton_themeresources.xaml` | 16 | `ModernWpf.Controls\CommandBar\AppBarButton.xaml` | Partial | Application-view layout, common chrome, input-mode padding, keyboard accelerator, and flyout chevron setters that map to existing WPF template parts now use `VisualStateEx.Setters` driven by `AppBarButton` or available as setter-backed source parity groups; WinUI's `SubItemChevronPanel` and `OverflowSubItemChevron` targets are not represented by the current template, and touch/game-controller input-mode selection is not automatically driven by WPF runtime input detection. |
| `dev\CommonStyles\AppBarToggleButton_themeresources.xaml` | 18 | `ModernWpf.Controls\CommandBar\AppBarToggleButton.xaml` | Partial | Application-view layout, common/checked/overflow chrome, input-mode padding/check-glyph margin, check-glyph opacity, and keyboard accelerator setters now use `VisualStateEx.Setters` driven by `AppBarToggleButton` or available as setter-backed source parity groups; touch/game-controller input-mode selection is not automatically driven by WPF runtime input detection. |
| `dev\CommonStyles\Button_themeresources.xaml` | 3 | `ModernWpf\Styles\Button.xaml`, `ModernWpf.Controls\HyperlinkButton\HyperlinkButton.xaml` | Unsupported | WinUI setters only target `AnimatedIcon.State`; ModernWpf button templates use static presenter/icon fallback and have no compatible `AnimatedIcon.State` surface yet. |
| `dev\CommonStyles\CalendarDatePicker_themeresources.xaml` | 1 | `ModernWpf\Styles\DatePicker.xaml` | Converted | The WPF DatePicker template now carries WinUI's `HeaderStates.LeftHeader` layout setters through `VisualStateEx.Setters`, driven by `DatePickerHelper.HeaderPlacement`. |
| `dev\CommonStyles\CheckBox_themeresources.xaml` | 12 | `ModernWpf\Styles\CheckBox.xaml` | Partial | The WPF template now has WinUI `CombinedStates` and uses `VisualStateEx.Setters` for the indeterminate glyph-margin setter equivalent. WinUI's `AnimatedIcon.State` setters remain unsupported because ModernWpf still uses a static `FontIconFallback` glyph. |
| `dev\CommonStyles\ContentDialog_themeresources.xaml` | 15 | `ModernWpf.Controls\ContentDialog\ContentDialog.xaml` | Converted | Dialog showing, full sizing, button visibility, default-button accent, and accent-border direct setters now use `VisualStateEx.Setters`; WPF show/hide transition storyboards remain for popup animation. |
| `dev\CommonStyles\DatePicker_themeresources.xaml` | 1 | `ModernWpf\Styles\DatePicker.xaml` | Converted | WinUI's `HasNoDate` flyout-button foreground setter is represented on the WPF DatePicker drop-down button and driven from `SelectedDate`. |
| `dev\CommonStyles\MenuFlyout_themeresources.xaml` | 42 | `ModernWpf.Controls\MenuFlyout\MenuFlyout.xaml`, `ModernWpf\Styles\MenuItem.xaml`, `ModernWpf.Controls\RadioMenuItem\RadioMenuItem.xaml` | Partial | WPF regular submenu items now use `VisualStateEx.Setters` for common chrome, checked opacity, and keyboard-accelerator visibility; WPF submenu headers now use setters for common, submenu-open, and disabled chrome. Both are driven by `MenuItemHelper`, and radio menu items already use setters. Placeholder/icon layout states, padding states, reveal states, and WinUI alternate subitem branches remain pending. |
| `dev\CommonStyles\Pivot_themeresources.xaml` | 5 | `ModernWpf\Styles\Pivot.xaml` | Converted | Pivot header item `SelectionStates` now use `VisualStateEx.Setters` for `SelectedPipe.Visibility`, driven by `PivotHelper.HeaderItemVisualStateSettersEnabled`; `UnselectedLocked` is retained as a source-parity state even though the WPF header does not use WinUI's locked animation path. |
| `dev\DropDownButton\DropDownButton.xaml` | 3 | `ModernWpf.Controls\DropDownButton\DropDownButton.xaml` | Unsupported | WinUI setters target `AnimatedIcon.State`; ModernWpf currently uses `FontIconFallback`, so this needs an `AnimatedIcon.State` compatibility surface or a documented fallback. |
| `dev\Expander\Expander.xaml`, `dev\Expander\Expander_themeresources.xaml` | 15 | `ModernWpf\Styles\Expander.xaml` | Partial | WPF `Expander` now uses `VisualStateEx.Setters` for disabled foreground, expanded content visibility, and expand-direction layout/style setters driven by WPF's native state names. Header toggle-button chrome still uses WPF template triggers, and WinUI `AnimatedIcon.State` setters remain represented by static chevron glyph changes. |
| `dev\NavigationView\NavigationBackButton.xaml` | 2 | `ModernWpf\Styles\NavigationBackButton.xaml` | Unsupported | WinUI setters target `AnimatedIcon.State`; ModernWpf uses `FontIconFallback`, so a real port needs an `AnimatedIcon.State` compatibility surface or an explicit static-icon fallback decision. |
| `dev\NavigationView\NavigationView.xaml` | 21 | `ModernWpf.Controls\NavigationView\NavigationView.xaml` | Partial | `NavigationViewItemSeparator` horizontal-compact and vertical line states now use `VisualStateEx.Setters`; toggle-pane visible, header, AutoSuggest, closed-compact pane, compact pane-list size, back-button collapsed, pane-collapsed, pane separator visibility, titlebar collapsed, overflow no-label, display-mode minimal/top-minimal, pane-not-overlaying, NavigationViewItem item-position, and NavigationViewItemHeader collapsed/top-mode states also now use setters. Remaining `NavigationView.xaml` gap is that WinUI's `ShadowCaster` pane-collapse/list-size targets are not represented by the current WPF template. |
| `dev\NavigationView\NavigationView_rs1_themeresources.xaml` | 71 | `ModernWpf.Controls\NavigationView\NavigationView.xaml`, `ModernWpf\Styles\NavigationView.xaml` | Partial | The left-pane `NavigationViewItemPresenter` pointer, disabled, icon-collapsed, InfoBadge collapsed, chevron-visible, and closed-compact top-level item states now use `VisualStateEx.Setters`, including WinUI's closed-compact `InfoBadgePresenter` attached-grid and alignment setters. The top-pane and top-pane overflow presenter InfoBadge collapsed states now use setters, and `NavigationViewItem.InfoBadge` is forwarded through the WinUI presenter slots. The top-pane settings item pointer and disabled states now use setters and now exposes the WinUI `InfoBadgePresenter` slot. Top navigation overflow button and pane toggle button `CommonStates` now use setters through `ButtonHelper.VisualStateSettersEnabled`; the remaining compatibility gap in this resource file is unsupported `AnimatedIcon.State` targets, which ModernWpf still represents with static `FontIconFallback` chevrons/icons. |
| `dev\PagerControl\PagerControl.xaml` | 16 | `ModernWpf.Controls\PagerControl\PagerControl.xaml` | Partial | Navigation button visibility, hidden-on-edge, and enabled state setters now use `VisualStateEx.Setters` driven by `PagerControl`; WinUI's number-box/combo-box display-mode and infinite-item label setters are not represented by the simplified WPF PagerControl template yet. |
| `dev\PipsPager\PipsPager.xaml` | 7 | `ModernWpf.Controls\PipsPager\PipsPager.xaml` | Converted | Previous/next button hidden/collapsed/enabled states and horizontal root orientation transform/tooltip setters now use `VisualStateEx.Setters`; code drives the WinUI state names instead of setting button properties directly. |
| `dev\PipsPager\PipsPager_themeresources.xaml` | 1 | `ModernWpf.Controls\PipsPager\PipsPager.xaml` | Converted | The default pip-button style now has WinUI-compatible `OrientationStates`; `VerticalOrientation` uses `VisualStateEx.Setters` to swap the pip root width/height, and generated pip buttons drive that state from `PipsPager.Orientation`. |
| `dev\RadioMenuFlyoutItem\RadioMenuFlyoutItem_themeresources.xaml` | 13 | `ModernWpf.Controls\RadioMenuItem\RadioMenuItem.xaml` | Partial | The default radio menu item common, check/icon, keyboard accelerator, and check-glyph state effects now use `VisualStateEx.Setters` driven by `RadioMenuItem`; WinUI's separate `MenuFlyoutSubItem` branch and text placeholder margin are not directly represented by the WPF grid layout. |
| `dev\RatingControl\RatingControl.xaml` | 6 | `ModernWpf.Controls\RatingControl\RatingControl.xaml` | Converted | Disabled, placeholder, pointer-over placeholder/unselected, set, and pointer-over set foreground states now use `VisualStateEx.Setters` instead of the WPF listener/trigger bridge. |
| `dev\SplitButton\SplitButton.xaml`, `dev\SplitButton\SplitButton_themeresources.xaml` | 34 | `ModernWpf.Controls\SplitButton\SplitButton.xaml` | Partial | Default SplitButton/ToggleSplitButton common-state chrome and secondary-button placement setters now use `VisualStateEx.Setters`, including the nested primary-button disabled template. WinUI animated-icon state setters remain unsupported because ModernWpf uses WPF content presenters and a font-icon fallback; WinUI 3's newer split primary/secondary border target names are not represented by the current WPF template structure yet. |
| `dev\SwipeControl\SwipeControl_themeresources.xaml` | 1 | `ModernWpf.Controls\SwipeControl\SwipeControl.xaml` | Converted | Generated swipe action buttons now use a WPF `SwipeItemStyle` whose `Pressed` state applies `SwipeItemBackgroundPressed` through `VisualStateEx.Setters`; item-specific background/foreground values remain local button values. |
| `dev\TabView\TabView.xaml` | 18 | `ModernWpf\Styles\TabControl.xaml` | Partial | WPF `TabItem` header common, disabled, icon, and foreground states that map to existing template parts now use `VisualStateEx.Setters` driven by `TabItemHelper`; full WinUI `TabView` list-view chrome, close-button, compact-width, drag/reorder, data-placeholder, and bottom-border states are not represented by the WPF `TabControl` surface yet. |
| `dev\TitleBar\TitleBar.xaml`, `dev\TitleBar\TitleBar_themeresources.xaml` | 8 | `ModernWpf\TitleBar\TitleBarControl.xaml`, `ModernWpf\TitleBar\TitleBarButton.xaml` | Partial | TitleBar activation, back-button visibility, icon visibility, title-text visibility, and WPF extend-titlebar chrome states now use `VisualStateEx.Setters` driven by `TitleBarControl`; WinUI caption-button `AnimatedIcon.State`, newer WinUI 3 subtitle/header/content/display-mode state groups, and full-screen/expanded-height states are not represented by the WPF titlebar surface yet. |
| `dev\TreeView\TreeViewItem.xaml` | 10 | `ModernWpf\Styles\TreeView.xaml` | Partial | Common-state background, foreground, glyph, border, and selection-indicator setters now use `VisualStateEx.Setters` driven by `TreeViewItemHelper`; WinUI multi-select and drag/reorder setter groups are not represented by the current WPF stock TreeView mapping. |

## Excluded Or Non-Product Sources

| WinUI 2.8.7 source | Setter blocks | Reason |
| --- | ---: | --- |
| `dev\AnimatedIcon\TestUI\AnimatedIconPage.xaml` | 12 | TestUI sample only; `AnimatedIcon` itself is excluded from ModernWpf core. |
| `dev\CommonStyles\InkToolbar_themeresources.xaml` | 112 | InkToolbar is not carried as a ModernWpf control. |
| `dev\CommonStyles\MediaTransportControls_themeresources.xaml` | 11 | Media transport controls are not carried as ModernWpf controls. |
| `dev\CommonStyles\TimePicker_themeresources.xaml` | 1 | TimePicker is not carried as a ModernWpf control. |
| `dev\Materials\Reveal\*.xaml` and `dev\Materials\Reveal\TestUI\*.xaml` | 331 | WinUI compositor/reveal material system is excluded from the WPF port. |
| `dev\MenuBar\MenuBarItem.xaml` | 3 | ModernWpf maps menu behavior through WPF Menu/MenuItem/MenuFlyout surfaces, not a WinUI MenuBar control. |

## Runtime Gaps Blocking Full Conversion

- Nested target paths such as `Rect.(Shape.Fill).(SolidColorBrush.Color)`.
- Dynamic resource setters now work on `FrameworkElement` and
  `FrameworkContentElement` targets, but non-framework targets are unsupported.
- Binding-valued setters now work by applying the captured `BindingBase` to the
  target property for the active state.
- WinUI attached properties that do not exist in ModernWpf, especially
  `AnimatedIcon.State`.
- State setter timing versus transitions: controls with transition storyboards
  must be converted carefully so immediate setter application does not preempt
  transition animations.

## Validation Hooks

- `TemplateParityTests.ProductTemplatesDoNotContainRawWinUIVisualStateSetters`
  guards against raw `<VisualState.Setters>` syntax in WPF XAML.
- `TemplateParityTests.ProductTemplatesUseVisualStateExForConvertedStateSetters`
  guards converted product templates so they keep using `VisualStateEx.Setters`.
