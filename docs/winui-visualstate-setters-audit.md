# WinUI VisualState.Setters Audit

This tracks WinUI `VisualState.Setters` parity for ModernWpf 1.x. The primary
source is the local final WinUI 2.8.7 checkout:
`D:\repos\microsoft-ui-xaml-v2.8.7\dev`. The current WinUI 3 checkout at
`D:\repos\microsoft-ui-xaml\src\controls\dev` was also counted to identify
newer deltas, but the repo-wide sync matrix is still anchored on WinUI 2.8.7.

## Status Legend

- `Converted`: ModernWpf product XAML uses `VisualStateEx.Setters`, or the
  equivalent WPF behavior is called out in the row notes where the WinUI setter
  target has no WPF attached-property equivalent.
- `StructuralGap`: mapped setter blocks were converted where the ModernWpf
  template has matching parts, but remaining WinUI targets belong to control
  surfaces or template branches that this WPF port does not currently carry.
- `RuntimeGap`: mapped setter blocks were converted where they can be expressed
  in XAML, but remaining parity needs a WPF runtime driver or platform feature
  before template setters can be selected correctly.
- `Pending`: the WinUI setter blocks map to an existing ModernWpf template and
  still need conversion or an explicit compatibility decision.
- `Unsupported`: the setter target needs runtime support that does not exist yet.
- `Excluded`: the upstream file maps to a WinUI surface this repo does not carry.

## Current Conversions

| WinUI 2.8.7 source | Setter blocks | ModernWpf file | Status | Notes |
| --- | ---: | --- | --- | --- |
| `dev\ProgressBar\ProgressBar.xaml` | 4 | `ModernWpf\ProgressBar\ProgressBar.xaml` | Converted | Direct opacity setters and the `UpdatingError` nested brush-color setter now use `VisualStateEx.Setters`; `VisualStateManagerEx` resolves WinUI-style nested target paths such as `(Shape.Fill).(SolidColorBrush.Color)`. Transition storyboards remain for animated progress movement and color transitions. |
| `dev\CommonStyles\AppBarSeparator_themeresources.xaml` | 2 | `ModernWpf.Controls\CommandBar\AppBarSeparator.xaml` | Converted | Compact and overflow layout states now use `VisualStateEx.Setters` instead of a `VisualStateGroupListener` trigger bridge. |
| `dev\CommonStyles\CalendarView_themeresources.xaml` | 2 | `ModernWpf\Styles\Calendar.xaml` | Converted | Stock WPF `Calendar` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\Calendar.xaml` uses WPF visual states/storyboards and WPF presenters rather than WinUI `VisualStateEx.Setters`. The previous WinUI-like Calendar navigation button setter path was deleted for the stock control. |
| `dev\CommonStyles\CommandBar_themeresources.xaml` | 3 | `ModernWpf.Controls\CommandBar\CommandBar.xaml` | Converted | The `DynamicOverflowEnabled` column-width setters and `CommandBarOverflowPresenter` full-width open-up/down padding and border-thickness setters now use `VisualStateEx.Setters`; the overflow presenter drives its display-mode states from popup placement. |
| `dev\InfoBadge\InfoBadge_themeresources.xaml` | 3 | `ModernWpf.Controls\InfoBadge\InfoBadge.xaml` | Converted | `Dot`, `Icon`, `FontIcon`, and `Value` state behavior now lives in `VisualStateEx.Setters`; code only chooses the state and creates the icon element. |
| `dev\InfoBar\InfoBar.xaml` | 8 | `ModernWpf.Controls\InfoBar\InfoBar.xaml` | Converted | Severity, icon, close-button, open/closed, and foreground states now use `VisualStateEx.Setters`; `VisualStateSetter` supports binding-valued setters for WinUI's `ForegroundSet` shape. |
| `dev\NumberBox\NumberBox.xaml` | 7 | `ModernWpf.Controls\NumberBox\NumberBox.xaml` | Converted | Disabled header, inline/compact spin-button placement, `PopupIndicator.Visibility`, `SpinButtonsColumn.Width`, and spin-button enabled states now use `VisualStateEx.Setters`; `NumberBox` drives `SpinButtonStates` on both the outer control and inner text box like WinUI. |
| `dev\PersonPicture\PersonPicture.xaml` | 5 | `ModernWpf.Controls\PersonPicture\PersonPicture.xaml` | Converted | Photo, placeholder/group, and badge states now use `VisualStateEx.Setters`, including the image-brush object setter for `BadgingEllipse.Fill`. The WPF template keeps its `FontIconFallback` placeholder mapping instead of WinUI's symbol-font `TextBlock` glyph setters. |
| `dev\ProgressRing\ProgressRing.xaml` | 1 | `ModernWpf.Controls\ProgressRing\ProgressRing.xaml` | Converted | The inactive opacity setter is represented with `VisualStateEx.Setters`; WinUI's `LottiePlayer.(AutomationProperties.AccessibilityView)=Raw` has no WPF attached-property equivalent, so ModernWpf uses `ProgressRingAutomationPeer.IsControlElementCore()` to remove inactive rings from the control view. |
| `dev\RadioButtons\RadioButtons.xaml` | 1 | `ModernWpf.Controls\RadioButtons\RadioButtons.xaml` | Converted | Disabled header foreground now uses a dynamic-resource `VisualStateEx.Setters` entry instead of a WPF template trigger. |
| `dev\CommonStyles\ScrollBar_themeresources.xaml` | 8 | `ModernWpf\Styles\ScrollBar.xaml` | Converted | Stock WPF `ScrollBar` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\ScrollBar.xaml` uses WPF `ControlTemplate.Triggers` and pointer-over storyboards rather than WinUI `VisualStateEx.Setters`. The previous WinUI-derived auto-hide, panning-thumb, `ScrollBarHelper`, and setter-backed template branches were deleted for the stock control. |
| `dev\SplitView\SplitView_themeresources.xaml` | 4 | `ModernWpf.Controls\SplitView\SplitView.xaml` | Converted | Closed compact, open inline left, and open compact overlay left direct setter blocks now use `VisualStateEx.Setters`; transition storyboards remain for animated pane movement. |
| `dev\TeachingTip\TeachingTip.xaml` | 29 | `ModernWpf.Controls\TeachingTip\TeachingTip.xaml` | Converted | Light-dismiss background, button/content/close-button-location/icon/hero-placement, tail-placement, title, and subtitle states now use `VisualStateEx.Setters` driven by `TeachingTip`; WPF popup placement, sizing, and animations remain code-driven. |
| `dev\CommonStyles\ToggleSwitch_themeresources.xaml` | 1 | `ModernWpf.Controls\ToggleSwitch\ToggleSwitch.xaml` | Converted | WinUI's pressed knob alignment setters are represented in ModernWpf's active `Dragging` state, because this WPF port routes thumb interaction through that state instead of `CommonStates.Pressed`. |
| `dev\MenuBar\MenuBarItem.xaml` | 3 | `ModernWpf.Controls\MenuBar\MenuBar.xaml` | Converted | MenuBarItem common/selected state setters now use `VisualStateEx.Setters` in the source-backed MenuBar template; the old WPF `Menu` / `MenuItem` mapping has been deleted. |

## Relevant Mapped WinUI 2.8.7 Sources

| WinUI 2.8.7 source | Setter blocks | ModernWpf mapping | Status | Main blocker or next action |
| --- | ---: | --- | --- | --- |
| `dev\AutoSuggestBox\AutoSuggestBox_themeresources.xaml` | 2 | `ModernWpf\Styles\AutoSuggestBox.xaml` | Converted | Query-button pointer-over and pressed `AnimatedIcon.State` setters now use `VisualStateEx.Setters` on the static `ContentPresenterEx` fallback while preserving the existing WPF trigger chrome. |
| `dev\Breadcrumb\BreadcrumbBar.xaml` | 16 | `ModernWpf.Controls\BreadcrumbBar\BreadcrumbBar.xaml` | Converted | Inline item, last item, ellipsis, ellipsis dropdown layout/focus, dropdown common states, and nested item-button common states now use `VisualStateEx.Setters` driven by `BreadcrumbBarItem`. WinUI `Control.IsTemplateFocusTarget` and `FocusVisualMargin` setter targets are represented by `FocusVisualHelper` on WPF template parts. |
| `dev\ColorPicker\ColorPicker.xaml` | 20 | `ModernWpf.Controls\ColorPicker\ColorPicker.xaml` | Converted | Top-level spectrum, preview, previous-color, slider, more-button, text-input visibility, color representation, RGB/HSV panel switching, alpha hex-length, horizontal-orientation layout, and color-picker slider thumb/focus setter blocks now use `VisualStateEx.Setters`. WPF keeps a functional `PART_Track` for `ColorPickerSlider` and routes WinUI's separate horizontal/vertical focus-engagement thumb targets through the source-named WPF thumb substitute. |
| `dev\ColorPicker\ColorSpectrum.xaml` | 5 | `ModernWpf.Controls\ColorPicker\ColorPicker.xaml` | Converted | Shape, pointer-over, pressed-large, focus, selection-ellipse color, and rectangle/ellipse border visibility states now use `VisualStateEx.Setters` driven by `ColorSpectrum`. |
| `dev\ComboBox\ComboBox_themeresources.xaml` | 10 | `ModernWpf\Styles\ComboBox.xaml` | Converted | Stock WPF `ComboBox` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\ComboBox.xaml` uses WPF `ControlTemplate.Triggers` rather than WinUI `VisualStateEx.Setters`. The previous WinUI-shaped `ComboBoxHelper`, editable split-corner state driver, `ContentPresenterEx`, and static animated-glyph fallback were deleted for the stock control. |
| `dev\CommandBarFlyout\CommandBarFlyout_themeresources.xaml` | 41 | `ModernWpf.Controls\CommandBarFlyout\CommandBarFlyout.xaml` | RuntimeGap | Flyout `AppBarButton` application-view overflow layout, common chrome, keyboard-accelerator visibility, and input-mode padding states now use `VisualStateEx.Setters`; flyout `AppBarToggleButton` application-view overflow layout, common/checked/overflow chrome, keyboard-accelerator visibility, and input-mode padding/check-glyph margin states also use setters. Toolbar expansion, available-command, combined placement/corner, and open-up/down animation behavior is represented with WPF storyboards plus setter-backed combined states. CommandBarFlyout secondary commands now propagate WPF touch/default input mode to AppBar entries. Remaining source gaps are WinUI `ThemeShadow`/shadow-caster semantics and game-controller/remote input-mode selection, both of which need WPF runtime/platform support rather than another template setter conversion. |
| `dev\CommonStyles\AppBarButton_themeresources.xaml` | 16 | `ModernWpf.Controls\CommandBar\AppBarButton.xaml` | RuntimeGap | Application-view layout, common chrome, input-mode padding, keyboard accelerator, and flyout chevron setters now use `VisualStateEx.Setters`; the regular template now includes WinUI's `SubItemChevronPanel`/`OverflowSubItemChevron` split, and `AppBarButton` now drives `OverflowSubMenuOpened` from `FlyoutBase.Opened`/`Closed` while preserving the overflow chevron. Normal `CommandBar` and `CommandBarFlyoutCommandBar` now propagate source-style touch-open overflow input mode to secondary AppBar buttons. Remaining gap: game-controller/remote input-mode selection needs WPF runtime/platform support. |
| `dev\CommonStyles\AppBarToggleButton_themeresources.xaml` | 18 | `ModernWpf.Controls\CommandBar\AppBarToggleButton.xaml` | RuntimeGap | Application-view layout, common/checked/overflow chrome, input-mode padding/check-glyph margin, check-glyph opacity, and keyboard accelerator setters now use `VisualStateEx.Setters` driven by `AppBarToggleButton` or available as setter-backed source parity groups. Normal `CommandBar` and `CommandBarFlyoutCommandBar` now propagate source-style touch-open overflow input mode to secondary AppBar toggle buttons. Remaining gap: game-controller/remote input-mode selection needs WPF runtime/platform support. |
| `dev\CommonStyles\Button_themeresources.xaml` | 3 | `ModernWpf\Styles\Button.xaml`, `ModernWpf.Controls\HyperlinkButton\HyperlinkButton.xaml` | Converted | Stock WPF `Button` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\Button.xaml` uses WPF `ControlTemplate.Triggers` rather than WinUI `VisualStateEx.Setters`. `HyperlinkButton` remains the WinUI-derived button-family control that uses setter-backed common states. `SubtleButtonStyle` is ModernWpf-specific: its resource aliases remain WinUI-derived, but its template now follows the official WPF Fluent trigger shape. |
| `dev\CommonStyles\ToggleButton_themeresources.xaml` | 11 | `ModernWpf\Styles\ToggleButton.xaml` | Converted | Stock WPF `ToggleButton` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\ToggleButton.xaml` uses WPF `MultiTrigger` entries rather than WinUI `VisualStateEx.Setters`. The previous stock-style WinUI indeterminate visual-state branch was removed; `IsChecked=null` follows official WPF Fluent fallback chrome. |
| `dev\CommonStyles\RepeatButton_themeresources.xaml` | 3 | `ModernWpf\Styles\RepeatButton.xaml` | Converted | Stock WPF `RepeatButton` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\RepeatButton.xaml` uses WPF `ControlTemplate.Triggers` rather than WinUI `VisualStateEx.Setters`. ModernWpf-only RepeatButton consumers still rely on the platform repeat behavior, but the stock style is no longer a WinUI setter-backed template. |
| `dev\CommonStyles\CalendarDatePicker_themeresources.xaml` | 1 | `ModernWpf\Styles\DatePicker.xaml` | Converted | Stock WPF `DatePicker` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\DatePicker.xaml` uses WPF `ControlTemplate.Triggers` rather than WinUI `VisualStateEx.Setters`. The previous WinUI `CalendarDatePicker` state-group mapping and `DatePickerHelper` driver were deleted for the stock control. |
| `dev\CommonStyles\CheckBox_themeresources.xaml` | 12 | `ModernWpf\Styles\CheckBox.xaml` | Converted | Stock WPF `CheckBox` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\CheckBox.xaml` uses WPF `Trigger` / `MultiTrigger` entries rather than WinUI `VisualStateEx.Setters`. The previous WinUI `CombinedStates` template, animated fallback glyph state, and `CheckBoxHelper` key/state driver were removed for the stock control. |
| `dev\CommonStyles\ContentDialog_themeresources.xaml` | 15 | `ModernWpf.Controls\ContentDialog\ContentDialog.xaml` | Converted | Dialog showing, full sizing, button visibility, default-button accent, and accent-border direct setters now use `VisualStateEx.Setters`; WPF show/hide transition storyboards remain for popup animation. |
| `dev\CommonStyles\DatePicker_themeresources.xaml` | 1 | Excluded | Excluded | WinUI's selector-style `DatePicker` is a day/month/year picker, while ModernWpf's existing WPF platform `DatePicker` now follows official WPF Fluent. This phase does not add new controls, so the WinUI selector control remains excluded. |
| `dev\CommonStyles\MenuFlyout_themeresources.xaml` | 42 | `ModernWpf.Controls\MenuFlyout\MenuFlyout.xaml`, `ModernWpf\Styles\MenuItem.xaml`, `ModernWpf.Controls\RadioMenuItem\RadioMenuItem.xaml` | StructuralGap | WPF regular submenu items now use `VisualStateEx.Setters` for common chrome, checked opacity, and keyboard-accelerator visibility; WPF submenu headers now use setters for common, submenu-open, and disabled chrome. Both are driven by `MenuItemHelper`, and radio menu items already use setters. Remaining WinUI placeholder/icon margin states are represented by WPF grid columns and shared item structure, narrow padding is handled by WPF style selection, reveal setters require WinUI's RevealBrush runtime, and the alternate `MenuFlyoutSubItem` branches do not have a separate ModernWpf control surface. |
| `dev\DropDownButton\DropDownButton.xaml` | 3 | `ModernWpf.Controls\DropDownButton\DropDownButton.xaml` | Converted | The WPF template now has WinUI-compatible `AnimatedIcon.State` setters on the static `FontIconFallback` chevron for pointer-over, pressed, and disabled states. The attached property preserves the source state contract without implementing WinUI animated-vector playback. |
| `dev\Expander\Expander.xaml`, `dev\Expander\Expander_themeresources.xaml` | 15 | `ModernWpf\Styles\Expander.xaml` | Excluded | Stock WPF `Expander` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\Expander.xaml` uses WPF `ControlTemplate.Triggers` and official WPF presenter slots rather than WinUI `VisualStateEx.Setters`. The previous WinUI-shaped header toggle, `ToggleButtonHelper`, `ContentPresenterEx`, and fallback animated-glyph guesses were deleted for this stock control. |
| `dev\NavigationView\NavigationBackButton.xaml` | 2 | `ModernWpf\Styles\NavigationBackButton.xaml` | Converted | The WPF back-button template no longer uses a `ControlTemplate.Triggers` chrome matrix. Pointer-over, pressed, and disabled foreground/background effects now live in source-shaped `CommonStates` through `VisualStateEx.Setters`, and the pointer-over/pressed `AnimatedIcon.State` setters still record the source animated-back state on the static `FontIconFallback`. |
| `dev\NavigationView\NavigationView.xaml` | 21 | `ModernWpf.Controls\NavigationView\NavigationView.xaml` | Converted | `NavigationViewItemSeparator` horizontal-compact and vertical line states now use `VisualStateEx.Setters`; toggle-pane visible, header, AutoSuggest, closed-compact pane, compact pane-list size, back-button collapsed, pane-collapsed, pane separator visibility, titlebar collapsed, overflow no-label, display-mode minimal/top-minimal, pane-not-overlaying, NavigationViewItem item-position, and NavigationViewItemHeader collapsed/top-mode states also now use setters. The WPF template now includes the source `ShadowCaster` target and WPF `TranslateTransform` substitute, so compact list, pane collapsed, and pane-overlay state setters can target `ShadowCaster.Width` / `ShadowCaster.Opacity` like the WinUI source. |
| `dev\NavigationView\NavigationView_rs1_themeresources.xaml` | 71 | `ModernWpf.Controls\NavigationView\NavigationView.xaml`, `ModernWpf\Styles\NavigationView.xaml` | StructuralGap | The left-pane `NavigationViewItemPresenter` pointer, disabled, icon-collapsed, InfoBadge collapsed, chevron-visible, pointer-chevron, and closed-compact top-level item states now use `VisualStateEx.Setters`, including WinUI's closed-compact `InfoBadgePresenter` attached-grid and alignment setters. The top-pane and top-pane overflow presenter InfoBadge collapsed, chevron-visible, and pointer-chevron states now use setters, and `NavigationViewItem.InfoBadge` is forwarded through the WinUI presenter slots. The top-pane settings item pointer and disabled states now use setters and now exposes the WinUI `InfoBadgePresenter` slot. Top navigation overflow button and pane toggle button `CommonStates` now use setters through `ButtonHelper.VisualStateSettersEnabled`, and pane-toggle, settings, `NavigationViewItemPresenter` item icons, and expand/collapse chevrons now have WinUI `AnimatedIcon.State` pointer/pressed setters on the WPF fallback targets. Remaining upstream branches are structural WinUI template surfaces that are not represented by the WPF NavigationView port. |
| `dev\PagerControl\PagerControl.xaml` | 16 | `ModernWpf.Controls\PagerControl\PagerControl.xaml` | Converted | Navigation button visibility, hidden-on-edge, enabled states, number-box/combo-box/button-panel display modes, infinite-item labels, and last-button hiding now use `VisualStateEx.Setters` against source-named template parts. Pager-owned navigation and page-number button styles now also use setter-backed source-shaped button templates. WPF substitutions remain for `AutomationProperties.LandmarkType`, `AutomationProperties.AccessibilityView=Raw`, and `RepositionThemeTransition`, which are not VisualState setter targets in this WPF surface. |
| `dev\RadioMenuFlyoutItem\RadioMenuFlyoutItem_themeresources.xaml` | 13 | `ModernWpf.Controls\RadioMenuItem\RadioMenuItem.xaml` | StructuralGap | The default radio menu item common, check/icon, keyboard accelerator, and check-glyph state effects now use `VisualStateEx.Setters` driven by `RadioMenuItem`; WinUI's separate `MenuFlyoutSubItem` branch and text placeholder margin are covered by WPF grid columns instead of WinUI margin shifting, so a literal setter port would double-apply indentation. |
| `dev\RatingControl\RatingControl.xaml` | 6 | `ModernWpf.Controls\RatingControl\RatingControl.xaml` | Converted | Disabled, placeholder, pointer-over placeholder/unselected, set, and pointer-over set foreground states now use `VisualStateEx.Setters` instead of the WPF listener/trigger bridge. |
| `dev\SplitButton\SplitButton.xaml`, `dev\SplitButton\SplitButton_themeresources.xaml` | 34 | `ModernWpf.Controls\SplitButton\SplitButton.xaml`, `ModernWpf.Controls\CommandBar\CommandBar.xaml` | Converted | Default SplitButton/ToggleSplitButton common-state chrome, secondary-button placement, nested inner-button disabled template, and command-bar `SplitButtonCommandBarStyle` state setters now use `VisualStateEx.Setters` without the old default-template `VisualStateGroupListener` bridge. Inner primary/secondary button pointer-over and pressed `AnimatedIcon.State` setters target the WPF `ContentPresenterEx` fallback; command-bar background surfaces keep WPF `Border` elements with the source target names for corner-radius and setter behavior. |
| `dev\TabView\TabView.xaml` | 18 | `ModernWpf\Styles\TabControl.xaml` | Excluded | Stock WPF `TabControl` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\TabControl.xaml` uses WPF presenters and WPF trigger/style behavior rather than WinUI `VisualStateEx.Setters`. The previous `TabItemHelper` / `TabControlHelper` state and header/footer guesses were deleted. Full WinUI `TabView` list-view chrome, close-button, compact-width, drag/reorder, data-placeholder, and overflow states remain excluded because this phase does not add a `TabView` control. |
| `dev\TitleBar\TitleBar.xaml`, `dev\TitleBar\TitleBar_themeresources.xaml` | 8 | `ModernWpf\TitleBar\TitleBarControl.xaml`, `ModernWpf\TitleBar\TitleBarButton.xaml` | StructuralGap | TitleBar activation, back-button visibility, icon visibility, title-text visibility, WPF extend-titlebar chrome states, and the WinUI 2 `TitleBarBackButtonStyle` `AnimatedIcon.State` setters now use `VisualStateEx.Setters`; newer WinUI 3 subtitle/header/content/display-mode state groups and full-screen/expanded-height states target titlebar surfaces that the WPF port does not currently expose. |
| `dev\TreeView\TreeViewItem.xaml` | 10 | `ModernWpf\Styles\TreeView.xaml`, `ModernWpf\Styles\TreeViewItem.xaml` | Converted | Stock WPF `TreeView` is now intentionally governed by official WPF Fluent, so `ModernWpf\Styles\TreeViewItem.xaml` uses WPF triggers rather than WinUI `VisualStateEx.Setters`. The previous WinUI-derived `TreeViewItemHelper`, indentation helper, `ContentPresenterEx`, `FontIconFallback`, and setter-backed selected-state matrix were deleted for the stock control. |

## Excluded Or Non-Product Sources

| WinUI 2.8.7 source | Setter blocks | Reason |
| --- | ---: | --- |
| `dev\AnimatedIcon\TestUI\AnimatedIconPage.xaml` | 12 | TestUI sample only; `AnimatedIcon` itself is excluded from ModernWpf core. |
| `dev\CommonStyles\InkToolbar_themeresources.xaml` | 112 | InkToolbar is not carried as a ModernWpf control. |
| `dev\CommonStyles\MediaTransportControls_themeresources.xaml` | 11 | Media transport controls are not carried as ModernWpf controls. |
| `dev\CommonStyles\TimePicker_themeresources.xaml` | 1 | TimePicker is not carried as a ModernWpf control. |
| `dev\Materials\Reveal\*.xaml` and `dev\Materials\Reveal\TestUI\*.xaml` | 331 | WinUI compositor/reveal material system is excluded from the WPF port. |

## Runtime Gaps Blocking Full Conversion

- Nested target paths such as `Rect.(Shape.Fill).(SolidColorBrush.Color)`.
- Dynamic resource setters now work on `FrameworkElement` and
  `FrameworkContentElement` targets, but non-framework targets are unsupported.
- Binding-valued setters now work by applying the captured `BindingBase` to the
  target property for the active state.
- `AnimatedIcon.State` now has a WPF attached-property compatibility surface,
  but ModernWpf still renders static fallback glyphs instead of WinUI animated
  vector playback.
- State setter timing versus transitions: controls with transition storyboards
  must be converted carefully so immediate setter application does not preempt
  transition animations.

## Validation Hooks

- `TemplateParityTests.ProductTemplatesDoNotContainRawWinUIVisualStateSetters`
  guards against raw `<VisualState.Setters>` syntax in WPF XAML.
- `TemplateParityTests.ProductTemplatesUseVisualStateExForConvertedStateSetters`
  guards converted product templates so they keep using `VisualStateEx.Setters`.
