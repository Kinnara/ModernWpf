# WinUI 3 Source Parity

ModernWpf parity work now targets WinUI 3 source behavior for existing controls only.

Source of truth: `D:\repos\microsoft-ui-xaml`

Local source snapshot used for the first round:

```text
c70471c511a0168b61dcca13af9556465f26b673
reference/winui3-current
```

The checkout was 12 commits behind `origin/winui3/main` when this note was created; refresh the snapshot before making claims about the latest WinUI 3 behavior.

## Rules

- Prefer WinUI 3 implementation behavior over old ModernWpf behavior.
- Do not add new controls in this phase.
- Do not preserve old ModernWpf behavior when it conflicts with source-backed WinUI 3 behavior.
- Treat guessed ModernWpf implementations as debt, not as a maintenance baseline. When WinUI 3 source exists for an existing control, delete/replace/adapt the guessed implementation as a whole-control parity slice instead of layering small compatibility patches on top of it.
- Port source behavior directly where WPF has an equivalent model.
- Where WinUI depends on compositor, popup island, WinRT automation, gamepad, or platform-only services, document the WPF substitution and add tests around the substitute behavior.
- Keep WinUI 2.8.7 notes only as historical/resource-reference material unless a slice explicitly needs that baseline.

## Recent Source-Parity Notes

- 2026-05-17 MenuFlyout cancelable Closing: WinUI `MenuFlyout::OnClosing` delegates to `FlyoutBase::OnClosing` and keeps the menu open when `Cancel` is true; ModernWpf now routes the same cancellation path through its WPF `ContextMenu` presenter and completes `Closed` exactly once from the `IsOpen=false` transition.
- 2026-05-17 FlyoutBase Target property: WinUI XamlOM defines `FlyoutBase.Target` as a read-only dependency property with an internal setter; ModernWpf now exposes the same WPF read-only dependency property and tracks it for both `Flyout` and `MenuFlyout` while open.
- 2026-05-17 ToggleSwitch pointer-capture/style cleanup: WinUI clears `PointerOver` from `OnPointerCaptureLost` after vertical-pan drag completion and CommonStyles does not set `VerticalContentAlignment`; ModernWpf now hooks the WPF thumb capture-lost path and removes the extra guessed vertical-content setter.
- 2026-05-17 ToggleSwitch source-audit correction: `docs\toggleswitch-winui3-source-audit.md` maps the current WPF implementation against WinUI 3 `ToggleSwitch_Partial`, XamlOM, CommonStyles, and native tests, but this is not a completed replacement. ToggleSwitch remains guessed implementation debt until the control is replaced/adapted as a whole from WinUI 3 source; the keyboard path now follows WinUI's `OriginalKey` shape by normalizing WPF system/IME/dead-char keys before source-style key processing.
- 2026-05-17 FlyoutBase cancelable Closing: WinUI `HideImpl` calls `OnClosing` and aborts hide when `FlyoutBaseClosingEventArgs.Cancel` is true; ModernWpf now exposes the source-shaped `Closing` event and `Cancel` property, then restores WPF `Popup.IsOpen` on canceled closes while suppressing the synthetic reopen notification.
- 2026-05-17 ToggleSwitch owner focus states: WinUI `ChangeVisualState` branches from the ToggleSwitch owner's `FocusState`, not focus inside template children; ModernWpf now uses owner keyboard focus for `Focused` / `PointerFocused` routing and pointer mouse-down focuses the owner when a custom template child previously held focus.
- 2026-05-17 ToggleSwitch canceled drag completion: `ToggleSwitch_Partial.cpp` returns immediately when `DragCompletedHandler` receives `Canceled=true`; ModernWpf now leaves the dragging state/translations untouched on that path and tests the resulting `Pressed` / `Dragging` states.
- 2026-05-17 ToggleSwitch thumb tap bridge: WinUI registers `Tapped` on `SwitchThumb`, and its tap handler runs after drag cleanup; ModernWpf now uses a handled bubbling `MouseLeftButtonUp` hook instead of the old preview mouse-up hook so the WPF `Thumb` completes `DragCompleted` before the tap substitute toggles.
- 2026-05-17 ToggleSwitch size updates: WinUI recalculates translation bounds only from the knob and knob-bounds `SizeChanged` handler; ModernWpf deleted the old template-apply/root render-size fallback and now tests knob-bounds resizing through the same part-size path.
- 2026-05-17 ToggleSwitch source test parity: added coverage for the WinUI native test cases where horizontal drag toggles both on and off, explicit `AutomationProperties.Name` wins over computed names, and style-provided On/Off content is included in automation names.
- 2026-05-17 ToggleSwitch automation string extraction: removed the guessed arbitrary `ToString()` fallback and matched WinUI's `FrameworkElement::GetStringFromObject` shape for known property values and unknown non-text objects.
- 2026-05-17 ToggleSwitch automation peer source shape: replaced the hard-coded localized control type with a ToggleSwitch resource matching WinUI's `UIA_AP_TOGGLESWITCH` lookup, and routed `IToggleProvider.Toggle` through `AutomationToggleSwitchOnToggle` before the owner toggles state.
- 2026-05-17 ToggleSwitch template source target: confirmed the packaged WinUI 3 style merge includes `src\controls\dev\CommonStyles\ToggleSwitch_themeresources.xaml` after the framework `dxaml` generic style, so the ModernWpf 40px track, 12px knob, knob growth animations, and pressed knob margins are source-backed. Removed the extra WPF-only `Off` state transform storyboard and added template-geometry tests to protect the CommonStyles source shape.
- 2026-05-17 ToggleSwitch native lifecycle/tap parity: added WPF coverage for WinUI native `CanInstantiate`, `CanEnterAndLeaveLiveTree`, and `CanToggleUsingTapInput`, plus a WPF tap-bridge guard test that prevents a post-drag mouse-up from double-toggling after horizontal drag already changed state.
- 2026-05-17 ToggleSwitch key process source shape: replaced the remaining inline guessed key-up toggle path with a private WPF port of WinUI's `ToggleSwitchKeyProcess` helper, keeping the source flow-direction branches behind `HandlesKey`; directional-key coverage now verifies both LTR and RTL stay non-toggle like WinUI's native test.
- 2026-05-17 AppBar automation/flyout source shape: WinUI creates explicit `AppBarButtonAutomationPeer` / `AppBarToggleButtonAutomationPeer`, derives automation name from `AutomationProperties.Name` or `Label` without `Content` fallback, reports source localized control-type strings and trimmed keyboard accelerator text, exposes Toggle automation through `AutomationToggleButtonOnToggle`, and opens associated AppBarButton flyouts from click/expand paths; ModernWpf now ports these WPF equivalents.
- 2026-05-17 AppBar button pointer focus: WinUI native `AllowFocusOnInteraction` tests verify `AppBarButton` and `AppBarToggleButton` reject pointer focus by default while still accepting keyboard focus; ModernWpf now cancels WPF mouse-origin focus for both controls and tests the WPF substitute.
- 2026-05-17 AppBar empty-label presence: WinUI `GetHasLabelAtPosition` treats any non-null `Label` as present, including the default empty string; ModernWpf now follows that rule for both AppBar button types when CommandBar decides whether bottom-label primary commands require the overflow button.

## Active Source Parity Matrix

| Area | WinUI 3 source files | ModernWpf files | Status | Evidence |
| --- | --- | --- | --- | --- |
| ToggleSwitch | `src\dxaml\xcp\dxaml\lib\ToggleSwitch_Partial.cpp`, `src\dxaml\xcp\dxaml\lib\ToggleSwitch_Partial.h`, `src\dxaml\xcp\dxaml\lib\ToggleSwitchAutomationPeer_Partial.cpp`, `src\dxaml\xcp\dxaml\dllsrv\winrt\Microsoft.UI.Xaml.Common.rc`, `src\controls\dev\CommonStyles\ToggleSwitch_themeresources.xaml`, `src\dxaml\xcp\dxaml\themes\generic.xaml`, `src\dxaml\xcp\components\controls\KeyDownUp\inc\ToggleSwitchKeyProcess.h`, `src\dxaml\xcp\tools\XCPTypesAutoGen\XamlOM\Model\Microsoft.UI.Xaml.Controls.cs`, `src\dxaml\xcp\tools\XCPTypesAutoGen\XamlOM\Model\Microsoft.UI.Xaml.Controls.Primitives.cs`, `src\dxaml\test\native\external\controls\toggleswitch\ToggleSwitchIntegrationTests.cpp`, `src\dxaml\test\native\external\controls\toggleswitch\ToggleSwitchAutomationIntegrationTests.cpp` | `ModernWpf.Controls\ControlHeaderPlacement.cs`, `ModernWpf.Controls\ToggleSwitch\ToggleSwitch.cs`, `ModernWpf.Controls\ToggleSwitch\ToggleSwitch.xaml`, `ModernWpf.Controls\ToggleSwitch\ToggleSwitchTemplateSettings.cs`, `ModernWpf.Controls\ToggleSwitch\ToggleSwitchAutomationPeer.cs`, `ModernWpf.Controls\ToggleSwitch\Strings\Resources.resx`, `ModernWpf\ModernWpfControlsResources.xaml`, `ModernWpf\ThemeResources\Light.xaml`, `ModernWpf\ThemeResources\Dark.xaml`, `ModernWpf\ThemeResources\HighContrast.xaml`, `test\ModernWpf.WinUI.Tests\ToggleSwitch\ToggleSwitchApiTests.cs`, `docs\toggleswitch-winui3-source-audit.md` | Audited; guessed implementation replacement incomplete | ToggleSwitch has source-aligned patches and tests documented in `docs\toggleswitch-winui3-source-audit.md`, but the current control must still be treated as guessed implementation debt until it is replaced/adapted as a whole-control WinUI 3 port. Existing source-aligned pieces include WinUI-shaped pointer/drag fields, WinUI focusability and pointer-origin `PointerFocused` focus-state routing, WinUI pointer-capture-lost cleanup for vertical-pan `PointerOver` state through the WPF thumb capture-lost path, WinUI `UseSystemFocusVisuals` and focus-visual margin defaults, CommonStyles style setter shape without the old guessed `VerticalContentAlignment=Center`, unhandled drag routed-event semantics, `TemplateSettings` offset object, `GetTranslations`/`SetTranslations`/`MoveDelta`/`MoveCompleted` methods, size-based knob bounds, part `SizeChanged`-only translation-bound updates without the old WPF root render-size fallback, deletion of the old WPF-only thumb bitmap-cache/DPI workaround that WinUI 3 source does not have, packaged CommonStyles template geometry with 40px track, 12px knob, source-defined 12/14/17 knob state animations, pressed knob alignment/margins, and empty `Off` state, `Normal`, `PointerOver`, and `Pressed` common states with WinUI-shaped on-track/knob resource setters plus WPF-safe off-track/container color resets, WinUI 3 color-alias theme resources for Light/Dark off-track/container animations and the HighContrast ToggleSwitch resource block, WinUI's header-presenter visibility rule of visible when `Header` is non-null or `HeaderTemplate` exists, WinUI's `ControlHeaderPlacement` / `HeaderPlacement` property and `TopHeader` / `LeftHeader` visual-state dispatch for custom templates plus WinUI 3 default-theme omission of `HeaderStates`, WinUI XamlOM `AffectsMeasure` metadata for the measured ToggleSwitch dependency properties, Pressed-vs-Dragging split, a WPF port of WinUI's `ToggleSwitchKeyProcess` with source-shaped Space key-down/up, pending-key reset, flow-direction branches guarded by `HandlesKey`, `OriginalKey` normalization, and no handled key-up when no toggle occurs, WinUI's directional-key non-toggle behavior, `GetDefaultValue2`-style On/Off dependency-property defaults, default On/Off automation-name filtering, `FrameworkElement::GetStringFromObject`-style automation name extraction for text content, WinUI property values, and ignored unknown non-text objects, `GetClickablePoint` routing to the thumb, resource-backed localized control type matching WinUI's `UIA_AP_TOGGLESWITCH`, `IToggleProvider.Toggle` routed through `AutomationToggleSwitchOnToggle`, WPF automation peer child filtering as the source-shaped substitute for WinUI's Raw template parts, WinUI's rule that dragging updates `Dragging` but leaves `OnContent`/`OffContent` unchanged, disabled common-state foreground/fill/knob resource setters moved out of WPF `ControlTemplate.Triggers` into `VisualStateEx.Setters` with matching disabled brush resource aliases, and generated protected callback paths for `OnToggled`, `OnHeaderChanged`, `OnOffContentChanged`, and `OnOnContentChanged`. Tests cover native instantiation/live-tree entry/leave, tap input toggling on and off, post-drag mouse-up suppression for the WPF tap bridge, threshold drag, bidirectional horizontal drag toggling, vertical-drag no-move behavior, pointer-capture-lost `PointerOver` cleanup, unhandled drag routed-event bubbling, tap-vs-drag completion, Toggled event raising, focusable/focus-visual defaults and keyboard vs pointer focus-state routing, CommonStyles style setter defaults, dragging/content visual-state separation, header-presenter null rules, `HeaderPlacement` custom-template state dispatch and default-template `HeaderStates` omission, source-model `AffectsMeasure` metadata, generated protected callbacks, part-size-driven template setting offsets, Normal off-track/container reset behavior, Normal, PointerOver, Pressed, and Disabled visual-state setters, CommonStyles template geometry and source-defined knob size animations, ToggleSwitch theme-resource aliases, keyboard handling including LTR/RTL directional-key non-toggle coverage, default-value source semantics, automation peer source shape and hidden template children, automation localized-control-type resource routing, automation naming including explicit and style-provided content cases, property-value conversion, and unknown-object filtering, thumb clickable point, no source-absent thumb cache mode, complete WinUI footprint sizing, and presenter shape. WPF substitutions: `GamepadA`, `ManipulationMode=System,TranslateX`, root `Grid CornerRadius` represented by a WPF `Border`, `RepositionThemeAnimation`, element sounds, direct XAML `AutomationProperties.AccessibilityView=Raw`, and WinRT automation APIs are not available directly; the framework `dxaml` generic template also remains in the WinUI tree, but the package merge appends the CommonStyles template, which is the ModernWpf template target for this slice. |
| FlyoutBase / Flyout / MenuFlyout | `src\dxaml\xcp\dxaml\lib\FlyoutBase_partial.cpp`, `src\dxaml\xcp\dxaml\lib\MenuFlyout_Partial.cpp`, `src\dxaml\xcp\dxaml\lib\winrtgeneratedclasses\FlyoutBaseClosingEventArgs.g.cpp`, `src\dxaml\xcp\tools\XCPTypesAutoGen\XamlOM\Model\Microsoft.UI.Xaml.Controls.Primitives.cs` | `ModernWpf.Controls\Flyout\FlyoutBase.cs`, `ModernWpf.Controls\Flyout\FlyoutBaseClosingEventArgs.cs`, `ModernWpf.Controls\MenuFlyout\MenuFlyout.cs`, `ModernWpf.Controls\MenuFlyout\MenuFlyoutPresenter.cs`, `ModernWpf\Controls\Primitives\PopupEx.cs`, `test\ModernWpf.WinUI.Tests\Flyout\FlyoutBaseApiTests.cs`, `test\ModernWpf.WinUI.Tests\MenuFlyout\MenuFlyoutApiTests.cs` | Guessed implementation; source-shaped replacement in progress | WinUI XamlOM defines `FlyoutBase.Target` as a read-only dependency property with an internal setter; ModernWpf now exposes a read-only WPF dependency property, sets it while `Flyout` and `MenuFlyout` are open, preserves it through canceled `Closing`, and clears it after close. WinUI `HideImpl` asks `OnClosing` for cancellation before hiding; `OnClosing` raises public `FlyoutBase.Closing`, reads `FlyoutBaseClosingEventArgs.Cancel`, and returns without hiding when canceled. WinUI `MenuFlyout::OnClosing` delegates to the same base path and only closes submenus when not canceled. ModernWpf now exposes `Closing`, moves the args type into the `Controls.Primitives` namespace, stores `Cancel`, shares an `OnClosing` helper, bridges cancellation through both `PopupEx` and the WPF `ContextMenu`-based `MenuFlyoutPresenter`, and suppresses WPF synthetic `Opened` notifications caused by restoring `IsOpen`. WPF substitution: WPF popup/menu `IsOpen` has already flipped by the metadata callback, so cancellation is modeled by restoring `IsOpen`; for `MenuFlyout`, final `Closed` cleanup is guarded and completed from the `IsOpen=false` transition because `ContextMenu.Closed` is not reliable after a canceled close/reopen cycle. |
| CommandBarFlyout | `src\controls\dev\CommandBarFlyout\CommandBarFlyout.cpp`, `src\controls\dev\CommandBarFlyout\CommandBarFlyoutCommandBar.cpp`, `src\controls\dev\CommandBarFlyout\CommandBarFlyout_themeresources.xaml` | `ModernWpf.Controls\CommandBarFlyout\CommandBarFlyout.cs`, `ModernWpf.Controls\CommandBarFlyout\CommandBarFlyoutCommandBar.cs`, `ModernWpf.Controls\CommandBarFlyout\CommandBarFlyoutOverflowPanel.cs`, `ModernWpf.Controls\CommandBarFlyout\CommandBarFlyout.xaml`, `test\ModernWpf.WinUI.Tests\CommandBarFlyout\CommandBarFlyoutApiTests.cs` | Source-backed WPF port in progress | Deleted the old `CommandBarFlyoutToolBar` WPF `ToolBar` host and moved flyout command behavior onto `CommandBarFlyoutCommandBar`, matching WinUI's implementation shape. Replaced the indirect `CommandBarOverflowPanel`/`ToolBarOverflowPanel` dependency with `CommandBarFlyoutOverflowPanel`, so `ModernWpf.Controls\CommandBarFlyout` has no WPF toolbar references. Constructor defaults, `AlwaysExpanded` show mode, secondary command property-change sizing refresh, template setting calculation, size-change visual-state refresh, command collection mirroring, available/combined visual states, toolbar-free secondary panel, flyout overflow application-view states, and source-shaped AppBar button overflow params now have focused tests. WPF keeps a narrow deferred sizing refresh because `TemplateBinding` text measurement lags the dependency-property callback; remaining platform-only gaps include compositor/system backdrop, WinRT automation `MenuOpened/MenuClosed`, and exact WinUI popup `ActualPlacement` behavior. |
| AppBarButton / AppBarToggleButton | `src\dxaml\xcp\dxaml\lib\AppBarButton_Partial.cpp`, `src\dxaml\xcp\dxaml\lib\AppBarToggleButton_Partial.cpp`, `src\dxaml\xcp\dxaml\lib\AppBarButtonAutomationPeer_Partial.cpp`, `src\dxaml\xcp\dxaml\lib\AppBarToggleButtonAutomationPeer_Partial.cpp`, `src\dxaml\xcp\dxaml\lib\AppBarButtonHelpers.h`, `src\dxaml\xcp\dxaml\lib\CommandBar_Partial.cpp`, `src\controls\dev\CommonStyles\AppBarButton_themeresources.xaml`, `src\controls\dev\CommonStyles\AppBarToggleButton_themeresources.xaml`, `src\dxaml\test\native\external\foundation\input\Focus\AllowFocusOnInteractionTests.cpp`, `src\dxaml\test\native\external\controls\appbarbutton\AppBarButtonAutomationIntegrationTests.cpp`, `src\dxaml\test\native\external\controls\appbartogglebutton\AppBarToggleButtonAutomationIntegrationTests.cpp` | `ModernWpf.Controls\CommandBar\AppBarButton.cs`, `ModernWpf.Controls\CommandBar\AppBarToggleButton.cs`, `ModernWpf.Controls\CommandBar\AppBarButtonAutomationPeer.cs`, `ModernWpf.Controls\CommandBar\AppBarToggleButtonAutomationPeer.cs`, `ModernWpf.Controls\CommandBar\AppBarElementProperties.cs`, `ModernWpf.Controls\CommandBar\CommandBarToolBar.cs`, `ModernWpf.Controls\CommandBar\CommandBarPanel.cs`, `ModernWpf.Controls\CommandBar\AppBarButton.xaml`, `ModernWpf.Controls\CommandBar\AppBarToggleButton.xaml`, `ModernWpf.Controls\CommandBarFlyout\CommandBarFlyout.xaml`, `test\ModernWpf.WinUI.Tests\CommandBar\CommandBarApiTests.cs`, `test\ModernWpf.WinUI.Tests\CommandBarFlyout\CommandBarFlyoutApiTests.cs` | Source-backed WPF port in progress | Replaced the guessed per-control overflow model with the WinUI source shape: `UseOverflowStyle`, `KeyboardAcceleratorTextOverride`, `SetOverflowStyleParams`, template settings for shared keyboard accelerator text width, source-style overflow application-view states, and overflow common states. The label-position path now follows WinUI's `SetDefaultLabelPosition`/`GetEffectiveLabelPosition`/`GetHasLabelAtPosition` shape instead of peeking at local WPF DP state; standalone AppBar buttons keep source-default bottom labels, empty-string labels count as present like WinUI's non-null `HSTRING` check, CommandBar propagates its default label position into hosted buttons, explicit `LabelPosition=Collapsed` wins, and CommandBar auto overflow-button visibility now accounts for visible primary commands with bottom labels. `CommandBarOverflowPanel` and `CommandBarFlyoutOverflowPanel` compute the WinUI `hasIcons`/`hasToggleButtons`/`hasKeyboardAcceleratorText` params and reset them when controls leave overflow. Pointer focus now follows the WinUI native `AllowFocusOnInteraction` tests: mouse-origin focus is suppressed for `AppBarButton` and `AppBarToggleButton`, while direct/keyboard focus remains allowed. AppBar automation now follows WinUI's explicit peer shape for class names, localized control types, `AutomationProperties.Name`/`Label` name fallback without inherited `Content` fallback, trimmed keyboard accelerator text, no template control children, Toggle automation through `AutomationToggleButtonOnToggle`, and AppBarButton flyout expand/collapse. AppBarButton click now opens the associated `Flyout` because the WPF `FlyoutService` click hook is replaced by the AppBarButton AddOwner callback. Tests cover AppBar peer shape, accelerator/name behavior, flyout click/open/close/expand/collapse, and toggle automation command execution. WPF substitutions: `TextTrimming="Clip"` is not a WPF enum value, automatic touch/gamepad input-mode switching is not wired, WinUI's keyboard-present check for accelerator text is approximated, localized AppBar automation type strings use the WinUI English source strings until localized resource packs are added, WPF has no public `FocusState.Pointer` so mouse-down focus cancellation is used as the portable substitute, AppBarButton flyout expansion uses `Flyout.ShowAt`/`Hide` rather than WinUI `CascadingMenuHelper`/`FlyoutShowOptions`, and normal `CommandBar` still uses WPF `ToolBar` hosting for overflow layout while the AppBar controls no longer depend on direct `ToolBar.IsOverflowItem` state. Remaining AppBar work should continue deleting old guessed behavior in whole-control slices rather than patching around it. |
| AppBarSeparator / AppBarElementContainer | `src\dxaml\xcp\dxaml\lib\AppBarSeparator_Partial.cpp`, `src\dxaml\xcp\dxaml\lib\AppBarElementContainer_Partial.cpp`, `src\dxaml\xcp\tools\XCPTypesAutoGen\Modules\Controls\AppBarButton.cs`, `src\dxaml\xcp\tools\XCPTypesAutoGen\Modules\Controls\AppBarElementContainer.cs`, `src\controls\dev\CommonStyles\AppBarSeparator_themeresources.xaml` | `ModernWpf.Controls\CommandBar\AppBarSeparator.cs`, `ModernWpf.Controls\CommandBar\AppBarElementContainer.cs`, `ModernWpf.Controls\CommandBar\AppBarElementProperties.cs`, `test\ModernWpf.WinUI.Tests\CommandBar\CommandBarApiTests.cs` | Source-backed WPF port | Deleted the old WPF `ToolBar.OverflowModeProperty` / `ToolBar.IsOverflowItemProperty` callbacks for both controls and routed overflow through the WinUI `ICommandBarOverflowElement.UseOverflowStyle` model already used by AppBar buttons. Removed the now-dead shared attached `ApplicationViewState`/`TryGetOverflowState` relay. Tests now assert separator overflow visual state and element-container `IsInOverflow` through `UseOverflowStyle`; static search leaves no direct WPF ToolBar overflow callbacks in `CommandBar` or `CommandBarFlyout`. |
