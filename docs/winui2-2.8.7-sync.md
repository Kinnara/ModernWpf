# WinUI 2.8.7 Sync Matrix

This is now a legacy/reference matrix. Current parity work targets WinUI 3 source behavior for existing ModernWpf controls; see `docs/winui3-source-parity.md`.

Source of truth: `D:\repos\microsoft-ui-xaml-v2.8.7`, a detached worktree at tag `v2.8.7`

Verified tag:

```text
232a16e5ddfc22c9a1b79a2c51abeb9a39a94494
2025-01-31 18:58:01 +0000
Merged PR 12217986: WebView2 test fixes and update TSA options area path
```

This file tracks the ModernWpf parity plan against final WinUI 2.8.7. Every upstream area should end in one of these states:

- `Parity`: implemented in ModernWpf with the core WinUI user semantics and upstream-derived tests.
- `WPF-equivalent`: covered by an existing WPF or ModernWpf equivalent with tests.
- `Functional subset`: intentionally smaller than WinUI, but includes the core user-facing behavior advertised by the control.
- `API/template shell`: public API and visual resources exist, but major interaction or layout behavior is not yet implemented.
- `Gallery fallback only`: represented in the gallery by an explanatory WPF fallback, not a ModernWpf parity control.
- `Ported`: legacy wording for rows that have not yet been reclassified; do not use for newly audited complex controls.
- `Mapped`: legacy wording for rows that have not yet been reclassified; prefer `WPF-equivalent`.
- `Source-backed WPF port`: current WinUI 3 source has replaced the old WinUI 2-era guessed implementation; see `docs/winui3-source-parity.md` for the active source evidence.
- `Optional`: supported through documentation/sample integration, not a core dependency.
- `Excluded`: not feasible or not appropriate for WPF; reason required.
- `Pending`: not completed yet.

## Stage 1 Status

Stage 1 retires the previous ad hoc tests from the solution and introduces the upstream-derived WPF test harness:

- `test\ModernWpf.WinUI.TestInfra`: dispatcher, window-host, input, and visual-tree helpers for ported WinUI tests.
- `test\ModernWpf.WinUI.TestApp`: WPF TestUI host assembly that will receive ported upstream TestUI pages.
- `test\ModernWpf.WinUI.Tests`: MSTest project for ported upstream API and interaction tests.

The old test projects remain on disk for reference while porting, but are no longer authoritative solution gates.

## Control And Feature Matrix

| Upstream WinUI 2.8.7 area | ModernWpf status | Test status | Notes |
| --- | --- | --- | --- |
| AnnotatedScrollBar | Source-backed WPF port | API/template + scroll-controller request tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\annotatedscrollbar-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source-shaped vertical parts, label grid layout, hover tooltip/thumb ghost, `IScrollController` request events, `SetValues` validation, cancelable scrolling, source small-change direction, and no guessed nearest-label fallback. WPF substitutions remain for platform `ScrollPresenter`, composition panning, and raw touch/TestUI automation. |
| AnimatedIcon | Excluded | Excluded | Depends on WinUI animated icon source infrastructure and compositor animation semantics not present in WPF. |
| AnimatedVisualPlayer | Excluded | Excluded | Depends on WinUI visual/lottie animation pipeline; do not add as ModernWpf core surface. |
| AutoSuggestBox | Source-backed WPF port | Ported API/resource + interaction slices | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\autosuggestbox-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source XamlOM properties, source delayed text-change counter and `CheckCurrent()` semantics, source user-typed text restoration, query-button/programmatic submit behavior, deferred item-click submit ordering, source automation peer shape, and CommonStyles resource/template coverage. WPF substitutions remain for SIP/candidate-window positioning, layout-transition overlay, popup-root placement, `ShouldConstrainToRootBounds`, reversed-above item source, input validation forwarding, header-placement visuals, WinUI visual-tree snapshots, and Axe scans. |
| BreadcrumbBar | Ported | Ported WPF-feasible APITest + interaction slices | Added WPF `BreadcrumbBar`, `BreadcrumbBarItem`, item-click args, generated item containers, item templates, collection-change refresh, keyboard focus movement, invoke automation, and UIA position/size metadata. Tests cover upstream defaults, empty control behavior, item generation/current item behavior, item templates, click args, invoke automation, and collection changes. WinUI ItemsRepeater overflow compression, ellipsis flyout recycling, access-key choreography, Axe scans, and full TestUI automation are excluded because WPF lacks the matching WinUI TestUI and ItemsRepeater overflow infrastructure. |
| ButtonInteraction / SliderInteraction | Mapped to WPF platform controls | Excluded | Upstream tests exercise WinUI interaction helper infrastructure for platform Button/Slider behavior. ModernWpf relies on WPF Button/Slider semantics rather than owning a separate control surface. |
| CalendarView common styles | Stock WPF Calendar/DatePicker now use official WPF Fluent | Excluded upstream snapshot | ModernWpf keeps CalendarView/CalendarDatePicker resource aliases for compatibility, but the active stock WPF Calendar and DatePicker templates are now sourced from `PresentationFramework.Fluent`. The final WinUI APITest is disabled upstream and only verifies a visual-tree baseline snapshot. |
| ColorPicker / ColorSpectrum | Source-backed WPF port | API/resource + source-shaped spectrum/text/slider/automation tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\colorpicker-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source-shaped `RootGrid`, `ColorSpectrumGrid`, `TextEntryGrid`, `ColorSpectrum` bitmap/color-map rendering, `HsvColor` state ownership, source third-dimension slider channel mapping, RGB/HSV/alpha/hex text validation, More button text-entry state, and value-pattern automation. WPF substitutions remain for WinUI `SpectrumBrush`, async software bitmap creation, localized resource strings, WinRT UIA notification details, and raw TestUI pointer automation. |
| ComboBox | Official WPF Fluent-backed stock control | Official WPF Fluent template/resource + adapter style tests | Current work uses official WPF Fluent rather than this legacy WinUI 2 matrix because `ComboBox` is a stock WPF control. `docs\combobox-wpf-fluent-source-audit.md` maps `PresentationFramework.Fluent\Styles\ComboBox.xaml` to ModernWpf: official WPF trigger templates, plain WPF presenter slots, editable TextBox style, popup/chevron layout, and DataGrid adapter style keys are covered. WPF substitutions remain for `System.Runtime` to `mscorlib`, `Border.CornerRadius` to `ControlHelper.CornerRadius`, and existing theme aliases. |
| DataGrid | Official WPF Fluent-backed stock control | Official WPF Fluent template/resource tests | Current work uses official WPF Fluent rather than this legacy WinUI 2 matrix because `DataGrid` is a stock WPF control. `docs\datagrid-wpf-fluent-source-audit.md` maps `PresentationFramework.Fluent\Styles\DataGrid.xaml` to ModernWpf: official WPF cell, row, row-header, column-header, column-floating-header, drop-separator, default DataGrid, and DataGrid CheckBox element styles are covered. The previous `DataGridHelper`, `DataGridRowHelper`, `DataGridCellPresenter`, `ContentPresenterEx`, `FontIconFallback`, expanded-cell, and row-group header guesses were deleted for the stock DataGrid path. WPF substitutions remain for `System.Runtime` to `mscorlib`, `Border.CornerRadius` to `ControlHelper.CornerRadius`, and localizing `FallbackBrushConverter` into ModernWpf. |
| CommandBar / AppBarButton / AppBarToggleButton / AppBarSeparator | Source-backed WPF port | Ported WPF-feasible API slice + source parity tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\commandbar-winui3-source-audit.md` and `docs\appbarbutton-winui3-source-audit.md` map the active WPF port against local WinUI 3 source: the old `CommandBarToolBar` / `CommandBarPanel` WPF `ToolBar` host is deleted, normal CommandBar owns source-shaped template settings and command panels, AppBar controls use explicit `UseOverflowStyle`, and tests cover command collections, visual states, overflow-button visibility, command execution, automation, and AppBar overflow styling. WPF substitutions remain for the missing WinUI `AppBar` base, generated command collection APIs, `DynamicOverflowOrder`, `DynamicOverflowItemsChanging`, popup `ActualPlacement`, ThemeShadow, and full TestUI input automation. |
| CommandBarFlyout / TextCommandBarFlyout | Ported existing WPF surface/resources | Ported APITest slices + WPF resource/style guard | Existing WPF port; default command collection, command propagation, command-bar/overflow sizing, and final WinUI 2.8.7 CommandBarFlyout resource/style mappings are covered. Primary-command overflow and raw popup interaction automation are excluded because WPF popup hosting and WinUI TestUI process input do not map directly. |
| CommonStyles and compact density resources | Ported WPF-feasible resource/API slice | Ported WPF-feasible API + resource override + compact slices | `VerifyAllThemesContainSameResourceKeys`, `VerifyUseCompactResourcesAPI`, `VerifyFinalWinUI2CompactResourceValues`, `CornerRadiusFilterConverterTest`, and `ThemeResourcesTests.VerifyOverrides` are ported. Final WinUI 2.8.7 compact density keys are covered, including `TreeViewItemMultiSelectCheckBoxMinHeight`, `TreeViewItemPresenterMargin`, and `TreeViewItemPresenterPadding`. WinUI visual-tree dump tests, interaction density automation, `ControlsResourcesVersion` V1/V2 comparisons, and previous-stable baseline alias preservation remain excluded because ModernWpf has no WinUI visual-tree verifier, no `ControlsResourcesVersion` split, and does not carry every legacy WinUI/UWP-only resource alias. |
| ContentDialog | Source-backed WPF port | Ported WPF-feasible native integration slice | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\contentdialog-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source XamlOM surface, source visual-state groups through `VisualStateEx.Setters`, source show/hide result and deferral flow, source button visibility/default-button behavior including command-area focus suppression, source same-host in-place guard, source Enter handling that invokes only an explicit enabled `DefaultButton`, and source Escape/back `ExecuteCloseAction` behavior that honors `CloseButtonClick` cancellation before falling back to `Hide(None)`. WPF substitutions remain for XamlRoot popup transplantation, `ContentDialogMetadata`, unconstrained/windowed popup placement, SIP/input-pane positioning, compositor shadows, native automation/gamepad/access-key paths, and `XamlUICommand` property binding. |
| DropDownButton | Ported existing WPF surface/resources | Ported API/resource + interaction slices | Existing WPF port is covered for property setters, default template alignment/focus margin, final WinUI 2 chevron foreground resource keys, chevron foreground binding, automation expand/collapse peer behavior, flyout open/close events, and replacement-flyout event rewiring. Broader popup/input timing and animated chevron visual-state automation are WPF-excluded. |
| Expander | Official WPF Fluent-backed stock control | Official WPF Fluent template/resource + automation slices | Current work uses official WPF Fluent rather than this legacy WinUI 2 matrix because `Expander` is a stock WPF control. `docs\expander-wpf-fluent-source-audit.md` maps `PresentationFramework.Fluent\Styles\Expander.xaml` to ModernWpf: official WPF trigger templates, plain WPF `ContentPresenter` slots, local chevron resources, collapse animation, focus visuals, and required theme aliases are covered. WPF substitutions remain for `System.Runtime` to `mscorlib`, `Border.CornerRadius` to `ControlHelper.CornerRadius`, and localizing `AnimationFactorToValueConverter` into ModernWpf. |
| IconSource / ImageIcon | Source-backed WPF port | Ported WPF-feasible APITests + source propagation tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\iconsource-imageicon-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source `CreateIconElement`, foreground propagation, weak live-element updates, concrete source-to-element property mapping, ImageIcon source application, and source `FontIconSource.IsTextScaleFactorEnabled` / `MirroredWhenRightToLeft` propagation through `FontIcon` and `SharedHelpers.MakeIconElementFrom`. `AnimatedIconSource` remains excluded because ModernWpf does not carry WinUI's compositor animated-icon pipeline as a core control. |
| InfoBadge | Ported | Ported WPF-feasible APITests | Added WPF control, template settings, default status styles, display-kind switching, value validation, and Symbol/Path/Font/Bitmap/Image IconSource support. AnimatedIconSource remains excluded with the broader animated-icon/compositor surface. |
| InfoBar | Ported | Ported WPF-feasible interaction/API slices | Added WPF control, template settings, close/cancel events, severity display states, IconSource switching, close-button command/style plumbing, StatusBar automation peer mapping, and InfoBarPanel layout tests. Axe scans, notification events, localized WinUI strings, and full TestUI visual coverage are excluded for WPF feasibility. |
| ItemsView / ItemContainer | Excluded from WinUI 2.8.7 scope | Excluded | These newer WinUI items primitives are not present as controls in the final WinUI 2.8.7 source tree under `D:\repos\microsoft-ui-xaml-v2.8.7\dev`; only Repeater TestUI sample page names contain `ItemsView`. There is no final WinUI 2.8.7 `ItemsView` / `ItemContainer` APITest, InteractionTest, or control implementation surface to port. Keep coverage under WPF `ListView`/`GridView`, `ItemsRepeater`, and `SelectorBar`; revisit only for a future WinUI 3 / Windows App SDK parity target. |
| LayoutPanel | Source-backed WPF port | Ported APITests + source invalidation lifecycle tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\layoutpanel-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: public surface, padding/border size adjustment, child fill arrange, custom `Layout` delegation, layout context children/state exposure, and source layout replacement semantics that unhook old invalidation handlers and hook the new layout. WPF substitutions remain for dependency-property metadata, panel protected chrome, rounded layout clip/hit-test geometry, and `Children().GetView()` through a WPF collection wrapper. |
| MapControl | Excluded | Excluded | Upstream API test class is empty; the control itself is a provider-dependent map surface outside ModernWpf core. |
| Materials / Acrylic / Reveal / Lights / Effects | Excluded | Excluded | UWP/WinUI compositor material system; keep ModernWpf's existing WPF material behavior separate. |
| MenuBar | Source-backed WPF port | Source-shaped API/template + automation tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\menubar-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: the old WPF `Menu` / `MenuItem` wrapper, `MenuBarStyleHelper`, and test-only shim are deleted; `MenuBar` now owns a source-shaped item collection and `ItemsControl` template, `MenuBarItem` now owns a `MenuBarItemFlyout`, mirrors flyout items, drives source-style visual states through `VisualStateEx.Setters`, and exposes MenuBar/MenuBarItem automation peers. Tests cover template parts, item collection, flyout mirroring, open/close state, automation peers, source min height, and XAML content property. WPF substitutions remain for popup pass-through, XYFocus/gamepad/access-key display mode, and strict WinUI menu flyout item typing. |
| NavigationView | Source-backed WPF port | Source-shaped API/template + automation tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\navigationview-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: ModernWpf keeps the source-shaped NavigationView, item, presenter, template-settings, top-navigation data-provider, item factory, visual-state, and selection-provider automation architecture while documenting WPF substitutions for popup/XamlRoot, top-overflow measurement, focus/gamepad/access-key paths, composition animations, and WinUI TestUI automation. Tests cover defaults, pane/display mode, selection, item automation, top/footer hosts, presenter visual states, theme resources, and the public source-shaped `NavigationViewAutomationPeer` selection provider. |
| NumberBox | Ported existing WPF surface/resources | Ported APITests + interaction/resource slices | WinUI 2.8.7 API tests for text alignment, input scope, enabled visual state, and UIA name forwarding are ported. Interaction coverage now includes spin buttons, spin-button enabled state, value/text sync, value-changed events, min/max coercion, validation-disabled range behavior, keyboard commit/cancel/step behavior, mouse-wheel stepping, expression parsing, custom formatter behavior, and header presenter behavior. Final popup offsets, `NumberBoxMinWidth`, spin-button metrics, popup padding, and Light/Dark/HighContrast popup resource mappings are covered. Right-click selection, raw `InputEater` overlay semantics, `SelectionFlyout`, `TextReadingOrder`, `PreventKeyboardDisplayOnProgrammaticFocus`, and accessibility scan coverage are excluded for WPF feasibility. |
| PagerControl | Ported | Ported WPF-feasible APITest + interaction slices | Added WPF `PagerControl`, display/button-visibility enums, template settings, selected-index event args, navigation buttons, generated number-panel buttons, page collections, and selection automation peer. Tests cover upstream automation selection contract, number-panel UIA position/size metadata, finite and infinite page lists, empty-pager safety, selected-index event args, defaults, and navigation/button selection. WinUI NumberBox/ComboBox visual-state switching, compact ellipsis number-panel layout, localization resources, keyboard focus choreography, and full TestUI automation are excluded because they depend on WinUI template/input infrastructure. |
| ParallaxView | Source-backed WPF port | Ported WPF-feasible APITest + source layout/refresh tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\parallaxview-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: public surface, child measure/arrange expansion, Border-like alignment, viewport clipping, source clip geometry reuse, source refresh guards, and clamped/unclamped parallax math. WPF substitutions remain for layout-offset parallax instead of compositor expressions, missing `ScrollPresenter` / zoom / overpan support, WPF `ScrollViewer.ScrollChanged`, and WPF child-alignment hooks. |
| PersonPicture | Ported existing WPF surface/resources | Ported WPF-feasible APITests + interaction/resource slices | Existing WPF port; upstream defaults, automation name, small-size safety, initials/group visual states, rendered initials/badge behavior, image priority, image clear behavior, visual-tree smoke coverage, final WinUI 2.8.7 Light/Dark/HighContrast badge/ellipse resource mappings, and badge margin template application are covered. WinUI `Contact`, `PreferSmallImage`, XAML metadata provider, list scrolling, raw accessibility-view annotations, and pixel-baseline verification are excluded for WPF feasibility. |
| PipsPager | Ported | Ported WPF-feasible APITest + interaction slices | Added WPF `PipsPager`, template settings, selection event, root selection automation peer, previous/next buttons, pip generation/windowing, and UIA position/size properties. Tests cover upstream defaults, setters, automation selection contract, button UIA set metadata, empty pager safety, selection events, visible pip windowing, and previous/next navigation. Pointer-over button choreography, ItemsRepeater virtualization details, RTL visual automation, and full TestUI coverage are excluded because they depend on WinUI visual-state/input automation. |
| ProgressBar resources | Source-backed WPF port | Ported APITests + source behavior interaction slices | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\progressbar-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source property-change refresh paths, visibility-gated indeterminate state selection, padding/border indicator-width math, paused/error indeterminate full-width indicator sizing, source animation keyframes, source `ContainerAnimationMidPosition`, and status automation-name prefixing. WPF substitutions remain for DPI rounding, early clip geometry clamping, omitting the source `Normal` visual state because WPF treats it as a base common state, storyboard/transform equivalents, and `ProgressBarTrackHeight` overridability through `TemplateSettings.TrackHeight`. |
| ProgressRing | Ported feasible WPF surface/resources | Ported APITests + interaction automation/state parity | Existing WPF port keeps its storyboard ellipse fallback because final WinUI 2.8.7 uses `AnimatedVisualPlayer`/Lottie. Default sizing/background/resource mappings are synced and guarded, including `ProgressRingStrokeThickness`, inactive automation/raw-view behavior, `IsIndeterminate`, `Value`, `Minimum`, and `Maximum` API/automation parity. Tests cover defaults, active/determinate/inactive states, determinate range automation, value/min/max coercion, and indeterminate RangeValue suppression. Lottie/custom animated visual source behavior remains excluded for WPF. |
| PullToRefresh / RefreshContainer | Source-backed WPF port | API/resource + provider/ScrollViewer pull tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\pulltorefresh-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source-shaped `RefreshContainer`, `RefreshVisualizer`, default `ScrollViewerIRefreshInfoProviderAdapter`, `IRefreshInfoProvider` / adapter ownership, source visualizer state machine, default visualizer sizing, custom visualizer preservation, request/deferral flow, final WinUI 2.8.7 resources, template content hosting, ScrollViewer-boundary pull detection, below-threshold idle behavior, and away-from-boundary suppression. WPF substitutions remain for `InteractionTracker`, composition property sets, direct manipulation, touch inertia, compositor animations, raw TestUI touch automation, and localized resource fan-out. |
| RadioButtons | Source-backed WPF port | Ported WPF-feasible APITests + interaction tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\radiobuttons-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source automation peer, source `MaxColumns` validation, source template-owned `ColumnMajorUniformToLargestGridLayout.MaxColumns` binding via a WPF `BindingProxy`, source `DataTemplateSelector` factory handling, selected-index/selected-item synchronization, focus handoff, checked-item insertion, column-layout test-hook coverage, UIA position/size updates, keyboard traversal, disabled-item traversal, control-modifier focus movement, and scroll/focus regression coverage. WPF substitutions remain for focus/gamepad handling, access-key routing, child-handler revokers, and WPF automation metadata. |
| RadioMenuFlyoutItem | Source-backed WPF port | Ported API/resource + interaction/source lifecycle slices | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\radiomenuflyoutitem-winui3-source-audit.md` maps WinUI 3 `RadioMenuFlyoutItem` to ModernWpf's existing WPF `RadioMenuItem` substitute: weak group selection tracking, guarded uncheck behavior, source loaded/unloaded group-map lifetime, default template state setters, radio glyph/icon layout, and feasible resource mappings. WPF substitutions remain for the public `MenuItem` base, coerced WPF `IsCheckable`, process-local UI-thread group storage, submenu checked visuals through `MenuItem.IsChecked`, and raw popup automation. |
| RadialGradientBrush | Mapped to WPF platform brush | Excluded | WPF already provides `System.Windows.Media.RadialGradientBrush`; upstream coverage is TestUI interaction/rendering for the WinUI platform brush rather than a ModernWpf-owned control. |
| RatingControl | Ported WPF-feasible resources/templates | Ported APITests + interaction/resource slices | Existing WPF port; default style/resources now track final WinUI 2.8.7 where WPF can represent them, including `MUX_RatingControlDefaultFontInfo`, default font item visuals, visual-state names, caption/template parts, Light/Dark/HighContrast resource mappings, API defaults, image item assignment, collapsed value set, value coercion, resource-size overrides, keyboard input, two-way value binding, read-only input suppression, max-rating UIA text, value/range text, automation property coverage, and the WPF path-item fallback. WinUI `AutomationProperties.AccessibilityView=Raw` and pixel-level pointer/TestUI visual baselines are excluded because WPF has no equivalent raw/content/control accessibility-view API and the port does not run WinUI TestUI automation. |
| Repeater / ItemsRepeater layouts | Source-backed WPF port | Ported APITest + layout/source-cycle slices | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\repeater-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source-derived `ItemsRepeater`, viewport manager, layouts, items-source/template/recycle helpers, selection model, scroll-host anchoring, source viewport `GetLayoutExtent`, and WinUI's 60-pass `StackLayout` measure-cycle shortcut. WPF substitutions remain for effective viewport, ScrollPresenter, phasing, focus/gamepad, WinRT data-source metadata, WPF invalid-rect representation, and full visual/TestUI automation. |
| ScrollPresenter / ScrollView | Excluded | Excluded | Large WinUI scrolling primitive; WPF ScrollViewer remains the platform primitive. Port only resource/style implications. |
| SelectorBar | Ported | Ported WPF-feasible APITest + interaction slices | Added WPF `SelectorBar`, `SelectorBarItem`, selection event args, item collection/selection synchronization, text/icon/child display, click and keyboard selection, horizontal scrolling template, and selection automation peers. Tests cover upstream defaults, item collection behavior, selected-item validation/removal, click selection, and UIA selection contracts. WinUI `ItemsView` internals, `ItemContainer` inheritance details, XYFocus behavior, private test hooks, and full visual/TestUI automation are excluded because the final WinUI behavior depends on ItemsView/TestUI infrastructure not present in WPF. |
| SplitButton | Ported existing WPF surface/resources | Ported APITests + interaction/resource slices | Existing WPF port; upstream default/setter coverage and ToggleSplitButton IsChecked API are now covered. Interaction coverage now includes primary/secondary button behavior, command enablement, automation invoke/expand/collapse, keyboard Space/F4 routing, ToggleSplitButton automation, default WPF style application, and final WinUI 2.8.7 Light/Dark/HighContrast resource mappings. Broader visual-state matrix and touch-specific behavior are excluded because they require WinUI visual-state and touch automation infrastructure. |
| SplitView | Source-backed WPF port | Ported WPF-feasible TestUI/API slice + source parity tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\splitview-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source visual-state table selection, `OpenPaneLength=NaN` measured pane sizing, source-shaped template-setting math, cancelable light-dismiss close flow, template-owned `LightDismissLayer`, Escape close, focus save/restore, and right-side inline transition coverage. WPF substitutions remain for WinUI XamlRoot popup dismiss layers, back-button/gamepad focus, Xbox-only `LightDismissOverlayMode.Auto`, and WinRT automation peer factory indexes. |
| SwipeControl | Source-backed WPF port | API/template + reveal/execute interaction tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\swipecontrol-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source-shaped `RootGrid`, `SwipeContentRoot`, dynamic `SwipeContentStackPanel`, `ContentRoot`, `InputEater`, generated `AppBarButton` swipe items, source drag sign semantics, threshold colors, execute `RemainOpen`, source reveal background selection, and source finite-measure fill behavior. WPF substitutions remain for `InteractionTracker`, `VisualInteractionSource`, composition animations, touch inertia, and raw WinUI TestUI automation. |
| SystemBackdropElement | Excluded | Excluded | Depends on WinUI `SystemBackdrop`, Mica, DesktopAcrylic, and compositor-backed lifecycle semantics. Keep ModernWpf material behavior separate. |
| TabView / stock WPF TabControl | Official WPF Fluent-backed stock control | Official WPF Fluent template/resource tests | Current work uses official WPF Fluent rather than this legacy WinUI 2 matrix because `TabControl` and `TabItem` are stock WPF controls. `docs\tabcontrol-wpf-fluent-source-audit.md` maps `PresentationFramework.Fluent\Styles\TabControl.xaml` to ModernWpf: official top/bottom/left/right TabControl templates, WPF TabItem triggers, plain WPF presenter slots, official TabView-named brush aliases, and deleted `TabItemHelper` / `TabControlHelper` guesses are covered. WinUI `TabView`/`TabViewItem` custom APIs, add-tab button command surface, tab close/reorder/drag/drop contracts, overflow scroll-button automation, localization strings, Axe scans, ItemsSource/TestUI pages, and visual-state/pixel automation are excluded because this phase does not add a `TabView` control. |
| TeachingTip | Source-backed WPF port | API/resource + popup/automation interaction tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\teachingtip-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source-shaped `Opened` event timing, `TeachingTipOpenedEventArgs`, `TeachingTipAutomationPeer`, WPF `IWindowProvider` state, close reason/tail/placement/hero-placement enums, closing/closed event args and deferral, template settings, icon-source projection, title/subtitle/hero/content/action/close button template wiring, close cancellation/deferral, final WinUI 2.8.7 resources, WPF `Popup` hosting, targeted placement, bounds-aware fallback ordering, outside-click light dismiss, target/window reposition hooks, and target-unload close exist. WPF substitutions remain for exact XamlRoot placement solver behavior, WinRT notification/window automation events, localization automation, Axe scans, F6/gamepad focus, and full TestUI interaction coverage. |
| TitleBar | Mapped to WPF title bar/window chrome helpers | Ported WPF-feasible API/resource slice | Existing WPF `TitleBar`, `TitleBarControl`, `TitleBarButton`, and `CoreApplicationViewTitleBar` bridge cover the usable WPF window-chrome surface. Added final WinUI 2.8.7 TitleBar resource keys for Light/Dark/HighContrast and tests for attached-property defaults/setters, back-request routing, template parts, title bar metric/visibility event bridging, and resource mappings. The upstream final WinUI 2.8.7 TitleBar is still experimental and only has a disabled interaction smoke test; WinUI `IconSource`, `CustomContent`, `TemplateSettings`, AppWindow caption-button color plumbing, `AnimatedIcon`, drag-region, and full TestUI automation are excluded for WPF feasibility. |
| ToggleSwitch | Source-backed WPF port | API/template + WinUI drag semantic tests | Existing WPF `ToggleSwitch` carries WinUI template parts, content/header presenters, automation peer, VisualState setter coverage, and default content/resource tests. The drag path is now aligned with WinUI source semantics: deltas accumulate, short drags do not toggle until crossing half the knob range, canceled drags restore VSM-owned state, and tap/click still toggles. WinUI curtain animation/template-settings internals, gamepad key handling, pointer capture edge cases, header-placement states, and element sound playback remain excluded or WPF-adapted. |
| TreeView | Mapped to WPF TreeView with final resource/style slice | Ported WPF-feasible resource/style + behavior slices | WPF stock `TreeView`/`TreeViewItem` remains the control mapping. Final WinUI 2.8.7 TreeView theme resource mappings, default density keys, WPF default item style resource references, chevron visibility, expansion/collapse, and indentation behavior are covered. WinUI `TreeViewNode`, `TreeViewList`, multi-select node vectors, drag/drop reordering, x:Bind content mode, unrealized children, localization resource fan-out, visual-tree dumps, UIA event automation, and TestUI input/gamepad coverage are excluded because WPF stock TreeView exposes a different object model and this repo has no WinUI TestUI automation host. |
| TwoPaneView | Source-backed WPF port | API/layout + display-region simulation tests | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\twopaneview-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: source-shaped internal `ViewMode`, `UpdateMode` / `UpdateRowsColumns` ordering, public `ModeChanged` semantics, source `ScrollViewer` / `Border` pane host template shape, and source display-region sizing math through an internal WPF simulation helper. WPF substitutions remain for platform spanning APIs, `XamlRoot`, `ScrollContentPresenter.SizesContentToTemplatedParent`, and native `VisualState.Setters`. |
| WebView2 | Optional WPF gallery fallback; core excluded | Ported optional dependency/catalog guard | ModernWpf does not add a core `Microsoft.Web.WebView2.Wpf` dependency or a `ModernWpf.Controls.WebView2` wrapper. The gallery keeps a WebView2 catalog entry but maps it to a local-content WPF `WebBrowser` fallback, covered by `WebView2OptionalTests`. Upstream final WinUI 2.8.7 Edge/WebView2 runtime, registry, native CoreWebView2, network/install, and process-failure coverage remains excluded because those tests target the WinUI WebView2 platform control and external runtime installation behavior rather than a ModernWpf-owned WPF control. |
| WrapPanel | Source-backed WPF port | Ported APITests + source row-cache test | Current work targets WinUI 3 source rather than this legacy WinUI 2 matrix. `docs\wrappanel-winui3-source-audit.md` maps the active WPF port against local WinUI 3 source: public `Padding`, `ItemSpacing`, `LineSpacing`, `Orientation`, `ItemsStretch`, source UV row-building, collapsed-child spacing, last-item stretch, and the source arrange row-cache rule. WPF substitutions remain for dependency-property metadata, `Orientation` type mapping, WPF desired-size clamping, and collapsed arrange cleanup. |

## Test Port Matrix

| Upstream test category | ModernWpf target | Status | Notes |
| --- | --- | --- | --- |
| `dev/*/APITests` | `ModernWpf.WinUI.Tests` | Ported WPF-feasible slices | Implemented or mapped control rows above identify the upstream-derived APITest coverage and any API-only exclusions. WinUI-only XAML metadata provider coverage, private test hooks, and controls not present in final WinUI 2.8.7 are excluded in the relevant rows. |
| `dev/*/InteractionTests` | `ModernWpf.WinUI.Tests` + `ModernWpf.WinUI.TestApp` | Ported WPF-feasible slices | Interaction coverage is represented by WPF dispatcher/window/input tests where the behavior maps to WPF controls. Raw WinUI TestUI input injection, gamepad/XYFocus choreography, compositor animation timing, Axe scans, and process-bound automation are excluded in the relevant control rows. |
| `dev/*/TestUI` pages | `ModernWpf.WinUI.TestApp` | Documented WPF mappings/exclusions | TestUI scenarios that expose reusable control behavior are represented by WPF sample/test hosts and per-control tests. Full WinUI TestUI pages, app chrome, private test hooks, localization harness plumbing, visual-tree dump baselines, and pixel automation remain excluded because this repo does not host a WinUI TestUI runner. |
| `test/MUXControlsTestApp/ThemeResourcesTests.cs` | `ModernWpf.WinUI.Tests` | Ported WPF-feasible slice | `VerifyOverrides` is ported for `RatingControlCaptionForeground` application-resource overrides; Light/Dark/HighContrast key parity is covered by `CommonStylesResourceTests`. |
| Localization/resource tests | `ModernWpf.WinUI.Tests` | Ported WPF-feasible resource slices | ModernWpf-owned resource keys are covered by per-control resource tests and Light/Dark/HighContrast key parity. WinUI `.resw` localization, localized color names/value strings, and platform string-loader tests are excluded where WPF has no matching ModernWpf-owned resource surface. |
| Leak and compositor tests | WPF equivalent or exclusion | Excluded / documented per row | Composition, interaction-tracker, Lottie/animated visual, DComp shadow, WinRT leak, and platform object-lifetime tests are excluded where there is no WPF equivalent; rows above call out the concrete excluded surfaces for each affected control. |

## Retired Local Tests

These projects were useful during earlier migration work but are not sufficient as final WinUI parity proof:

- `test\ModernWpf.Test`
- `test\ModernWpfTestApp`
- `test\NavigationView_TestUI`
- `test\TestAppUtils`
- `test\ModernWpf.Gallery.Tests`
- `test\ItemsRepeaterTestApp`

They may be consulted while porting but should not be treated as completion evidence.
