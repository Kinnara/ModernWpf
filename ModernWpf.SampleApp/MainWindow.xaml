<Window
    x:Name="Window"
    x:Class="ModernWpf.SampleApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    mc:Ignorable="d"
    Icon="Assets/AppIcon.ico"
    Title="ModernWPF Sample App"
    Width="1000"
    Height="750"
    WindowStartupLocation="CenterScreen"
    Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
    Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"
    ui:ThemeManager.IsThemeAware="True"
    ui:WindowHelper.UseModernWindowStyle="True"
    ui:TitleBar.IsBackButtonVisible="False"
    ui:TitleBar.IsBackEnabled="{Binding ElementName=RootFrame, Path=CanGoBack}"
    ui:TitleBar.BackRequested="OnBackRequested">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid
            x:Name="CustomTitleBar"
            Height="{DynamicResource TitleBarHeight}"
            Visibility="{Binding Path=(ui:TitleBar.ExtendViewIntoTitleBar), ElementName=Window, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock
                Grid.Column="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="12"
                Text="Custom Title Bar" />
        </Grid>

        <Frame
            x:Name="RootFrame"
            Grid.Row="1"
            NavigationUIVisibility="Hidden"
            IsTabStop="False"
            Source="/MainPage.xaml" />
    </Grid>
</Window>
