<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="clr-namespace:ModernWpf.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Common.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Thickness x:Key="SliderTopHeaderMargin">0,0,0,4</Thickness>
    <CornerRadius x:Key="SliderTrackCornerRadius">2</CornerRadius>
    <CornerRadius x:Key="SliderThumbCornerRadius">16</CornerRadius>
    <sys:Double x:Key="SliderPreContentMargin">14</sys:Double>
    <sys:Double x:Key="SliderPostContentMargin">14</sys:Double>
    <sys:Double x:Key="SliderHorizontalHeight">32</sys:Double>
    <sys:Double x:Key="SliderVerticalWidth">32</sys:Double>
    <sys:Double x:Key="SliderHorizontalThumbWidth">20</sys:Double>
    <sys:Double x:Key="SliderHorizontalThumbHeight">20</sys:Double>
    <sys:Double x:Key="SliderVerticalThumbWidth">20</sys:Double>
    <sys:Double x:Key="SliderVerticalThumbHeight">20</sys:Double>
    <sys:Double x:Key="SliderInnerThumbWidth">12</sys:Double>
    <sys:Double x:Key="SliderInnerThumbHeight">12</sys:Double>

    <Style x:Key="SliderButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Height" Value="20" />
        <Setter Property="Width" Value="20" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="BorderBrush" Value="{DynamicResource SliderThumbBorderBrush}" />
        <Setter Property="Background" Value="{DynamicResource SliderThumbBackground}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border
                        Margin="-2"
                        Background="{DynamicResource SliderOuterThumbBackground}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource SliderThumbCornerRadius}">
                        <Ellipse
                            x:Name="SliderInnerThumb"
                            Width="{DynamicResource SliderInnerThumbWidth}"
                            Height="{DynamicResource SliderInnerThumbHeight}"
                            Fill="{TemplateBinding Background}"
                            RenderTransformOrigin="0.5,0.5">
                            <Ellipse.RenderTransform>
                                <ScaleTransform />
                            </Ellipse.RenderTransform>
                        </Ellipse>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleX)" To="0.86" Duration="0:0:0.1" />
                                        <DoubleAnimation Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleY)" To="0.86" Duration="0:0:0.1" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleX)" To="1.167" Duration="0:0:0.1" />
                                        <DoubleAnimation Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleY)" To="1.167" Duration="0:0:0.1" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleX)" To="0.71" Duration="0:0:0.1" />
                                        <DoubleAnimation Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleY)" To="0.71" Duration="0:0:0.1" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="SliderHorizontal" TargetType="{x:Type Slider}">
        <Grid x:Name="HorizontalTemplate" Margin="{TemplateBinding Padding}" MinHeight="{DynamicResource SliderHorizontalHeight}" SnapsToDevicePixels="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="14" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="14" />
            </Grid.RowDefinitions>
            <TickBar x:Name="TopTick" Grid.Row="0" Height="6" Fill="{DynamicResource SliderTickBarFill}" Placement="Top" SnapsToDevicePixels="True" Visibility="Collapsed" />
            <Border x:Name="TrackBackground" Grid.Row="1" Height="4" Margin="0" Background="{DynamicResource SliderTrackFill}" BorderThickness="0" CornerRadius="{DynamicResource SliderTrackCornerRadius}">
                <ui:SliderRangeCanvas Margin="-10,0">
                    <Border x:Name="PART_SelectionRange" Height="4" Visibility="Hidden" CornerRadius="{DynamicResource SliderTrackCornerRadius}" Background="{DynamicResource SliderThumbBackground}" BorderThickness="0" />
                    <Border x:Name="PART_SelectedRange" Height="4" Visibility="Hidden" CornerRadius="{DynamicResource SliderTrackCornerRadius}" Margin="0,0,0,0" Background="{DynamicResource SliderThumbBackground}" BorderThickness="0" />
                </ui:SliderRangeCanvas>
            </Border>
            <TickBar x:Name="BottomTick" Grid.Row="2" Height="6" Fill="{DynamicResource SliderTickBarFill}" Placement="Bottom" SnapsToDevicePixels="True" Visibility="Collapsed" />
            <Track x:Name="PART_Track" Grid.Row="0" Grid.RowSpan="3">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb x:Name="Thumb" Style="{StaticResource SliderThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TrackBackground" Property="Background" Value="{DynamicResource SliderTrackFillPointerOver}" />
                <Setter TargetName="Thumb" Property="Foreground" Value="{DynamicResource SliderThumbBackgroundPointerOver}" />
            </Trigger>
            <Trigger Property="IsSelectionRangeEnabled" Value="True">
                <Setter TargetName="PART_SelectionRange" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsSelectionRangeEnabled" Value="False">
                <Setter TargetName="PART_SelectedRange" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderVertical" TargetType="{x:Type Slider}">
        <Grid x:Name="VerticalTemplate" Margin="{TemplateBinding Padding}" MinWidth="{DynamicResource SliderVerticalWidth}" SnapsToDevicePixels="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="14" />
            </Grid.ColumnDefinitions>
            <TickBar x:Name="TopTick" Width="6" Fill="{DynamicResource SliderTickBarFill}" Placement="Left" SnapsToDevicePixels="True" Visibility="Collapsed" />
            <Border x:Name="TrackBackground" Grid.Column="1" Width="4" Margin="0" Background="{DynamicResource SliderTrackFill}" BorderThickness="0" CornerRadius="{DynamicResource SliderTrackCornerRadius}">
                <ui:SliderRangeCanvas Margin="0,-10">
                    <Border x:Name="PART_SelectionRange" Width="4" Visibility="Hidden" CornerRadius="{DynamicResource SliderTrackCornerRadius}" Background="{DynamicResource SliderThumbBackground}" BorderThickness="0" />
                    <Border x:Name="PART_SelectedRange" Width="4" Visibility="Hidden" CornerRadius="{DynamicResource SliderTrackCornerRadius}" Margin="0,0,0,0" Background="{DynamicResource SliderThumbBackground}" BorderThickness="0" />
                </ui:SliderRangeCanvas>
            </Border>
            <TickBar x:Name="BottomTick" Grid.Column="2" Width="6" Fill="{DynamicResource SliderTickBarFill}" Placement="Right" SnapsToDevicePixels="True" Visibility="Collapsed" />
            <Track x:Name="PART_Track" Grid.Column="0" Grid.ColumnSpan="3">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb x:Name="Thumb" Style="{StaticResource SliderThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
                <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
                <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TrackBackground" Property="Background" Value="{DynamicResource SliderTrackFillPointerOver}" />
                <Setter TargetName="Thumb" Property="Foreground" Value="{DynamicResource SliderThumbBackgroundPointerOver}" />
            </Trigger>
            <Trigger Property="IsSelectionRangeEnabled" Value="True">
                <Setter TargetName="PART_SelectionRange" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsSelectionRangeEnabled" Value="False">
                <Setter TargetName="PART_SelectedRange" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="DefaultSliderStyle" TargetType="{x:Type Slider}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="MinWidth" Value="104" />
                <Setter Property="Template" Value="{StaticResource SliderHorizontal}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="MinHeight" Value="104" />
                <Setter Property="Template" Value="{StaticResource SliderVertical}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type Slider}" BasedOn="{StaticResource DefaultSliderStyle}" />

</ResourceDictionary>
