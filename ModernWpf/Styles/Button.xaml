<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:root="clr-namespace:ModernWpf"
    xmlns:local="clr-namespace:ModernWpf.Controls"
    xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">

    <Thickness x:Key="ButtonPadding">11,5,11,6</Thickness>

    <Style x:Key="DefaultButtonStyle" TargetType="Button">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}" />
        <Setter Property="primitives:ControlHelper.BackgroundSizing" Value="InnerBorderEdge" />
        <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ButtonBorderThemeThickness}" />
        <Setter Property="Padding" Value="{StaticResource ButtonPadding}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="primitives:FocusVisualHelper.UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="primitives:FocusVisualHelper.FocusVisualMargin" Value="-3" />
        <Setter Property="primitives:ControlHelper.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
        <Setter Property="primitives:ButtonHelper.VisualStateSettersEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <local:ContentPresenterEx
                        x:Name="ContentPresenter"
                        Background="{TemplateBinding Background}"
                        BackgroundSizing="{TemplateBinding primitives:ControlHelper.BackgroundSizing}"
                        Foreground="{TemplateBinding Foreground}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CharacterSpacing="{TemplateBinding primitives:ControlHelper.CharacterSpacing}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                        ContentTransitions="{TemplateBinding primitives:ControlHelper.ContentTransitions}"
                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                        CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}"
                        Padding="{TemplateBinding Padding}"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        IsTextScaleFactorEnabled="{TemplateBinding primitives:ControlHelper.IsTextScaleFactorEnabled}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        RecognizesAccessKey="True"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                        local:AnimatedIcon.State="Normal">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <root:VisualStateEx x:Name="PointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource ButtonBackgroundPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource ButtonBorderBrushPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource ButtonForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.(local:AnimatedIcon.State)" Value="PointerOver" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Pressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource ButtonBackgroundPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource ButtonBorderBrushPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource ButtonForegroundPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.(local:AnimatedIcon.State)" Value="Pressed" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Disabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource ButtonBackgroundDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource ButtonBorderBrushDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource ButtonForegroundDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.(local:AnimatedIcon.State)" Value="Normal" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <local:ContentPresenterEx.BackgroundTransition>
                            <local:BrushTransition Duration="0:0:0.083" />
                        </local:ContentPresenterEx.BackgroundTransition>
                    </local:ContentPresenterEx>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}" />

    <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="Foreground" Value="{DynamicResource AccentButtonForeground}" />
        <Setter Property="Background" Value="{DynamicResource AccentButtonBackground}" />
        <Setter Property="primitives:ControlHelper.BackgroundSizing" Value="OuterBorderEdge" />
        <Setter Property="BorderBrush" Value="{DynamicResource AccentButtonBorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <local:ContentPresenterEx
                        x:Name="ContentPresenter"
                        Background="{TemplateBinding Background}"
                        BackgroundSizing="{TemplateBinding primitives:ControlHelper.BackgroundSizing}"
                        Foreground="{TemplateBinding Foreground}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CharacterSpacing="{TemplateBinding primitives:ControlHelper.CharacterSpacing}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                        ContentTransitions="{TemplateBinding primitives:ControlHelper.ContentTransitions}"
                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                        CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}"
                        Padding="{TemplateBinding Padding}"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        IsTextScaleFactorEnabled="{TemplateBinding primitives:ControlHelper.IsTextScaleFactorEnabled}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        RecognizesAccessKey="True"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <root:VisualStateEx x:Name="PointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource AccentButtonBackgroundPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource AccentButtonBorderBrushPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource AccentButtonForegroundPointerOver}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Pressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource AccentButtonBackgroundPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource AccentButtonBorderBrushPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource AccentButtonForegroundPressed}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Disabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource AccentButtonBackgroundDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource AccentButtonBorderBrushDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource AccentButtonForegroundDisabled}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <local:ContentPresenterEx.BackgroundTransition>
                            <local:BrushTransition Duration="0:0:0.083" />
                        </local:ContentPresenterEx.BackgroundTransition>
                    </local:ContentPresenterEx>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SubtleButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="Foreground" Value="{DynamicResource SubtleButtonForeground}" />
        <Setter Property="Background" Value="{DynamicResource SubtleButtonBackground}" />
        <Setter Property="primitives:ControlHelper.BackgroundSizing" Value="InnerBorderEdge" />
        <Setter Property="BorderBrush" Value="{DynamicResource SubtleButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ButtonBorderThemeThickness}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <local:ContentPresenterEx
                        x:Name="ContentPresenter"
                        Background="{TemplateBinding Background}"
                        BackgroundSizing="{TemplateBinding primitives:ControlHelper.BackgroundSizing}"
                        Foreground="{TemplateBinding Foreground}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CharacterSpacing="{TemplateBinding primitives:ControlHelper.CharacterSpacing}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                        ContentTransitions="{TemplateBinding primitives:ControlHelper.ContentTransitions}"
                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                        CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}"
                        Padding="{TemplateBinding Padding}"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        IsTextScaleFactorEnabled="{TemplateBinding primitives:ControlHelper.IsTextScaleFactorEnabled}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        RecognizesAccessKey="True"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                        local:AnimatedIcon.State="Normal">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <root:VisualStateEx x:Name="PointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource SubtleButtonBackgroundPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource SubtleButtonBorderBrushPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource SubtleButtonForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenter.(local:AnimatedIcon.State)" Value="PointerOver" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Pressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource SubtleButtonBackgroundPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource SubtleButtonBorderBrushPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource SubtleButtonForegroundPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenter.(local:AnimatedIcon.State)" Value="Pressed" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Disabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Background" Value="{DynamicResource SubtleButtonBackgroundDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.BorderBrush" Value="{DynamicResource SubtleButtonBorderBrushDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource SubtleButtonForegroundDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenter.(local:AnimatedIcon.State)" Value="Normal" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <local:ContentPresenterEx.BackgroundTransition>
                            <local:BrushTransition Duration="0:0:0.083" />
                        </local:ContentPresenterEx.BackgroundTransition>
                    </local:ContentPresenterEx>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
