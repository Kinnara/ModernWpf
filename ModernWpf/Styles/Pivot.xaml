<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:root="clr-namespace:ModernWpf"
    xmlns:local="clr-namespace:ModernWpf.Controls"
    xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">

    <primitives:CornerRadiusFilterConverter x:Key="TopLeftCornerRadiusDoubleValueConverter" Filter="TopLeftValue" />
    <primitives:CornerRadiusFilterConverter x:Key="BottomRightCornerRadiusDoubleValueConverter" Filter="BottomRightValue" />

    <Style x:Key="TabItemPivotStyle" TargetType="TabItem">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource PivotHeaderItemBackgroundUnselected}" />
        <Setter Property="Padding" Value="{DynamicResource PivotHeaderItemMargin}" />
        <Setter Property="Height" Value="48" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="primitives:FocusVisualHelper.UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="primitives:PivotHelper.HeaderItemVisualStateSettersEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}"
                        SnapsToDevicePixels="True">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="SelectionStates">
                                <root:VisualStateEx x:Name="Disabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="SelectedPipe.Visibility" Value="Collapsed" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource PivotHeaderItemForegroundDisabled}" />
                                        <root:VisualStateSetter Target="Border.Background" Value="{DynamicResource PivotHeaderItemBackgroundDisabled}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Unselected">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="SelectedPipe.Visibility" Value="Collapsed" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="UnselectedLocked">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="SelectedPipe.Visibility" Value="Collapsed" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Selected">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Property="(Panel.ZIndex)" Value="1" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource PivotHeaderItemForegroundSelected}" />
                                        <root:VisualStateSetter Target="Border.Background" Value="{DynamicResource PivotHeaderItemBackgroundSelected}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="UnselectedPointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="SelectedPipe.Visibility" Value="Collapsed" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource PivotHeaderItemForegroundUnselectedPointerOver}" />
                                        <root:VisualStateSetter Target="Border.Background" Value="{DynamicResource PivotHeaderItemBackgroundUnselectedPointerOver}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="UnselectedPressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="SelectedPipe.Visibility" Value="Collapsed" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource PivotHeaderItemForegroundUnselectedPressed}" />
                                        <root:VisualStateSetter Target="Border.Background" Value="{DynamicResource PivotHeaderItemBackgroundUnselectedPressed}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="SelectedPointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Property="(Panel.ZIndex)" Value="1" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource PivotHeaderItemForegroundSelectedPointerOver}" />
                                        <root:VisualStateSetter Target="Border.Background" Value="{DynamicResource PivotHeaderItemBackgroundSelectedPointerOver}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="SelectedPressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Property="(Panel.ZIndex)" Value="1" />
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource PivotHeaderItemForegroundSelectedPressed}" />
                                        <root:VisualStateSetter Target="Border.Background" Value="{DynamicResource PivotHeaderItemBackgroundSelectedPressed}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid>
                            <local:ContentPresenterEx
                                x:Name="ContentPresenter"
                                ContentSource="Header"
                                Foreground="{DynamicResource PivotHeaderItemForegroundUnselected}"
                                FontSize="{DynamicResource PivotHeaderItemFontSize}"
                                FontFamily="{DynamicResource PivotHeaderItemFontFamily}"
                                FontWeight="{DynamicResource PivotHeaderItemThemeFontWeight}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Stretch"
                                Focusable="False"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <Rectangle
                                x:Name="SelectedPipe"
                                Fill="{DynamicResource PivotHeaderItemSelectedPipeFill}"
                                Height="2"
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="Stretch"
                                Margin="0,0,0,2"
                                RadiusX="{TemplateBinding Border.CornerRadius, Converter={StaticResource TopLeftCornerRadiusDoubleValueConverter}}"
                                RadiusY="{TemplateBinding Border.CornerRadius, Converter={StaticResource BottomRightCornerRadiusDoubleValueConverter}}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TabControlPivotStyle" TargetType="TabControl">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource PivotBackground}" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TabItemPivotStyle}" />
        <Setter Property="primitives:PivotHelper.NavigationButtonsVisualStateSettersEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <ControlTemplate.Resources>
                        <StreamGeometry x:Key="ChevronLeft">M 14.091797 19.970703 L 4.111328 10 L 14.091797 0.029297 L 14.970703 0.908203 L 5.888672 10 L 14.970703 19.091797 Z</StreamGeometry>
                        <StreamGeometry x:Key="ChevronRight">M 5.029297 19.091797 L 14.111328 10 L 5.029297 0.908203 L 5.908203 0.029297 L 15.888672 10 L 5.908203 19.970703 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    
                    <Grid
                        x:Name="templateRoot"
                        Background="{TemplateBinding Background}"
                        KeyboardNavigation.TabNavigation="Local"
                        SnapsToDevicePixels="true"
                        ClipToBounds="true">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="NavigationButtonsVisibility">
                                <root:VisualStateEx x:Name="NavigationButtonsHidden">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="PreviousButton.Opacity" Value="0" />
                                        <root:VisualStateSetter Target="PreviousButton.IsEnabled" Value="False" />
                                        <root:VisualStateSetter Target="NextButton.Opacity" Value="0" />
                                        <root:VisualStateSetter Target="NextButton.IsEnabled" Value="False" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="NavigationButtonsVisible">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="PreviousButton.Opacity" Value="1" />
                                        <root:VisualStateSetter Target="PreviousButton.IsEnabled" Value="True" />
                                        <root:VisualStateSetter Target="NextButton.Opacity" Value="1" />
                                        <root:VisualStateSetter Target="NextButton.IsEnabled" Value="True" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="PreviousButtonVisible">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="PreviousButton.Opacity" Value="1" />
                                        <root:VisualStateSetter Target="PreviousButton.IsEnabled" Value="True" />
                                        <root:VisualStateSetter Target="NextButton.Opacity" Value="0" />
                                        <root:VisualStateSetter Target="NextButton.IsEnabled" Value="False" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="NextButtonVisible">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="PreviousButton.Opacity" Value="0" />
                                        <root:VisualStateSetter Target="PreviousButton.IsEnabled" Value="False" />
                                        <root:VisualStateSetter Target="NextButton.Opacity" Value="1" />
                                        <root:VisualStateSetter Target="NextButton.IsEnabled" Value="True" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Grid.Resources>
                            <Style x:Key="BaseContentControlStyle" TargetType="ContentControl">
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                <Setter Property="FontFamily" Value="{DynamicResource {x:Static SystemFonts.MessageFontFamilyKey}}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ContentControl">
                                            <local:ContentPresenterEx
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Margin="{TemplateBinding Padding}"
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                            <Style x:Key="TitleContentControlStyle" TargetType="ContentControl" BasedOn="{StaticResource BaseContentControlStyle}">
                                <Setter Property="FontFamily" Value="{DynamicResource PivotTitleFontFamily}" />
                                <Setter Property="FontWeight" Value="{DynamicResource PivotTitleThemeFontWeight}" />
                                <Setter Property="FontSize" Value="{DynamicResource PivotTitleFontSize}" />
                            </Style>
                        </Grid.Resources>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ContentControl
                            x:Name="TitleContentControl"
                            IsTabStop="False"
                            Margin="{DynamicResource PivotPortraitThemePadding}"
                            Style="{StaticResource TitleContentControlStyle}"
                            Visibility="{TemplateBinding primitives:PivotHelper.TitleVisibility}"
                            Content="{TemplateBinding primitives:PivotHelper.Title}"
                            ContentTemplate="{TemplateBinding primitives:PivotHelper.TitleTemplate}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <Grid
                            Grid.Row="1"
                            Margin="{TemplateBinding Padding}">
                            <Grid.Resources>
                                <ControlTemplate x:Key="NextTemplate" TargetType="ButtonBase">
                                    <Border
                                        x:Name="Root"
                                        Background="{DynamicResource PivotNextButtonBackground}"
                                        BorderThickness="{DynamicResource PivotNavButtonBorderThemeThickness}"
                                        BorderBrush="{DynamicResource PivotNextButtonBorderBrush}">
                                        <VisualStateManager.CustomVisualStateManager>
                                            <root:VisualStateManagerEx />
                                        </VisualStateManager.CustomVisualStateManager>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal" />
                                                <root:VisualStateEx x:Name="PointerOver">
                                                    <root:VisualStateEx.Setters>
                                                        <root:VisualStateSetter Target="Root.Background" Value="{DynamicResource PivotNextButtonBackgroundPointerOver}" />
                                                        <root:VisualStateSetter Target="Root.BorderBrush" Value="{DynamicResource PivotNextButtonBorderBrushPointerOver}" />
                                                        <root:VisualStateSetter Target="Arrow.Foreground" Value="{DynamicResource PivotNextButtonForegroundPointerOver}" />
                                                    </root:VisualStateEx.Setters>
                                                </root:VisualStateEx>
                                                <root:VisualStateEx x:Name="Pressed">
                                                    <root:VisualStateEx.Setters>
                                                        <root:VisualStateSetter Target="Root.Background" Value="{DynamicResource PivotNextButtonBackgroundPressed}" />
                                                        <root:VisualStateSetter Target="Root.BorderBrush" Value="{DynamicResource PivotNextButtonBorderBrushPressed}" />
                                                        <root:VisualStateSetter Target="Arrow.Foreground" Value="{DynamicResource PivotNextButtonForegroundPressed}" />
                                                    </root:VisualStateEx.Setters>
                                                </root:VisualStateEx>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                        <local:FontIconFallback
                                            x:Name="Arrow"
                                            FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                            FontSize="12"
                                            Foreground="{DynamicResource PivotNextButtonForeground}"
                                            Data="{StaticResource ChevronRight}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            UseLayoutRounding="False" />
                                    </Border>
                                </ControlTemplate>
                                <ControlTemplate x:Key="PreviousTemplate" TargetType="ButtonBase">
                                    <Border
                                        x:Name="Root"
                                        Background="{DynamicResource PivotPreviousButtonBackground}"
                                        BorderThickness="{DynamicResource PivotNavButtonBorderThemeThickness}"
                                        BorderBrush="{DynamicResource PivotPreviousButtonBorderBrush}">
                                        <VisualStateManager.CustomVisualStateManager>
                                            <root:VisualStateManagerEx />
                                        </VisualStateManager.CustomVisualStateManager>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal" />
                                                <root:VisualStateEx x:Name="PointerOver">
                                                    <root:VisualStateEx.Setters>
                                                        <root:VisualStateSetter Target="Root.Background" Value="{DynamicResource PivotPreviousButtonBackgroundPointerOver}" />
                                                        <root:VisualStateSetter Target="Root.BorderBrush" Value="{DynamicResource PivotPreviousButtonBorderBrushPointerOver}" />
                                                        <root:VisualStateSetter Target="Arrow.Foreground" Value="{DynamicResource PivotPreviousButtonForegroundPointerOver}" />
                                                    </root:VisualStateEx.Setters>
                                                </root:VisualStateEx>
                                                <root:VisualStateEx x:Name="Pressed">
                                                    <root:VisualStateEx.Setters>
                                                        <root:VisualStateSetter Target="Root.Background" Value="{DynamicResource PivotPreviousButtonBackgroundPressed}" />
                                                        <root:VisualStateSetter Target="Root.BorderBrush" Value="{DynamicResource PivotPreviousButtonBorderBrushPressed}" />
                                                        <root:VisualStateSetter Target="Arrow.Foreground" Value="{DynamicResource PivotPreviousButtonForegroundPressed}" />
                                                    </root:VisualStateEx.Setters>
                                                </root:VisualStateEx>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                        <local:FontIconFallback
                                            x:Name="Arrow"
                                            FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                            FontSize="12"
                                            Foreground="{DynamicResource PivotPreviousButtonForeground}"
                                            Data="{StaticResource ChevronLeft}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            UseLayoutRounding="False" />
                                    </Border>
                                </ControlTemplate>
                            </Grid.Resources>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid
                                x:Name="headerPanel"
                                Grid.Row="0"
                                Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <local:ContentPresenterEx
                                    x:Name="LeftHeaderPresenter"
                                    Content="{TemplateBinding primitives:PivotHelper.LeftHeader}"
                                    ContentTemplate="{TemplateBinding primitives:PivotHelper.LeftHeaderTemplate}"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                <primitives:PivotHeaderScrollViewer
                                    x:Name="ScrollViewer"
                                    Grid.Column="1"
                                    Style="{DynamicResource {x:Type ScrollViewer}}"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Disabled"
                                    VerticalContentAlignment="Stretch"
                                    Template="{DynamicResource ScrollViewerScrollBarlessTemplate}">
                                    <StackPanel
                                        Panel.ZIndex="1"
                                        Background="{DynamicResource PivotHeaderBackground}"
                                        IsItemsHost="true"
                                        Orientation="Horizontal"
                                        KeyboardNavigation.TabIndex="1"
                                        KeyboardNavigation.TabNavigation="Once"
                                        KeyboardNavigation.DirectionalNavigation="Contained" />
                                </primitives:PivotHeaderScrollViewer>
                                <RepeatButton
                                    x:Name="PreviousButton"
                                    Grid.Column="1"
                                    Template="{StaticResource PreviousTemplate}"
                                    Width="20"
                                    Height="36"
                                    FocusVisualStyle="{x:Null}"
                                    Margin="{DynamicResource PivotNavButtonMargin}"
                                    primitives:ButtonHelper.VisualStateSettersEnabled="True"
                                    IsTabStop="False"
                                    IsEnabled="False"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top"
                                    Opacity="0"
                                    Background="Transparent"
                                    Command="{x:Static ScrollBar.LineLeftCommand}"
                                    CommandTarget="{Binding ElementName=ScrollViewer}" />
                                <RepeatButton
                                    x:Name="NextButton"
                                    Grid.Column="1"
                                    Template="{StaticResource NextTemplate}"
                                    Width="20"
                                    Height="36"
                                    FocusVisualStyle="{x:Null}"
                                    Margin="{DynamicResource PivotNavButtonMargin}"
                                    primitives:ButtonHelper.VisualStateSettersEnabled="True"
                                    IsTabStop="False"
                                    IsEnabled="False"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Opacity="0"
                                    Background="Transparent"
                                    Command="{x:Static ScrollBar.LineRightCommand}"
                                    CommandTarget="{Binding ElementName=ScrollViewer}" />
                                <local:ContentPresenterEx
                                    x:Name="RightHeaderPresenter"
                                    Grid.Column="2"
                                    Content="{TemplateBinding primitives:PivotHelper.RightHeader}"
                                    ContentTemplate="{TemplateBinding primitives:PivotHelper.RightHeaderTemplate}"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                            <Border
                                x:Name="contentPanel"
                                Grid.Row="1"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                KeyboardNavigation.DirectionalNavigation="Contained"
                                KeyboardNavigation.TabIndex="2"
                                KeyboardNavigation.TabNavigation="Local">
                                <local:ContentPresenterEx
                                    x:Name="PART_SelectedContentHost"
                                    ContentSource="SelectedContent"
                                    Margin="{DynamicResource PivotItemMargin}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                        </Grid>
                    </Grid>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
