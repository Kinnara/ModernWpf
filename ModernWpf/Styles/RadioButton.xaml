<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:root="clr-namespace:ModernWpf"
    xmlns:local="clr-namespace:ModernWpf.Controls"
    xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Common.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <sys:Double x:Key="RadioButtonCheckGlyphSize">12</sys:Double>
    <sys:Double x:Key="RadioButtonCheckGlyphPointerOverSize">14</sys:Double>
    <sys:Double x:Key="RadioButtonCheckGlyphPressedOverSize">10</sys:Double>

    <Style x:Key="DefaultRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource RadioButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource RadioButtonForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RadioButtonBorderBrush}" />
        <Setter Property="Padding" Value="8,6,0,0" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="primitives:FocusVisualHelper.UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="primitives:FocusVisualHelper.FocusVisualMargin" Value="-7,-3,-7,-3" />
        <Setter Property="primitives:ControlHelper.CornerRadius" Value="{DynamicResource ControlCornerRadius}"/>
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
        <Setter Property="primitives:ButtonHelper.VisualStateSettersEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border
                        x:Name="RootGrid"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}"
                        SnapsToDevicePixels="True">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <root:VisualStateEx x:Name="PointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource RadioButtonForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="RootGrid.Background" Value="{DynamicResource RadioButtonBackgroundPointerOver}" />
                                        <root:VisualStateSetter Target="RootGrid.BorderBrush" Value="{DynamicResource RadioButtonBorderBrushPointerOver}" />
                                        <root:VisualStateSetter Target="OuterEllipse.Stroke" Value="{DynamicResource RadioButtonOuterEllipseStrokePointerOver}" />
                                        <root:VisualStateSetter Target="OuterEllipse.Fill" Value="{DynamicResource RadioButtonOuterEllipseFillPointerOver}" />
                                        <root:VisualStateSetter Target="CheckOuterEllipse.Stroke" Value="{DynamicResource RadioButtonOuterEllipseCheckedStrokePointerOver}" />
                                        <root:VisualStateSetter Target="CheckOuterEllipse.Fill" Value="{DynamicResource RadioButtonOuterEllipseCheckedFillPointerOver}" />
                                        <root:VisualStateSetter Target="CheckGlyph.Fill" Value="{DynamicResource RadioButtonCheckGlyphFillPointerOver}" />
                                        <root:VisualStateSetter Target="CheckGlyph.Stroke" Value="{DynamicResource RadioButtonCheckGlyphStrokePointerOver}" />
                                    </root:VisualStateEx.Setters>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckGlyph" Storyboard.TargetProperty="Width">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlNormalAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="14" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckGlyph" Storyboard.TargetProperty="Height">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlNormalAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="14" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Pressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource RadioButtonForegroundPressed}" />
                                        <root:VisualStateSetter Target="RootGrid.Background" Value="{DynamicResource RadioButtonBackgroundPressed}" />
                                        <root:VisualStateSetter Target="RootGrid.BorderBrush" Value="{DynamicResource RadioButtonBorderBrushPressed}" />
                                        <root:VisualStateSetter Target="OuterEllipse.Stroke" Value="{DynamicResource RadioButtonOuterEllipseStrokePressed}" />
                                        <root:VisualStateSetter Target="OuterEllipse.Fill" Value="{DynamicResource RadioButtonOuterEllipseFillPressed}" />
                                        <root:VisualStateSetter Target="CheckOuterEllipse.Stroke" Value="{DynamicResource RadioButtonOuterEllipseCheckedStrokePressed}" />
                                        <root:VisualStateSetter Target="CheckOuterEllipse.Fill" Value="{DynamicResource RadioButtonOuterEllipseCheckedFillPressed}" />
                                        <root:VisualStateSetter Target="CheckGlyph.Fill" Value="{DynamicResource RadioButtonCheckGlyphFillPressed}" />
                                        <root:VisualStateSetter Target="CheckGlyph.Stroke" Value="{DynamicResource RadioButtonCheckGlyphStrokePressed}" />
                                    </root:VisualStateEx.Setters>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckGlyph" Storyboard.TargetProperty="Width">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlNormalAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="10" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckGlyph" Storyboard.TargetProperty="Height">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlNormalAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="10" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PressedCheckGlyph" Storyboard.TargetProperty="Opacity">
                                            <LinearDoubleKeyFrame KeyTime="0" Value="1" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PressedCheckGlyph" Storyboard.TargetProperty="Width">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlFastAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="10" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PressedCheckGlyph" Storyboard.TargetProperty="Height">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlFastAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="10" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Disabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenter.Foreground" Value="{DynamicResource RadioButtonForegroundDisabled}" />
                                        <root:VisualStateSetter Target="RootGrid.Background" Value="{DynamicResource RadioButtonBackgroundDisabled}" />
                                        <root:VisualStateSetter Target="RootGrid.BorderBrush" Value="{DynamicResource RadioButtonBorderBrushDisabled}" />
                                        <root:VisualStateSetter Target="OuterEllipse.Stroke" Value="{DynamicResource RadioButtonOuterEllipseStrokeDisabled}" />
                                        <root:VisualStateSetter Target="OuterEllipse.Fill" Value="{DynamicResource RadioButtonOuterEllipseFillDisabled}" />
                                        <root:VisualStateSetter Target="CheckOuterEllipse.Stroke" Value="{DynamicResource RadioButtonOuterEllipseCheckedStrokeDisabled}" />
                                        <root:VisualStateSetter Target="CheckOuterEllipse.Fill" Value="{DynamicResource RadioButtonOuterEllipseCheckedFillDisabled}" />
                                        <root:VisualStateSetter Target="CheckGlyph.Fill" Value="{DynamicResource RadioButtonCheckGlyphFillDisabled}" />
                                        <root:VisualStateSetter Target="CheckGlyph.Stroke" Value="{DynamicResource RadioButtonCheckGlyphStrokeDisabled}" />
                                    </root:VisualStateEx.Setters>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckGlyph" Storyboard.TargetProperty="Width">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlFastAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="14" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CheckGlyph" Storyboard.TargetProperty="Height">
                                            <SplineDoubleKeyFrame KeyTime="{StaticResource ControlFastAnimationDuration}" KeySpline="{StaticResource ControlFastOutSlowInKeySpline}" Value="14" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CheckStates">
                                <root:VisualStateEx x:Name="Checked">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="CheckGlyph.Stroke" Value="{DynamicResource RadioButtonCheckGlyphStrokeChecked}" />
                                        <root:VisualStateSetter Target="PressedCheckGlyph.Background" Value="{DynamicResource RadioButtonCheckGlyphFillPressed}" />
                                    </root:VisualStateEx.Setters>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="CheckGlyph" Storyboard.TargetProperty="Opacity" To="1" Duration="0" />
                                        <DoubleAnimation Storyboard.TargetName="OuterEllipse" Storyboard.TargetProperty="Opacity" To="0" Duration="0" />
                                        <DoubleAnimation Storyboard.TargetName="CheckOuterEllipse" Storyboard.TargetProperty="Opacity" To="1" Duration="0" />
                                        <DoubleAnimation Storyboard.TargetName="PressedCheckGlyph" Storyboard.TargetProperty="Opacity" To="0" Duration="0" />
                                    </Storyboard>
                                </root:VisualStateEx>
                                <VisualState x:Name="Unchecked" />
                                <VisualState x:Name="Indeterminate" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid VerticalAlignment="Top" Height="32">
                                <Ellipse x:Name="OuterEllipse" Width="20" Height="20" UseLayoutRounding="False" Stroke="{DynamicResource RadioButtonOuterEllipseStroke}" Fill="{DynamicResource RadioButtonOuterEllipseFill}" StrokeThickness="{DynamicResource RadioButtonBorderThemeThickness}" />
                                <!-- A seperate element is added since the two orthogonal state groups that cannot touch the same property -->
                                <Ellipse x:Name="CheckOuterEllipse" Width="20" Height="20" UseLayoutRounding="False" Stroke="{DynamicResource RadioButtonOuterEllipseCheckedStroke}" Fill="{DynamicResource RadioButtonOuterEllipseCheckedFill}" Opacity="0" StrokeThickness="{DynamicResource RadioButtonBorderThemeThickness}" />
                                <Ellipse x:Name="CheckGlyph" Width="{DynamicResource RadioButtonCheckGlyphSize}" Height="{DynamicResource RadioButtonCheckGlyphSize}" UseLayoutRounding="False" Opacity="0" Fill="{DynamicResource RadioButtonCheckGlyphFill}" Stroke="{DynamicResource RadioButtonCheckGlyphStroke}">
                                </Ellipse>
                                <!-- A seperate element is added since the two orthogonal state groups that cannot touch the same property -->
                                <Border x:Name="PressedCheckGlyph" Width="4" Height="4" CornerRadius="6" UseLayoutRounding="False" Opacity="0" Background="{DynamicResource RadioButtonCheckGlyphFill}" BorderBrush="{DynamicResource RadioButtonCheckGlyphStroke}">
                                </Border>
                            </Grid>

                            <local:ContentPresenterEx
                                x:Name="ContentPresenter"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                Foreground="{TemplateBinding Foreground}"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Grid.Column="1"
                                TextWrapping="Wrap"
                                Focusable="False"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="RadioButton" BasedOn="{StaticResource DefaultRadioButtonStyle}" />

</ResourceDictionary>
