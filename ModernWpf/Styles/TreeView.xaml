<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:root="clr-namespace:ModernWpf"
    xmlns:local="clr-namespace:ModernWpf.Controls"
    xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">

    <sys:Double x:Key="TreeViewItemMinHeight">28</sys:Double>
    <Thickness x:Key="TreeViewItemPresenterMargin">4,2</Thickness>
    <Thickness x:Key="TreeViewItemPresenterPadding">0,3,0,5</Thickness>
    <Thickness x:Key="TreeViewItemMultiSelectSelectedItemBorderMargin">0</Thickness>
    <sys:Double x:Key="TreeViewItemMultiSelectCheckBoxMinHeight">28</sys:Double>
    <sys:Double x:Key="TreeViewItemContentHeight">20</sys:Double>

    <Style x:Key="ExpandCollapseToggleStyle" TargetType="ToggleButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                        <local:ContentPresenterEx />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultTreeViewItemStyle" TargetType="TreeViewItem">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="Background" Value="{DynamicResource TreeViewItemBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource TreeViewItemForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TreeViewItemBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource TreeViewItemBorderThemeThickness}" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
        <!--<Setter Property="IsHoldingEnabled" Value="True" />-->
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="MinWidth" Value="{DynamicResource ListViewItemMinWidth}" />
        <Setter Property="MinHeight" Value="{DynamicResource TreeViewItemMinHeight}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="primitives:FocusVisualHelper.UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="primitives:TreeViewItemHelper.IsEnabled" Value="True" />
        <Setter Property="primitives:TreeViewItemHelper.VisualStateSettersEnabled" Value="True" />
        <Setter Property="primitives:TreeViewItemHelper.GlyphBrush" Value="{DynamicResource TreeViewItemForeground}" />
        <Setter Property="primitives:TreeViewItemHelper.CollapsedPath" Value="M 5.029297 19.091797 L 14.111328 10 L 5.029297 0.908203 L 5.908203 0.029297 L 15.888672 10 L 5.908203 19.970703 Z" />
        <Setter Property="primitives:TreeViewItemHelper.ExpandedPath" Value="M 18.935547 4.560547 L 19.814453 5.439453 L 10 15.253906 L 0.185547 5.439453 L 1.064453 4.560547 L 10 13.496094 Z" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <Grid x:Name="RootGrid">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <root:VisualStateEx x:Name="PointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenterGrid.Background" Value="{DynamicResource TreeViewItemBackgroundPointerOver}" />
                                        <root:VisualStateSetter Target="PART_Header.Foreground" Value="{DynamicResource TreeViewItemForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Fill" Value="{DynamicResource TreeViewItemSelectionIndicatorForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="CollapsedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="ExpandedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenterGrid.BorderBrush" Value="{DynamicResource TreeViewItemBorderBrushPointerOver}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Opacity" Value="0" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Pressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenterGrid.Background" Value="{DynamicResource TreeViewItemBackgroundPressed}" />
                                        <root:VisualStateSetter Target="PART_Header.Foreground" Value="{DynamicResource TreeViewItemForegroundPressed}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Fill" Value="{DynamicResource TreeViewItemSelectionIndicatorForegroundPressed}" />
                                        <root:VisualStateSetter Target="CollapsedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundPressed}" />
                                        <root:VisualStateSetter Target="ExpandedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenterGrid.BorderBrush" Value="{DynamicResource TreeViewItemBorderBrushPressed}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Opacity" Value="0" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Selected">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenterGrid.Background" Value="{DynamicResource TreeViewItemBackgroundSelected}" />
                                        <root:VisualStateSetter Target="PART_Header.Foreground" Value="{DynamicResource TreeViewItemForegroundSelected}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Fill" Value="{DynamicResource TreeViewItemSelectionIndicatorForeground}" />
                                        <root:VisualStateSetter Target="CollapsedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelected}" />
                                        <root:VisualStateSetter Target="ExpandedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelected}" />
                                        <root:VisualStateSetter Target="ContentPresenterGrid.BorderBrush" Value="{DynamicResource TreeViewItemBorderBrushSelected}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Opacity" Value="1" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Disabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenterGrid.Background" Value="{DynamicResource TreeViewItemBackgroundDisabled}" />
                                        <root:VisualStateSetter Target="PART_Header.Foreground" Value="{DynamicResource TreeViewItemForegroundDisabled}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Fill" Value="{DynamicResource TreeViewItemSelectionIndicatorForegroundDisabled}" />
                                        <root:VisualStateSetter Target="CollapsedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundDisabled}" />
                                        <root:VisualStateSetter Target="ExpandedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenterGrid.BorderBrush" Value="{DynamicResource TreeViewItemBorderBrushDisabled}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="PointerOverSelected">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenterGrid.Background" Value="{DynamicResource TreeViewItemBackgroundSelectedPointerOver}" />
                                        <root:VisualStateSetter Target="PART_Header.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedPointerOver}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Fill" Value="{DynamicResource TreeViewItemSelectionIndicatorForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="CollapsedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedPointerOver}" />
                                        <root:VisualStateSetter Target="ExpandedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedPointerOver}" />
                                        <root:VisualStateSetter Target="ContentPresenterGrid.BorderBrush" Value="{DynamicResource TreeViewItemBorderBrushSelectedPointerOver}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Opacity" Value="1" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="PressedSelected">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenterGrid.Background" Value="{DynamicResource TreeViewItemBackgroundSelectedPressed}" />
                                        <root:VisualStateSetter Target="PART_Header.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedPressed}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Fill" Value="{DynamicResource TreeViewItemSelectionIndicatorForegroundPressed}" />
                                        <root:VisualStateSetter Target="CollapsedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedPressed}" />
                                        <root:VisualStateSetter Target="ExpandedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedPressed}" />
                                        <root:VisualStateSetter Target="ContentPresenterGrid.BorderBrush" Value="{DynamicResource TreeViewItemBorderBrushSelectedPressed}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Opacity" Value="1" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="SelectedDisabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="ContentPresenterGrid.Background" Value="{DynamicResource TreeViewItemBackgroundSelectedDisabled}" />
                                        <root:VisualStateSetter Target="PART_Header.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedDisabled}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Fill" Value="{DynamicResource TreeViewItemSelectionIndicatorForegroundDisabled}" />
                                        <root:VisualStateSetter Target="CollapsedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedDisabled}" />
                                        <root:VisualStateSetter Target="ExpandedGlyph.Foreground" Value="{DynamicResource TreeViewItemForegroundSelectedDisabled}" />
                                        <root:VisualStateSetter Target="ContentPresenterGrid.BorderBrush" Value="{DynamicResource TreeViewItemBorderBrushSelectedDisabled}" />
                                        <root:VisualStateSetter Target="SelectionIndicator.Opacity" Value="1" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <Border
                            x:Name="ContentPresenterGrid"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}"
                            MinHeight="{TemplateBinding MinHeight}"
                            Padding="{TemplateBinding primitives:TreeViewItemHelper.Indentation}"
                            SnapsToDevicePixels="true"
                            primitives:PressHelper.IsEnabled="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <Rectangle
                                    x:Name="SelectionIndicator"
                                    Width="3"
                                    Height="16"
                                    Fill="{DynamicResource TreeViewItemSelectionIndicatorForeground}"
                                    Opacity="0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center" />

                                <ToggleButton
                                    x:Name="ExpandCollapseChevron"
                                    Grid.Column="0"
                                    Padding="12,0,12,0"
                                    Width="Auto"
                                    Opacity="{TemplateBinding primitives:TreeViewItemHelper.GlyphOpacity}"
                                    Background="Transparent"
                                    IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource ExpandCollapseToggleStyle}">
                                    <Grid>
                                        <local:FontIconFallback
                                            x:Name="CollapsedGlyph"
                                            Foreground="{TemplateBinding primitives:TreeViewItemHelper.GlyphBrush}"
                                            Width="12"
                                            Height="12"
                                            Visibility="Collapsed"
                                            FontSize="{TemplateBinding primitives:TreeViewItemHelper.GlyphSize}"
                                            Data="{TemplateBinding primitives:TreeViewItemHelper.CollapsedPath}"
                                            FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                            VerticalAlignment="Center"
                                            IsHitTestVisible="False"/>
                                        <local:FontIconFallback
                                            x:Name="ExpandedGlyph"
                                            Foreground="{TemplateBinding primitives:TreeViewItemHelper.GlyphBrush}"
                                            Width="12"
                                            Height="12"
                                            Visibility="Collapsed"
                                            FontSize="{TemplateBinding primitives:TreeViewItemHelper.GlyphSize}"
                                            Data="{TemplateBinding primitives:TreeViewItemHelper.ExpandedPath}"
                                            FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                            VerticalAlignment="Center"
                                            IsHitTestVisible="False"/>
                                    </Grid>
                                </ToggleButton>

                                <local:ContentPresenterEx
                                    x:Name="PART_Header"
                                    Grid.Column="1"
                                    ContentSource="Header"
                                    Foreground="{TemplateBinding Foreground}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Margin="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                        </Border>

                        <ItemsPresenter
                            x:Name="ItemsHost"
                            Grid.Row="1" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="CollapsedGlyph" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ExpandedGlyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="ExpandCollapseChevron" Property="Visibility" Value="Hidden" />
                            <Setter Property="primitives:TreeViewItemHelper.GlyphOpacity" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="true">
                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TreeViewItem" BasedOn="{StaticResource DefaultTreeViewItemStyle}" />

    <Style x:Key="DefaultTreeViewStyle" TargetType="TreeView">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="primitives:FocusVisualHelper.UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeView">
                    <Border
                        x:Name="Bd"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ScrollViewer
                            x:Name="_tv_scrollviewer_"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CanContentScroll="false"
                            Focusable="false"
                            HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="true">
                            <Setter TargetName="_tv_scrollviewer_" Property="CanContentScroll" Value="true" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="true">
                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TreeView" BasedOn="{StaticResource DefaultTreeViewStyle}" />

</ResourceDictionary>
