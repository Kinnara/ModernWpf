<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:root="clr-namespace:ModernWpf"
    xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <sys:Double x:Key="TitleBarHeight">32</sys:Double>

    <Style x:Key="DefaultWindowStyle" TargetType="Window">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource SystemControlPageTextBaseHighBrush}" />
        <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAltHighBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource WindowBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome
                    CaptionHeight="{StaticResource TitleBarHeight}"
                    CornerRadius="0"
                    GlassFrameThickness="1"
                    UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
        <Setter Property="primitives:WindowHelper.IsEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Border
                        x:Name="LayoutRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Border.Resources>
                            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
                            <primitives:WindowPaddingConveter x:Key="WindowPaddingConveter" />
                            <Style x:Key="CloseButtonStyle" TargetType="primitives:CaptionButton">
                                <Setter Property="MouseOverBackground" Value="#E81123" />
                                <Setter Property="MouseOverForeground" Value="{DynamicResource SystemControlBackgroundChromeWhiteBrush}" />
                                <Setter Property="PressedBackground" Value="#F1707A" />
                                <Setter Property="PressedForeground" Value="{DynamicResource SystemControlBackgroundChromeWhiteBrush}" />
                            </Style>
                        </Border.Resources>
                        <Border.Padding>
                            <MultiBinding Converter="{StaticResource WindowPaddingConveter}">
                                <Binding
                                    Path="WindowState"
                                    RelativeSource="{RelativeSource TemplatedParent}" />
                                <Binding
                                    Path="WindowNonClientFrameThickness"
                                    Source="{x:Static root:BindableSystemParameters.Current}" />
                                <Binding
                                    Path="WindowResizeBorderThickness"
                                    Source="{x:Static root:BindableSystemParameters.Current}" />
                                <Binding
                                    Path="WindowCaptionHeight"
                                    Source="{x:Static root:BindableSystemParameters.Current}" />
                            </MultiBinding>
                        </Border.Padding>
                        <Border
                            x:Name="HighContrastBorder"
                            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <Grid Height="{StaticResource TitleBarHeight}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Rectangle
                                        x:Name="HighContrastCaptionBackground"
                                        Grid.ColumnSpan="3"
                                        Fill="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}"
                                        Visibility="Collapsed" />

                                    <Button
                                        x:Name="BackButton"
                                        Width="48"
                                        Height="32"
                                        Content="&#xE830;"
                                        FontSize="12"
                                        IsEnabled="{TemplateBinding primitives:WindowHelper.IsBackEnabled}"
                                        Style="{DynamicResource NavigationBackButtonNormalStyle}"
                                        Visibility="{Binding Path=(primitives:WindowHelper.IsBackButtonVisible), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        WindowChrome.IsHitTestVisibleInChrome="True"
                                        primitives:WindowHelper.IsBackButton="True"
                                        IsTabStop="False" />

                                    <TextBlock
                                        x:Name="Title"
                                        Grid.Column="1"
                                        Margin="12,0,12,0"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Background="{x:Null}"
                                        Foreground="{TemplateBinding Foreground}"
                                        Text="{TemplateBinding Title}"
                                        TextTrimming="CharacterEllipsis">
                                        <TextBlock.RenderTransform>
                                            <TranslateTransform Y="-1" />
                                        </TextBlock.RenderTransform>
                                    </TextBlock>

                                    <StackPanel
                                        Grid.Column="2"
                                        Orientation="Horizontal">
                                        <primitives:CaptionButton
                                            x:Name="MinimizeButton"
                                            Command="{x:Static SystemCommands.MinimizeWindowCommand}"
                                            Content="&#xE921;" />

                                        <primitives:CaptionButton
                                            x:Name="MaximizeRestoreButton"
                                            Command="{x:Static SystemCommands.MaximizeWindowCommand}"
                                            Content="&#xE922;" />

                                        <primitives:CaptionButton
                                            x:Name="CloseButton"
                                            Command="{x:Static SystemCommands.CloseWindowCommand}"
                                            Content="&#xE106;"
                                            Style="{StaticResource CloseButtonStyle}" />
                                    </StackPanel>
                                </Grid>

                                <AdornerDecorator Grid.Row="1">
                                    <ContentPresenter />
                                </AdornerDecorator>
                                <ResizeGrip
                                    x:Name="WindowResizeGrip"
                                    Grid.Row="1"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    IsTabStop="false"
                                    Visibility="Collapsed" />
                            </Grid>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ResizeMode" Value="CanResizeWithGrip" />
                                <Condition Property="WindowState" Value="Normal" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="WindowResizeGrip" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <Trigger Property="IsActive" Value="False">
                            <Setter TargetName="LayoutRoot" Property="BorderBrush" Value="{DynamicResource WindowBorderInactive}" />
                            <Setter TargetName="HighContrastBorder" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.InactiveCaptionBrushKey}}" />
                            <Setter TargetName="HighContrastCaptionBackground" Property="Fill" Value="{DynamicResource {x:Static SystemColors.InactiveCaptionBrushKey}}" />
                            <Setter TargetName="Title" Property="Foreground" Value="{DynamicResource SystemControlForegroundChromeDisabledLowBrush}" />
                            <Setter TargetName="MinimizeButton" Property="Background" Value="Transparent" />
                            <Setter TargetName="MinimizeButton" Property="Foreground" Value="{DynamicResource SystemControlForegroundChromeDisabledLowBrush}" />
                            <Setter TargetName="MaximizeRestoreButton" Property="Background" Value="Transparent" />
                            <Setter TargetName="MaximizeRestoreButton" Property="Foreground" Value="{DynamicResource SystemControlForegroundChromeDisabledLowBrush}" />
                            <Setter TargetName="CloseButton" Property="Background" Value="Transparent" />
                            <Setter TargetName="CloseButton" Property="Foreground" Value="{DynamicResource SystemControlForegroundChromeDisabledLowBrush}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding HighContrast, Source={x:Static root:BindableSystemParameters.Current}}" Value="true">
                            <Setter TargetName="HighContrastBorder" Property="BorderThickness" Value="7,0,7,7" />
                            <Setter TargetName="HighContrastCaptionBackground" Property="Visibility" Value="Visible" />
                            <Setter TargetName="BackButton" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                            <Setter TargetName="Title" Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ActiveCaptionTextBrushKey}}" />
                            <Setter TargetName="CloseButton" Property="Style" Value="{x:Null}" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding HighContrast, Source={x:Static root:BindableSystemParameters.Current}}" Value="True" />
                                <Condition Binding="{Binding IsActive, RelativeSource={RelativeSource Self}}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="Title" Property="Foreground" Value="{DynamicResource {x:Static SystemColors.InactiveCaptionTextBrushKey}}" />
                        </MultiDataTrigger>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="LayoutRoot" Property="BorderThickness" Value="0" />
                            <Setter TargetName="HighContrastBorder" Property="BorderThickness" Value="0" />
                            <Setter TargetName="MaximizeRestoreButton" Property="Content" Value="&#xE923;" />
                            <Setter TargetName="MaximizeRestoreButton" Property="Command" Value="{x:Static SystemCommands.RestoreWindowCommand}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Window" BasedOn="{StaticResource DefaultWindowStyle}" />

</ResourceDictionary>