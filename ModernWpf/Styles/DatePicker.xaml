<!-- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See LICENSE in the project root for license information. -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:root="clr-namespace:ModernWpf"
    xmlns:local="clr-namespace:ModernWpf.Controls"
    xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">

    <Thickness x:Key="CalendarDatePickerTopHeaderMargin">0,0,0,8</Thickness>
    <Thickness x:Key="CalendarDatePickerLeftHeaderMargin">0,0,8,0</Thickness>
    <sys:Double x:Key="CalendarDatePickerLeftHeaderMaxWidth">100</sys:Double>

    <Style x:Key="DefaultDatePickerTextBoxStyle" TargetType="DatePickerTextBox">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource CalendarDatePickerTextForeground}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="ContextMenu" Value="{DynamicResource TextControlContextMenu}" />
        <Setter Property="local:TextContextMenu.UsingTextContextMenu" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePickerTextBox">
                    <Grid SnapsToDevicePixels="True">
                        <Border
                            x:Name="BorderElement"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}" />
                        <Border
                            x:Name="ContentElement"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <ContentControl
                            x:Name="PART_Watermark"
                            Focusable="False"
                            IsHitTestVisible="False"
                            Opacity="0"
                            Foreground="{DynamicResource CalendarDatePickerTextForeground}"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <ScrollViewer
                            x:Name="PART_ContentHost"
                            Style="{DynamicResource TextControlContentHostStyle}"
                            Margin="0"
                            Padding="{TemplateBinding Padding}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="WatermarkStates">
                                <VisualState x:Name="Unwatermarked" />
                                <VisualState x:Name="Watermarked">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentElement"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0"
                                            Duration="0" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_Watermark"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_ContentHost" Property="Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundDisabled}" />
                            <Setter TargetName="PART_Watermark" Property="Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundDisabled}" />
                        </Trigger>
                        <Trigger Property="IsSelectionActive" Value="True">
                            <Setter TargetName="BorderElement" Property="Background" Value="{DynamicResource TextControlBackgroundFocused}" />
                            <Setter TargetName="BorderElement" Property="BorderBrush" Value="{DynamicResource TextControlBorderBrushFocused}" />
                            <Setter TargetName="BorderElement" Property="BorderThickness" Value="{DynamicResource TextControlBorderThemeThicknessFocused}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextControlForegroundFocused}" />
                            <Setter Property="CaretBrush" Value="{DynamicResource TextControlForegroundFocused}" />
                            <Setter TargetName="PART_Watermark" Property="Foreground" Value="{DynamicResource TextControlPlaceholderForegroundFocused}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DatePickerTextBox" BasedOn="{StaticResource DefaultDatePickerTextBoxStyle}" />

    <Style x:Key="DefaultDatePickerStyle" TargetType="DatePicker">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource CalendarDatePickerTextForeground}" />
        <Setter Property="IsTodayHighlighted" Value="True" />
        <Setter Property="SelectedDateFormat" Value="Short" />
        <Setter Property="CalendarStyle" Value="{DynamicResource DatePickerCalendarStyle}" />
        <Setter Property="Background" Value="{DynamicResource CalendarDatePickerBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CalendarDatePickerBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource CalendarDatePickerBorderThemeThickness}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="Padding" Value="{DynamicResource TextControlThemePadding}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
        <Setter Property="primitives:FocusVisualHelper.UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="primitives:FocusVisualHelper.FocusVisualMargin" Value="-3" />
        <Setter Property="primitives:ControlHelper.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="primitives:DatePickerHelper.IsEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePicker">
                    <ControlTemplate.Resources>
                        <StreamGeometry x:Key="Calendar">F1 M 7.5 7.5 L 8.75 7.5 L 8.75 8.75 L 7.5 8.75 Z M 11.25 15 L 12.5 15 L 12.5 16.25 L 11.25 16.25 Z M 15 7.5 L 16.25 7.5 L 16.25 8.75 L 15 8.75 Z M 11.25 7.5 L 12.5 7.5 L 12.5 8.75 L 11.25 8.75 Z M 7.5 10 L 8.75 10 L 8.75 11.25 L 7.5 11.25 Z M 3.75 10 L 5 10 L 5 11.25 L 3.75 11.25 Z M 15 10 L 16.25 10 L 16.25 11.25 L 15 11.25 Z M 11.25 10 L 12.5 10 L 12.5 11.25 L 11.25 11.25 Z M 7.5 12.5 L 8.75 12.5 L 8.75 13.75 L 7.5 13.75 Z M 3.75 12.5 L 5 12.5 L 5 13.75 L 3.75 13.75 Z M 15 12.5 L 16.25 12.5 L 16.25 13.75 L 15 13.75 Z M 11.25 12.5 L 12.5 12.5 L 12.5 13.75 L 11.25 13.75 Z M 7.5 15 L 8.75 15 L 8.75 16.25 L 7.5 16.25 Z M 3.75 15 L 5 15 L 5 16.25 L 3.75 16.25 Z M 20 1.25 L 20 18.75 L 0 18.75 L 0 1.25 L 3.75 1.25 L 3.75 0 L 5 0 L 5 1.25 L 15 1.25 L 15 0 L 16.25 0 L 16.25 1.25 Z M 1.25 2.5 L 1.25 5 L 18.75 5 L 18.75 2.5 L 16.25 2.5 L 16.25 3.75 L 15 3.75 L 15 2.5 L 5 2.5 L 5 3.75 L 3.75 3.75 L 3.75 2.5 Z M 18.75 17.5 L 18.75 6.25 L 1.25 6.25 L 1.25 17.5 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    <Grid x:Name="Root" SnapsToDevicePixels="True">
                        <VisualStateManager.CustomVisualStateManager>
                            <root:VisualStateManagerEx />
                        </VisualStateManager.CustomVisualStateManager>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <root:VisualStateEx x:Name="PointerOver">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="Background.Background" Value="{DynamicResource CalendarDatePickerBackgroundPointerOver}" />
                                        <root:VisualStateSetter Target="Background.BorderBrush" Value="{DynamicResource CalendarDatePickerBorderBrushPointerOver}" />
                                        <root:VisualStateSetter Target="PART_TextBox.Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundPointerOver}" />
                                        <root:VisualStateSetter Target="PART_Button.Foreground" Value="{DynamicResource CalendarDatePickerCalendarGlyphForegroundPointerOver}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Pressed">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="Background.Background" Value="{DynamicResource CalendarDatePickerBackgroundPressed}" />
                                        <root:VisualStateSetter Target="Background.BorderBrush" Value="{DynamicResource CalendarDatePickerBorderBrushPressed}" />
                                        <root:VisualStateSetter Target="PART_TextBox.Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundPressed}" />
                                        <root:VisualStateSetter Target="PART_Button.Foreground" Value="{DynamicResource CalendarDatePickerCalendarGlyphForegroundPressed}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                                <root:VisualStateEx x:Name="Disabled">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="Background.Background" Value="{DynamicResource CalendarDatePickerBackgroundDisabled}" />
                                        <root:VisualStateSetter Target="Background.BorderBrush" Value="{DynamicResource CalendarDatePickerBorderBrushDisabled}" />
                                        <root:VisualStateSetter Target="HeaderContentPresenter.Foreground" Value="{DynamicResource CalendarDatePickerHeaderForegroundDisabled}" />
                                        <root:VisualStateSetter Target="PART_TextBox.Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundDisabled}" />
                                        <root:VisualStateSetter Target="PART_Button.Foreground" Value="{DynamicResource CalendarDatePickerCalendarGlyphForegroundDisabled}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Unfocused" />
                                <VisualState x:Name="PointerFocused" />
                                <VisualState x:Name="Focused" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualState x:Name="Unselected" />
                                <root:VisualStateEx x:Name="Selected">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="PART_TextBox.Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundSelected}" />
                                        <root:VisualStateSetter Target="PART_Button.Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundSelected}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="HeaderStates">
                                <VisualState x:Name="TopHeader" />
                                <root:VisualStateEx x:Name="LeftHeader">
                                    <root:VisualStateEx.Setters>
                                        <root:VisualStateSetter Target="HeaderContentPresenter.(Grid.Row)" Value="1" />
                                        <root:VisualStateSetter Target="HeaderContentPresenter.(Grid.Column)" Value="0" />
                                        <root:VisualStateSetter Target="HeaderContentPresenter.(Grid.ColumnSpan)" Value="1" />
                                        <root:VisualStateSetter Target="HeaderContentPresenter.Margin" Value="{StaticResource CalendarDatePickerLeftHeaderMargin}" />
                                        <root:VisualStateSetter Target="HeaderContentPresenter.MaxWidth" Value="{StaticResource CalendarDatePickerLeftHeaderMaxWidth}" />
                                    </root:VisualStateEx.Setters>
                                </root:VisualStateEx>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <local:ContentPresenterEx
                            x:Name="HeaderContentPresenter"
                            Grid.Column="1"
                            Grid.ColumnSpan="2"
                            Visibility="{TemplateBinding primitives:ControlHelper.HeaderVisibility}"
                            Content="{TemplateBinding primitives:ControlHelper.Header}"
                            ContentTemplate="{TemplateBinding primitives:ControlHelper.HeaderTemplate}"
                            Foreground="{DynamicResource CalendarDatePickerHeaderForeground}"
                            Margin="{StaticResource CalendarDatePickerTopHeaderMargin}"
                            TextWrapping="Wrap"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <Grid
                            x:Name="PART_Root"
                            Grid.Row="1"
                            Grid.Column="1"
                            Grid.ColumnSpan="2">
                            <Grid.Resources>
                                <ControlTemplate x:Key="DropDownButtonTemplate" TargetType="Button">
                                    <Grid>
                                        <Rectangle
                                            Fill="{TemplateBinding Background}"
                                            Margin="{TemplateBinding Padding}" />
                                        <local:FontIconFallback
                                            Foreground="{TemplateBinding Foreground}"
                                            FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                            FontSize="12"
                                            Data="{StaticResource Calendar}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center" />
                                    </Grid>
                                </ControlTemplate>
                            </Grid.Resources>
                            <Border
                                x:Name="Background"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}"
                                MinHeight="{DynamicResource ComboBoxMinHeight}" />
                            <DatePickerTextBox
                                x:Name="PART_TextBox"
                                Foreground="{TemplateBinding Foreground}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{DynamicResource ComboBoxEditableTextPadding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                Focusable="{TemplateBinding Focusable}"
                                primitives:ControlHelper.CornerRadius="{TemplateBinding primitives:ControlHelper.CornerRadius}" />
                            <Button
                                x:Name="PART_Button"
                                Focusable="False"
                                Background="Transparent"
                                Foreground="{DynamicResource CalendarDatePickerCalendarGlyphForeground}"
                                Width="30"
                                Padding="0,1,1,1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Stretch"
                                Template="{StaticResource DropDownButtonTemplate}" />
                            <Popup
                                x:Name="PART_Popup"
                                AllowsTransparency="True"
                                Placement="Bottom"
                                StaysOpen="False">
                                <Popup.PlacementRectangle>
                                    <MultiBinding>
                                        <MultiBinding.Converter>
                                            <primitives:PlacementRectangleConverter Margin="0,-4" />
                                        </MultiBinding.Converter>
                                        <Binding ElementName="PART_Root" Path="ActualWidth" />
                                        <Binding ElementName="PART_Root" Path="ActualHeight" />
                                    </MultiBinding>
                                </Popup.PlacementRectangle>
                            </Popup>
                        </Grid>
                        <local:ContentPresenterEx
                            x:Name="DescriptionPresenter"
                            Grid.Row="2"
                            Grid.Column="1"
                            Grid.ColumnSpan="2"
                            Content="{TemplateBinding primitives:ControlHelper.Description}"
                            Visibility="{TemplateBinding primitives:ControlHelper.DescriptionVisibility}"
                            Foreground="{DynamicResource SystemControlDescriptionTextForegroundBrush}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DatePicker" BasedOn="{StaticResource DefaultDatePickerStyle}" />

</ResourceDictionary>
